{"metadata":{"kernelspec":{"display_name":"Python 3","language":"python","name":"python3"},"language_info":{"name":"python","version":"3.12.13","mimetype":"text/x-python","codemirror_mode":{"name":"ipython","version":3},"pygments_lexer":"ipython3","nbconvert_exporter":"python","file_extension":".py"}},"nbformat_minor":4,"nbformat":4,"cells":[{"cell_type":"markdown","source":"# trace2 -- raw data in, trained checkpoint out\n\nSame pipeline as `trace`, with every enhancement from the review slate implemented **behind\na flag in Cell 1**. With Cell 1 untouched, trace2 renders and trains the *same function* as\ntrace: all prompt- and model-changing flags default OFF, and only tooling, measurement and\nrobustness are on. That makes the default run a valid **control arm**.\n\n**Flip one flag at a time.** Settle each one with `COMPARE_OOF`, which does a paired,\nper-row comparison of two OOF files. The level standard errors quoted in Cell 6 (0.0054 on\none fold) are the wrong yardstick for a *difference* and are several times too large.\n\n**Nothing truncates or cuts text.** The whole raw transcript is rendered. The only content\never removed is a line whose entire body is an `[unclear]` marker (`DROP_UNCLEAR_ONLY`).\nToken-space truncation at `MAX_LEN` is the single length control and touches ~1-2% of rows.\n\n| cell | what |\n|---|---|\n| 1 | CONFIG -- every flag, with its measured expected value |\n| 2-4 | offline install, GPU check, locate data |\n| 5 | render: transcript + objective -> string (mirrors `src/context.py`) |\n| 6 | cross-validation schemes |\n| 7 | build the cached training table; render-keyed cache; schema and session-meta checks |\n| 8 | model wrapper, pooling, checkpoint I/O, render fixtures, inference cost model |\n| 9 | dataset / collate |\n| 10 | build the LoRA model |\n| 12 | family sanity check |\n| 13 | **TOKEN_AUDIT + MICRO_BENCH** -- measure chars/token and ms/step before committing |\n| 14 | train, OOF dump, calibration, blend |\n| 15 | **inference contract** -- order restoration, fallbacks, submission writer |\n| 16 | zip the artefacts |\n\n**Read Cell 13 first.** Every wall-clock number in this notebook is a FLOP estimate at an\nassumed 40% MFU, and every length number rests on a `chars/3.6` proxy no tokenizer has ever\nconfirmed. Cell 13 replaces both with measurements, for about two minutes of GPU.\n\n**Keep Cell 5 identical to `src/context.py`.** Cell 8 freezes fixtures of its output into\nevery checkpoint and `load_checkpoint` re-runs them, so drift now raises instead of quietly\ncosting score -- but the fixtures only catch it, they cannot fix it.\n","metadata":{"_uuid":"bc3a566a-76c3-4643-afc5-2e7ef025295c","_cell_guid":"e7f20d23-b4a2-4791-b48a-7c79daf54451","trusted":true,"collapsed":false,"jupyter":{"outputs_hidden":false}}},{"cell_type":"code","source":"# =========================================================\n# Cell 1 - CONFIG  (everything you tune lives here)\n# =========================================================\n# trace2. Every enhancement from the review slate is implemented behind a flag below.\n#\n# DEFAULTS ARE DELIBERATE. With this file untouched, trace2 trains the SAME FUNCTION as\n# trace: every flag that changes the prompt string or the trained model is OFF, and only\n# tooling, measurement and robustness are ON. That makes the default run a valid control\n# arm. Flip ONE model/prompt flag at a time and settle it with COMPARE_OOF, which does a\n# paired per-row comparison -- the level SEs quoted in Cell 6 are the wrong yardstick for\n# a difference and are several times too large.\n#\n# NOTHING TRUNCATES OR CUTS TEXT. The whole raw transcript is rendered. The only content\n# ever removed is a line whose entire body is an [unclear] marker (DROP_UNCLEAR_ONLY).\n# Token-space truncation at MAX_LEN is the single length control and touches ~1-2% of rows.\n\n# ---- data / rendering: these CHANGE THE PROMPT STRING ------------------------------------\n# Set the same values in src/context.py and build_submission.py. Cell 7's parquet cache and\n# Cell 8's fixture block are both keyed on them, so a mismatch is caught rather than silently\n# scored. Flags marked [trace] shipped in trace and stay on; the rest default OFF.\nSPLIT_SPEAKER_TAGS  = True   # [trace] split utterances on [Speaker:Name]/[Speaker] markers\n                             # instead of deleting them. 60.6% of sessions carry a marker;\n                             # 1.78% of all words are otherwise printed under the wrong tag.\nDROP_UNCLEAR_ONLY   = True   # [trace] drop lines whose ENTIRE body is an [unclear] marker.\n                             # Lines where [unclear] sits among real words are kept verbatim.\n                             # This is the ONLY content removal in the pipeline.\nADD_SESSION_META    = True   # [trace] footer line stating how many objectives the lesson\n                             # covered. Measured +0.0098 standalone, session-grouped folds.\n# --- E20 / E21 / E22 / E35: new, all OFF so the default run is a control ------------------\nSHORT_UNCLEAR       = False  # E20. Render the [unclear] marker as UNCLEAR_GLYPH instead of\n                             # the 3-token literal. Removes NO content -- it is a shorter\n                             # spelling of the same marker. Est -2.5 to -3.7% of prompt\n                             # tokens; verify with TOKEN_AUDIT before trusting that.\nUNCLEAR_GLYPH       = \"~\"    # deliberately NOT \"...\" -- ASCII \"...\" already appears ~9.7x\n                             # per session as a genuine transcribed pause, and reusing it\n                             # would collapse two different signals into one symbol.\nMERGE_SPEAKER_RUNS  = False  # E21b. Merge consecutive same-speaker lines into one line.\n                             # -1.75% chars. Costs turn granularity: 17.4% of lines are\n                             # continuations, and turn COUNT is a real signal, so this\n                             # fights ADD_PRODUCTION_META. Do not enable both blind.\nADD_PRODUCTION_META = False  # E22. Footer line with the student's turn count and long-turn\n                             # count. Measured scalar ceiling +0.0013, realistic +0.0006.\nOBJ_MENTION_META    = False  # E35. Footer line counting how many of the student's OWN turns\n                             # contain a content word from the objective. Measured +0.0042\n                             # marginal over n_resp alone, but only +0.0007 on top of a\n                             # per-objective prior, which the model likely learns anyway.\n                             # NOTE: this is keyword matching. It SELECTS AND REMOVES\n                             # NOTHING -- the whole transcript still renders verbatim; it\n                             # only states a count in the footer.\n\n# ---- model --------------------------------------------------------------------------------\nMODEL       = \"/kaggle/input/models/qwen-lm/qwen-3/transformers/8b/1\"\nMAX_LEN     = 8192     # tokens kept, from the END. Median row is ~5,500 est tokens and only\n                       # ~1.2% of rows reach this cap. MAX_LEN=6144 would cost 0.96x the\n                       # compute while truncating 31.8%, so lowering it is NOT a compute\n                       # lever here -- most rows are simply shorter than the cap.\nPREPEND_SPECIALS = True  # keep whatever prefix the tokenizer adds (GLM prepends\n                         # [gMASK]<sop>, Qwen prepends nothing) when left-truncating.\nUSE_4BIT    = False    # bf16 LoRA. Training in NF4 and merging into bf16 at inference\n                       # optimises one function and evaluates another.\nLORA_R      = 16\nLORA_ALPHA  = 32       # alpha/r = 1.0, so LR means what it says.\nDROPOUT     = 0.05\nLORA_TOP_N  = 0        # E24. 0 = LoRA on every layer (current). N > 0 = only the TOP N\n                       # decoder layers get adapters. Est a further 0.75x on top of\n                       # GRAD_CKPT=False, but this is THE item on the slate most able to\n                       # cost score: the lower layers stop adapting entirely. Only worth it\n                       # once MICRO_BENCH has told you what a step actually costs.\nHEAD_BIAS_INIT = True  # head starts at weight=0, bias=logit(train base rate), i.e. exactly\n                       # at the constant predictor (0.60876).\nPOOL        = \"last\"   # E26. \"last\" (current) | \"last+mean\". last+mean concatenates the\n                       # mean over non-pad positions to the last-token state and doubles the\n                       # head's input width. Speculative, +0.000 to +0.0015 -- at or below\n                       # this harness's paired detection floor. Changes the head shape, so a\n                       # checkpoint trained under one value cannot load under the other.\nUSE_CHAT_TEMPLATE = False  # E25. Wrap the prompt in the model's chat template and read out\n                           # at the generation prompt instead of the raw last token.\n                           # Speculative. Real risk: the template is a property of the\n                           # tokenizer files, so a different base-model snapshot at\n                           # inference silently changes the readout position.\n\n# ---- training -----------------------------------------------------------------------------\nFOLD        = 0        # 0..4, or -1 to train on everything\nSEED        = 42       # the ONE seed. Single-seed runs only; folds follow it directly.\nEPOCHS      = 3.0      # E23. A 1-epoch run is 3x cheaper and buys 3 experimental arms.\n                       # SNAPSHOT_EPOCH1 below answers \"did epochs 2-3 help?\" from a single\n                       # 3-epoch run for ~1.8% extra wall clock -- read that first, then\n                       # decide. If you cut, cut EVAL cadence (EVAL_EVERY), NOT EVAL_SUBSET:\n                       # 2000 -> 1500 saves ~0.15 h and costs SE 0.0101 -> 0.0117.\nBS          = 2\nACCUM       = 8\nGRAD_CKPT   = True     # E4. False trades memory for ~1.4-1.5x throughput. LoRA does not\n                       # need checkpointing the way full fine-tuning does, and bf16 Qwen3-8B\n                       # weights are <30 GB. Run MICRO_BENCH first: it reports peak GB and\n                       # ms/step, so you can see whether False fits before committing hours.\nEVAL_BS     = 4\nEVAL_SUBSET = 0     # 0 = whole fold. 2000 -> one-fold SE ~0.0101. The FINAL OOF always\n                       # covers the full fold regardless.\nGROUP_BY_LENGTH  = True  # bucket similar-length TRAINING rows into a batch.\nSORT_EVAL_BY_LENGTH = True  # E10. Split out from GROUP_BY_LENGTH so the two can move\n                            # independently. This one sorts the eval/OOF frames by\n                            # descending length, which is worth ~24.5% of every eval pass\n                            # and is what makes SequentialSampler safe to use there.\nLR          = 1e-4\nHEAD_LR     = 1e-3\nWEIGHT_DECAY = 0.01    # not applied to head.bias -- it would fight HEAD_BIAS_INIT\nWARMUP_RATIO = 0.03\nMAX_GRAD_NORM = 1.0\nWORKERS     = 2\nFORCE_SINGLE_GPU = True\nTIME_BUDGET_MIN = 0    # E31. 0 = off. N > 0 stops training cleanly after N minutes, saving\n                       # a checkpoint you can RESUME from. Only useful on a box with a\n                       # session cap (Kaggle's 12 h); pointless on a dedicated card.\n                       # A budget-stopped checkpoint sits mid-cosine at a high LR and is NOT\n                       # a fair read of the endpoint -- only compare completed runs.\nPAIR_LOSS_W = 0.0      # E34. 0 = off. Weight on a pairwise rank loss over discordant\n                       # siblings (same session, opposite labels -- 5,323 such pairs share\n                       # ~99.5% of their prompt characters, so they are the only place the\n                       # objective channel is directly supervised). Cap at 0.1: a rank loss\n                       # is not a proper scoring rule and log loss is.\n                       # WARNING: without PAIR_SIBLING_BATCH this term is nearly inert --\n                       # LengthGroupedSampler puts a sibling in the same micro-batch only\n                       # ~1.4% of the time. Cell 14 prints the realised pair count per epoch.\nPAIR_SIBLING_BATCH = False  # E34. Force siblings into the same micro-batch. Replaces the\n                            # length-grouped sampler, so it changes the batch composition\n                            # regime as well as adding a loss term -- two changes at once.\nSWA_LAST_K  = 0        # E32. 0 = off. K > 0 averages the last K adapter checkpoints in\n                       # delta-weight space (stacking A and B, NOT averaging them\n                       # separately -- B_avg @ A_avg != mean(B_i A_i)). Free at inference\n                       # because averaged weights merge into one model. Speculative.\n\n# ---- measurement / tooling (all cheap, all default ON) ------------------------------------\nTOKEN_AUDIT   = True   # E2. Tokenise a sample with the REAL tokenizer and report chars/token\n                       # against the 3.6 proxy every length decision here rests on.\nMICRO_BENCH   = True   # E3. 20 real training steps -> ms/step, tok/s, peak GB, projected\n                       # h/epoch. ~2 minutes of GPU that turns every compute estimate on the\n                       # slate into a measurement.\nMICRO_BENCH_STEPS = 20\nMICRO_BENCH_MAX_SEC = 180  # hard wall-clock cap. A step is a real training step, so the\n                       # benchmark costs real time: 23 steps of Qwen3-8B at ~5,500 tokens\n                       # is ~113 PFLOP, which is ~12 min on a Blackwell 96 GB, ~15 min on\n                       # an A100, and ~96 min on a Kaggle T4. The cap stops whenever it is\n                       # reached and projects from however many steps completed -- 5 steps\n                       # measure ms/step nearly as well as 20.\nNEUTRALISE_PEFT_BACKENDS = True  # PEFT probes every quantisation backend (torchao, awq,\n                       # gptq, eetq, aqlm) while building each LoRA layer, and those probes\n                       # RAISE on a version mismatch instead of returning False. The Kaggle\n                       # image ships torchao 0.10.0 against PEFT's >=0.16.0 floor, which\n                       # kills get_peft_model on the first module -- for a backend this\n                       # pipeline never uses. True stubs only the probes that raise.\nSTRICT_ATTN   = True   # E12. Fail loudly if attn_implementation=\"sdpa\" is rejected and the\n                       # backbone silently falls back to eager attention.\nCHECK_ROLES   = True   # E13. Count unseen role values and assert the transcript columns.\n                       # Provably a no-op on all training data; this is detection for test.\nCHECK_SESSION_META = True  # E14. Verify the feature table lists every row of each session,\n                           # which is what ADD_SESSION_META's footer number depends on.\nSNAPSHOT_EPOCH1 = True # E15. Dump a full OOF at the end of epoch 1 as well as at the end.\n                       # Costs ~1.8% wall clock and answers \"were epochs 2-3 worth 2/3 of\n                       # the run?\" with a paired comparison instead of a guess.\nCALIB_GROUPED_CV = True  # E16. Session-group the inner CV inside calibration_report, so the\n                         # reported calibration gain is not inflated by siblings straddling\n                         # the inner split.\nSHIP_CALIBRATION = True  # E17. Write the fitted Platt (a, b) into the adapter directory, so\n                         # inference applies the same map the OOF was scored under.\nOOF_JOIN_KEYS = True   # E8. Widen the OOF csv with session_id, learning_objective_id and the\n                       # scalar meta columns. Precondition for any blend layer.\nBLEND_SCALARS = True   # E27. Fit a small logistic blend of [llm_logit, scalars] on the OOF\n                       # and REPORT what it buys. No retraining, no effect on the model.\nBLEND_MIN_ROWS = 2000  # E18. Refuse to fit a blend on fewer rows than this. One fold of\n                       # ~7,014 rows is enough for a 2-5 parameter blend: fitting on 28,000\n                       # instead moves it by 0.0002-0.0004, so running folds 1-4 to\n                       # \"stabilise the blend\" is ~216 GPU-h for nothing.\nWRITE_BLEND_JSON = True  # E28. Emit blend.json + a round-trip assert, so build_submission\n                         # cannot apply a different transform than the one that was fitted.\nRENDER_FIXTURES = True   # E19. Freeze a handful of (input -> expected string) fixtures into\n                         # every checkpoint. build_submission re-runs them and refuses to\n                         # score if its render() disagrees. This is the mechanical guard for\n                         # the train/inference contract Cell 0 warns about.\nCALIBRATE_OOF = True   # Newton-fitted 2-parameter Platt map on the OOF, reported out-of-fold.\nCOMPARE_OOF   = \"\"     # path to another oof_*.csv -> paired per-row loss difference with a\n                       # bootstrap CI. The only honest way to call an A/B here.\n\n# ---- inference / submission contract (consumed by Cell 16 + build_submission.py) ----------\nINFER_SORT_BY_LENGTH = True  # E9. Length-sorted batching at inference: -19.7% at EVAL_BS=4.\nINFER_ORDER_CANARY   = True  # E1. MUST be on whenever INFER_SORT_BY_LENGTH is. Re-scores 64\n                             # rows unsorted at batch_size=1 and asserts the logits match.\n                             # A full permutation of the submission costs +0.117 and has NO\n                             # symptom -- right shape, right row count, right value range.\nINFER_SAFE_FALLBACK  = True  # E5. Never raise on the inference path. A row that cannot be\n                             # rendered or scored falls back to the base rate and is counted.\n                             # Crashing before any file exists is the one irreversible\n                             # failure mode in this pipeline.\nSUBMIT_CLIP   = 0.01   # E11. Clip submitted probabilities to [CLIP, 1-CLIP]. Caps the cost\n                       # of a single broken row at 0.00044 instead of 0.00131. 0 disables.\nINFER_COST_MODEL = True  # E6. Derive infer_min from the real token distribution instead of\n                         # the registry's static numbers, which assume max_len 1792 and are\n                         # ~2.9x optimistic against a real mean of ~5,455 tokens.\nDRY_RUN_N     = 0      # E7. N > 0 -> build_submission scores only N rows and extrapolates\n                       # the wall clock. Collapses a 95-158 min planning band into a\n                       # measurement before you spend the 6 h submission window.\n\n# ---- proxy harness -------------------------------------------------------------------------\nPROXY_MODE  = False    # E30. True swaps in PROXY_MODEL and trains ALL folds. A 1.7B model\n                       # across 5 folds costs about what one 8B fold costs and gives a\n                       # ~2.24x tighter paired CI, so prompt/loss A/Bs get decided on the\n                       # proxy and only the winner is confirmed at 8B. The risk is transfer\n                       # failure: a change that helps 1.7B need not help 8B. Use it to\n                       # RANK, never to decide the final number.\nPROXY_MODEL = \"/kaggle/input/models/qwen-lm/qwen-3/transformers/1.7b/1\"\nif PROXY_MODE:\n    MODEL = PROXY_MODEL\n\nSAVE_TOTAL_LIMIT = 1\nSAVE_ONLY_MODEL  = False\nLOAD_BEST        = False\nRESUME           = '/kaggle/input/datasets/mohamed3abdelrazik/trsce-second-run/out/8b_Cp_f0_s42/checkpoint-439'   # True, \"auto\", or a \"checkpoint-1234\" path\nLOG_WITH_EVAL    = False\nLOGGING_STEPS    = 25\n\n# ---- cross-validation: \"A\" | \"B\" | \"C\" | \"C+\" | \"D\" | \"E\" --------------------------------\n# Sessions are held intact by A/B/C/C+; D and E hold objectives intact. Objective overlap\n# across folds is NOT treated as leakage here -- it matches deployment -- so the\n# session-grouped schemes are the working default.\n#\n#   code  splitter                                     sess   obj    label    probe\n#                                                      leak   TVD    spread   spread\n#   A     GroupKFold(session)                            0%   0.057  0.0095   0.01497\n#   B     StratifiedGroupKFold(session, y=label)         0%   0.057  0.0000   0.00716\n#   C     StratifiedGroupKFold(session, y=objective)     0%   0.010  0.0155   0.01662\n#   C+    StratifiedGroupKFold(session, y=obj x label)   0%   0.014  0.0032   0.00206\n#   D     StratifiedGroupKFold(objective, y=label)      51%   0.800  0.0182   0.01586\n#   E     purged: objective folds + session purge        0%   0.800  0.0000   n/a\n#\n# Those numbers are the notebook's own and have NOT been re-measured. The base rate\n# (0.70247), the constant-predictor log loss (0.60876) and the 93.6% connected component\n# HAVE been re-derived from the CSVs and are correct.\nCV_SCHEME  = \"C+\"   # \"C+\" romsnis denmsrk finland norway sweden cezichia south africa morrocco brazil mexico\nN_SPLITS   = 5\nCOMPARE_SCHEMES = False\n_SCHEME_INFO = {\n    \"A\":  (\"fold_A\",  False, False, True),\n    \"B\":  (\"fold_B\",  False, False, True),\n    \"C\":  (\"fold_C\",  False, False, True),\n    \"C+\": (\"fold_Cp\", False, False, True),\n    \"D\":  (\"fold_D\",  False, True,  False),\n    \"E\":  (\"fold_E\",  True,  False, False),\n}\nassert CV_SCHEME in _SCHEME_INFO, f\"CV_SCHEME must be one of {tuple(_SCHEME_INFO)}\"\n(FOLD_COL, USE_PURGE, EXPECT_SESSION_LEAK,\n EXPECT_OBJECTIVE_LEAK) = _SCHEME_INFO[CV_SCHEME]\n_SCHEME_TAG = CV_SCHEME.replace(\"+\", \"p\")\nassert -1 <= FOLD < N_SPLITS, f\"FOLD must be -1 or 0..{N_SPLITS-1}\"\n\n# ---- cadence: \"epoch\" | \"steps\" | \"no\" ----------------------------------------------------\n# BOTH strategies take *_EVERY as a FRACTION OF AN EPOCH: 0.25 -> four times per epoch.\n# Deriving the step count from steps_per_epoch is the point -- a hand-typed absolute step\n# count does not divide total_steps, and the remainder is the tail of the cosine schedule.\nEVAL_STRATEGY = \"steps\"\nEVAL_EVERY    = 0.25\nSAVE_STRATEGY = \"steps\"\nSAVE_EVERY    = 0.25\nEVAL_ON_FINAL_STEP = True\n\n_MODEL_TAG = next((p for p in reversed(MODEL.rstrip(\"/\").split(\"/\"))\n                   if p and not p.isdigit()), \"model\")\nOUT = f\"/kaggle/working/out/{_MODEL_TAG}_{_SCHEME_TAG}_f{FOLD}_s{SEED}\"\n\n# ---- validation ---------------------------------------------------------------------------\n_STRATEGIES = (\"epoch\", \"steps\", \"no\")\nassert EVAL_STRATEGY in _STRATEGIES, f\"EVAL_STRATEGY must be one of {_STRATEGIES}\"\nassert SAVE_STRATEGY in _STRATEGIES, f\"SAVE_STRATEGY must be one of {_STRATEGIES}\"\nfor _n, _s, _e in ((\"EVAL_EVERY\", EVAL_STRATEGY, EVAL_EVERY),\n                   (\"SAVE_EVERY\", SAVE_STRATEGY, SAVE_EVERY)):\n    if _s != \"no\":\n        assert float(_e) > 0, f\"{_n} must be > 0 (it is a fraction of an epoch)\"\nassert isinstance(EVAL_SUBSET, int) and EVAL_SUBSET >= 0\nassert isinstance(EVAL_BS, int) and EVAL_BS > 0\nassert POOL in (\"last\", \"last+mean\"), 'POOL must be \"last\" or \"last+mean\"'\nassert 0.0 <= PAIR_LOSS_W <= 0.1, \"PAIR_LOSS_W must be in [0, 0.1] -- log loss is the metric\"\nassert isinstance(LORA_TOP_N, int) and LORA_TOP_N >= 0\nassert isinstance(SWA_LAST_K, int) and SWA_LAST_K >= 0\nassert 0.0 <= SUBMIT_CLIP < 0.5\nassert not (INFER_SORT_BY_LENGTH and not INFER_ORDER_CANARY), (\n    \"INFER_SORT_BY_LENGTH reorders the submission; INFER_ORDER_CANARY is what proves the \"\n    \"order was restored. A silent permutation costs +0.117 and has no symptom.\")\nif PAIR_LOSS_W > 0 and not PAIR_SIBLING_BATCH:\n    print(\"WARNING: PAIR_LOSS_W > 0 without PAIR_SIBLING_BATCH -- siblings land in the same \"\n          \"micro-batch only ~1.4% of the time, so the term is nearly inert.\")\nif MERGE_SPEAKER_RUNS and ADD_PRODUCTION_META:\n    print(\"WARNING: MERGE_SPEAKER_RUNS destroys the turn granularity that \"\n          \"ADD_PRODUCTION_META reports. Enable one or the other, not both, until you have \"\n          \"A/B'd them separately.\")\n\n# ---- banner -------------------------------------------------------------------------------\n_RENDER_FLAGS = {\"split_speaker_tags\": SPLIT_SPEAKER_TAGS,\n                 \"drop_unclear_only\": DROP_UNCLEAR_ONLY,\n                 \"short_unclear\": SHORT_UNCLEAR, \"unclear_glyph\": UNCLEAR_GLYPH,\n                 \"merge_speaker_runs\": MERGE_SPEAKER_RUNS,\n                 \"add_session_meta\": ADD_SESSION_META,\n                 \"add_production_meta\": ADD_PRODUCTION_META,\n                 \"obj_mention_meta\": OBJ_MENTION_META}\n_MODEL_FLAGS = {\"pool\": POOL, \"use_chat_template\": USE_CHAT_TEMPLATE,\n                \"lora_top_n\": LORA_TOP_N, \"grad_ckpt\": GRAD_CKPT,\n                \"use_4bit\": USE_4BIT, \"lora_r\": LORA_R, \"lora_alpha\": LORA_ALPHA,\n                \"head_bias_init\": HEAD_BIAS_INIT, \"pair_loss_w\": PAIR_LOSS_W,\n                \"pair_sibling_batch\": PAIR_SIBLING_BATCH, \"swa_last_k\": SWA_LAST_K}\n_NON_CONTROL = [k for k, v in _RENDER_FLAGS.items()\n                if k in (\"short_unclear\", \"merge_speaker_runs\", \"add_production_meta\",\n                         \"obj_mention_meta\") and v]\n_NON_CONTROL += [k for k, v in _MODEL_FLAGS.items()\n                 if (k == \"pool\" and v != \"last\") or (k == \"use_chat_template\" and v)\n                 or (k == \"lora_top_n\" and v) or (k == \"pair_loss_w\" and v)\n                 or (k == \"swa_last_k\" and v)]\n\nprint(\"output ->\", OUT)\nprint(f\"cv     : {CV_SCHEME} ({FOLD_COL}, {N_SPLITS} splits, purge={USE_PURGE})  \"\n      f\"fold {FOLD}  seed {SEED}\" + (\"   [PROXY MODE]\" if PROXY_MODE else \"\"))\nprint(f\"render : \" + \"  \".join(f\"{k}={v}\" for k, v in _RENDER_FLAGS.items() if k != \"unclear_glyph\"))\nprint(f\"model  : \" + \"  \".join(f\"{k}={v}\" for k, v in _MODEL_FLAGS.items()))\nprint(f\"eval   : {EVAL_STRATEGY}/{EVAL_EVERY} epoch  save {SAVE_STRATEGY}/{SAVE_EVERY} epoch  \"\n      f\"subset {EVAL_SUBSET or 'full'}  load_best={LOAD_BEST}  final_step={EVAL_ON_FINAL_STEP}\")\nprint(f\"tooling: token_audit={TOKEN_AUDIT} micro_bench={MICRO_BENCH} snapshot_e1={SNAPSHOT_EPOCH1} \"\n      f\"blend={BLEND_SCALARS} fixtures={RENDER_FIXTURES}\")\nprint(f\"infer  : sort={INFER_SORT_BY_LENGTH} canary={INFER_ORDER_CANARY} \"\n      f\"fallback={INFER_SAFE_FALLBACK} clip={SUBMIT_CLIP} dry_run={DRY_RUN_N or 'off'}\")\nif _NON_CONTROL:\n    print(\"\\n*** THIS IS NOT A CONTROL ARM. Non-default flags: \"\n          + \", \".join(sorted(set(_NON_CONTROL))))\n    print(\"    Compare it against a default run with COMPARE_OOF, not against the \"\n          \"leaderboard or a remembered number.\")\nelse:\n    print(\"\\ncontrol arm: renders and trains the same function as trace. Only tooling is on.\")\n","metadata":{"_uuid":"3bfc50bb-5f22-4149-a457-3e876ae353bf","_cell_guid":"ddc3d6cf-5a81-44e0-8afb-a8006ff3ac01","trusted":true,"collapsed":false,"jupyter":{"outputs_hidden":false},"execution":{"iopub.status.busy":"2026-08-13T11:40:34.041745Z","iopub.execute_input":"2026-08-13T11:40:34.041919Z","iopub.status.idle":"2026-08-13T11:40:34.055194Z","shell.execute_reply.started":"2026-08-13T11:40:34.041906Z","shell.execute_reply":"2026-08-13T11:40:34.054992Z"}},"outputs":[],"execution_count":null},{"cell_type":"code","source":"# =========================================================\n# Cell 2 - OFFLINE install from the wheels dataset\n# =========================================================\nimport os, sys, subprocess, glob\nif FORCE_SINGLE_GPU:\n    os.environ[\"CUDA_VISIBLE_DEVICES\"] = \"0\"     # must happen before torch is imported\nos.environ[\"HF_HUB_OFFLINE\"]         = \"1\"\nos.environ[\"TRANSFORMERS_OFFLINE\"]   = \"1\"\nos.environ[\"HF_DATASETS_OFFLINE\"]    = \"1\"\nos.environ[\"TOKENIZERS_PARALLELISM\"] = \"false\"\nWHEELS = \"/kaggle/input/trace-ace-wheels/wheels\"\nif not glob.glob(os.path.join(WHEELS, \"*.whl\")):\n    hits = glob.glob(\"/kaggle/input/**/*.whl\", recursive=True)\n    if hits:\n        WHEELS = os.path.dirname(sorted(hits, key=len)[0])\nn_wheels = len(glob.glob(os.path.join(WHEELS, \"*.whl\")))\nprint(\"wheels dir:\", WHEELS, \" found:\", n_wheels)\nassert n_wheels, \"no wheels found -- attach the wheels dataset\"\n\ndef stream(cmd, tail=0):\n    \"\"\"Run a subprocess and print output AS IT HAPPENS, so nothing looks hung.\"\"\"\n    p = subprocess.Popen(cmd, stdout=subprocess.PIPE, stderr=subprocess.STDOUT,\n                         text=True, bufsize=1)\n    lines = []\n    for line in p.stdout:\n        lines.append(line)\n        if not tail:\n            print(line, end=\"\")\n    p.wait()\n    if tail:\n        print(\"\".join(lines[-tail:]), end=\"\")\n    return p.returncode\n\nREQUIRED = [\"torch\", \"transformers\", \"peft\", \"accelerate\", \"safetensors\",\n            \"huggingface_hub\", \"tokenizers\", \"sentencepiece\", \"protobuf\",\n            \"numpy\", \"pandas\", \"pyarrow\", \"scikit-learn\", \"scipy\"]\n# Under --no-index one missing package aborts the WHOLE install, so 4-bit goes separately.\nassert stream([sys.executable, \"-m\", \"pip\", \"install\", \"--no-index\",\n               \"--find-links\", WHEELS, *REQUIRED], tail=6) == 0, \"offline install failed\"\nprint(\"installed:\", \", \".join(REQUIRED))\nrc = stream([sys.executable, \"-m\", \"pip\", \"install\", \"-q\", \"--no-index\",\n             \"--find-links\", WHEELS, \"bitsandbytes\"], tail=2)\nprint(\"bitsandbytes:\", \"installed\" if rc == 0 else \"absent (set USE_4BIT=False)\")\n\n# The Kaggle base image ships torchao 0.10.0, below PEFT's >=0.16.0 floor. PEFT's probe\n# RAISES rather than returning False, which kills get_peft_model for a backend nothing\n# here uses. Removing it is the cleanest fix and needs no network -- pip uninstall is\n# purely local. Cell 10 also neutralises the probe in-process, so this is belt and braces\n# and either one alone is sufficient.\n_rc = subprocess.call([sys.executable, \"-m\", \"pip\", \"uninstall\", \"-y\", \"-q\", \"torchao\"],\n                      stdout=subprocess.DEVNULL, stderr=subprocess.DEVNULL)\nprint(\"torchao:\", \"removed\" if _rc == 0 else \"not present / not removable \"\n      \"(Cell 10 will neutralise the probe instead)\")","metadata":{"_uuid":"1062772c-f8be-480c-9593-ce048473c325","_cell_guid":"24b7f7a8-1532-4099-91bc-c4bebd54411a","trusted":true,"collapsed":false,"jupyter":{"outputs_hidden":false},"execution":{"iopub.status.busy":"2026-08-13T11:40:34.071706Z","iopub.execute_input":"2026-08-13T11:40:34.071794Z","iopub.status.idle":"2026-08-13T11:41:15.624406Z","shell.execute_reply.started":"2026-08-13T11:40:34.071785Z","shell.execute_reply":"2026-08-13T11:41:15.624075Z"}},"outputs":[],"execution_count":null},{"cell_type":"code","source":"# =========================================================\n# Cell 3 - GPU check\n# =========================================================\nimport torch, importlib\nfor mod in [\"torch\", \"transformers\", \"peft\", \"accelerate\", \"pandas\", \"pyarrow\", \"sklearn\"]:\n    print(f\"  {mod:14s} {getattr(importlib.import_module(mod), '__version__', '?')}\")\nprint(\"\\ncuda:\", torch.cuda.is_available())\nif torch.cuda.is_available():\n    for i in range(torch.cuda.device_count()):\n        p = torch.cuda.get_device_properties(i)\n        arch = f\"sm_{p.major}{p.minor}\"\n        warn = \"\" if arch in torch.cuda.get_arch_list() else \"  *** torch has NO kernels for this arch ***\"\n        print(f\"  [{i}] {p.name}  {p.total_memory/1e9:.0f} GB  {arch}{warn}\")\n    print(\"  torch built for:\", \", \".join(torch.cuda.get_arch_list()))\n    print(\"  bf16:\", torch.cuda.is_bf16_supported())\nelse:\n    print(\"  CPU-only torch -- rebuild the wheel bundle from .../whl/cu128\")","metadata":{"_uuid":"4392ac17-0226-4643-bdb7-7cc3efbc6138","_cell_guid":"ba1a8bf7-4264-4874-9ec2-a2c6e28cc436","trusted":true,"collapsed":false,"jupyter":{"outputs_hidden":false},"execution":{"iopub.status.busy":"2026-08-13T11:41:15.625045Z","iopub.execute_input":"2026-08-13T11:41:15.625135Z","iopub.status.idle":"2026-08-13T11:41:29.180004Z","shell.execute_reply.started":"2026-08-13T11:41:15.625126Z","shell.execute_reply":"2026-08-13T11:41:29.179663Z"}},"outputs":[],"execution_count":null},{"cell_type":"code","source":"# =========================================================\n# Cell 4 - locate the raw data\n# =========================================================\n# Kaggle EXPANDS an uploaded .zip into a normal folder, so there is usually no archive to\n# open -- the 22,821 transcript CSVs are already sitting in /kaggle/input, read-only, which\n# is all we need since we only read them. This cell handles both layouts and does no\n# copying in the common case.\nimport glob, os, shutil, time, zipfile\nimport pandas as pd\n\ndef find(pattern, what):\n    hits = glob.glob(pattern, recursive=True)\n    assert hits, f\"could not find {what} -- is the dataset attached?\"\n    return sorted(hits, key=len)[0]\n\nfeats_csv  = find(\"/kaggle/input/**/train_features*.csv\", \"train_features CSV\")\nlabels_csv = find(\"/kaggle/input/**/train_labels*.csv\", \"train_labels CSV\")\nfeats  = pd.read_csv(feats_csv)\nlabels = pd.read_csv(labels_csv)\nprint(\"features:\", feats_csv)\nprint(\"labels  :\", labels_csv)\n\n# Locate the transcripts by looking for a real session file rather than by folder name --\n# that works no matter how Kaggle nested the upload.\nprobe = feats.session_id.iloc[0]\nhits = [h for h in glob.glob(f\"/kaggle/input/**/{probe}.csv\", recursive=True)]\nif hits:\n    TD = os.path.dirname(sorted(hits, key=len)[0])\n    print(f\"\\ntranscripts: {TD}  (already expanded by Kaggle -- no extraction needed)\")\nelse:\n    zip_path = find(\"/kaggle/input/**/train_transcripts*.zip\", \"transcripts (folder or zip)\")\n    TD = \"/kaggle/temp/train_transcripts\" if os.path.isdir(\"/kaggle/temp\") \\\n         else \"/kaggle/working/train_transcripts\"\n    os.makedirs(TD, exist_ok=True)\n    with zipfile.ZipFile(zip_path) as z:\n        members = [m for m in z.infolist() if not m.is_dir()]\n        if len(os.listdir(TD)) >= len(members):\n            print(f\"\\ntranscripts: {TD}  (already extracted, {len(members):,} files)\")\n        else:\n            print(f\"\\nextracting {zip_path} -> {TD}\")\n            t0 = last = time.time()\n            for i, m in enumerate(members, 1):\n                with z.open(m) as s, open(os.path.join(TD, os.path.basename(m.filename)), \"wb\") as d:\n                    shutil.copyfileobj(s, d)\n                if time.time() - last > 10 or i == len(members):\n                    el = time.time() - t0\n                    print(f\"  {i:,}/{len(members):,} ({i/len(members):.0%})  {el:.0f}s \"\n                          f\"elapsed, ~{el/i*(len(members)-i):.0f}s left\", flush=True)\n                    last = time.time()\n            print(f\"extracted {len(members):,} files in {time.time()-t0:.0f}s\")\n\n# Every session in the feature table must have a transcript, or rendering dies mid-loop\n# thousands of rows in. Check it now, cheaply.\npresent = {f[:-4] for f in os.listdir(TD) if f.endswith(\".csv\")}\nneed = set(feats.session_id.unique())\nmissing = need - present\nprint(f\"   {len(present):,} transcript files, {len(need):,} sessions referenced, \"\n      f\"{len(missing):,} missing\")\nassert not missing, f\"missing transcripts, e.g. {sorted(missing)[:5]}\"\nprint(f\"\\n{len(feats):,} responses / {feats.session_id.nunique():,} sessions / \"\n      f\"{feats.learning_objective_id.nunique()} objectives   \"\n      f\"base rate {labels.is_correct.mean():.5f}\")","metadata":{"_uuid":"9bfa57c5-405f-418d-a92c-fe22cf297525","_cell_guid":"9c621355-048c-40e5-8e12-7258236368ad","trusted":true,"collapsed":false,"jupyter":{"outputs_hidden":false},"execution":{"iopub.status.busy":"2026-08-13T11:41:29.180457Z","iopub.execute_input":"2026-08-13T11:41:29.180787Z","iopub.status.idle":"2026-08-13T11:42:07.905349Z","shell.execute_reply.started":"2026-08-13T11:41:29.180775Z","shell.execute_reply":"2026-08-13T11:42:07.905015Z"}},"outputs":[],"execution_count":null},{"cell_type":"code","source":"# =========================================================\n# Cell 5 - RENDERING: transcript + objective -> model input\n# inlined from src/context.py -- edit here, then port back to src/context.py\n# =========================================================\n\"\"\"\nTranscript -> model-input construction for \"Trace the Ace\".\n\nThis module is the ONE place where a (session transcript, learning objective) pair is\nturned into a string. It is imported by training AND copied verbatim into the submission\nbundle, so train/inference can never drift apart. Cell 8 freezes fixtures of its output\ninto every checkpoint; build_submission re-runs them and refuses to score on a mismatch.\n\nTHE WHOLE TRANSCRIPT IS PASSED, ALWAYS. No excerpt selection, no summarisation, no\ncharacter cap. The ONLY content ever removed is a line whose entire body is an [unclear]\nmarker (drop_unclear_only). Length is controlled in exactly one place -- MAX_LEN tokens in\nRows.__getitem__, which keeps the END and re-attaches the tokenizer's special prefix.\n\nEvery flag below changes the string. Set the same values in src/context.py and\nbuild_submission.py; Cell 7's parquet cache is keyed on all of them plus this source.\n\nMeasured on all 22,821 training transcripts:\n  * median 267 utterances, ~18.7k raw chars; rendered median ~19.9k chars / p95 ~28.0k /\n    max ~46.8k, i.e. ~5.5k / 7.8k / 13.0k tokens at the chars/3.6 proxy. TOKEN_AUDIT\n    (Cell 12) replaces that proxy with the real tokenizer -- every length decision in this\n    notebook rests on it and it has never been confirmed.\n  * 36.7% of sessions carry >1 objective, covering 58.8% of rows; 5,323 within-session\n    pairs carry OPPOSITE labels while sharing ~99.5% of prompt characters. The objective\n    text is the only thing separating them, which is why it appears in the header AND the\n    footer.\n  * The graded quiz happens AFTER the session, so no answer leakage exists.\n\"\"\"\nfrom __future__ import annotations\nimport re\nfrom typing import List, Sequence, Tuple\n\nROLE_TAG = {\"student\": \"S\", \"tutor\": \"T\", \"background\": \"B\"}\n\n# [Speaker:Name] and the bare [Speaker]. 99.9% of occurrences are MID-utterance: they mark\n# where the ASR merged a second speaker's turn into this row. 104,745 named tags in 39.4%\n# of sessions, 45,681 bare ones in 35.4%; some marker appears in 60.6% of sessions.\n_SPEAKER_RE = re.compile(r\"\\[Speaker(?::([^\\]]*))?\\]\")\n_TUTOR_NAMES = {\"tutor\", \"teacher\"}\n_BG_NAMES = {\"background\", \"unknown\", \"unattributed\", \"other\"}\n_UNCLEAR_RE = re.compile(r\"\\[unclear\\]\", re.IGNORECASE)\n\n# Words that appear in almost every learning objective and carry no topic information.\n_OBJ_STOP = {\n    \"the\", \"a\", \"an\", \"of\", \"and\", \"to\", \"in\", \"with\", \"up\", \"on\", \"for\", \"by\", \"is\",\n    \"are\", \"be\", \"as\", \"at\", \"from\", \"that\", \"this\", \"it\", \"or\", \"your\", \"you\", \"using\",\n    \"use\", \"given\", \"when\", \"into\", \"than\", \"then\", \"its\", \"their\", \"them\", \"we\", \"can\",\n    \"including\", \"include\", \"etc\", \"each\", \"any\", \"all\", \"both\", \"how\", \"what\", \"which\",\n    \"where\", \"who\", \"know\", \"knowing\", \"understand\", \"understanding\", \"find\", \"finding\",\n    \"solve\", \"solving\", \"problems\", \"numbers\", \"number\",\n}\nLONG_TURN_WORDS = 12          # a \"long\" student turn, for add_production_meta\n\n\ndef _clean(text: str) -> str:\n    \"\"\"Light normalisation of an ASR utterance. Leaves speaker markers alone.\"\"\"\n    text = text.replace(\"[UNCLEAR]\", \"[unclear]\")\n    text = re.sub(r\"(\\[unclear\\]\\s*){2,}\", \"[unclear] \", text)\n    text = re.sub(r\"\\s+\", \" \", text)\n    return text.strip()\n\n\ndef _strip_speakers(text: str) -> str:\n    r\"\"\"Old behaviour, reproduced EXACTLY so split_speaker_tags=False is a true baseline.\n\n    Note the colon: the original pattern was r\"\\[Speaker:[^\\]]*\\]\", which deletes\n    [Speaker:Name] and leaves the bare [Speaker] standing as literal noise tokens. That\n    asymmetry is a defect, but it belongs to the OFF arm -- fixing it here would make the\n    A/B measure two changes at once.\n    \"\"\"\n    return re.sub(r\"\\s+\", \" \", re.sub(r\"\\[Speaker:[^\\]]*\\]\", \"\", text)).strip()\n\n\ndef _is_unclear_only(text: str) -> bool:\n    \"\"\"True when a line carries an [unclear] and NOTHING else of substance.\n\n    \"B: [unclear]\" is a speaker tag, a colon and a marker meaning the ASR heard nothing.\n    \"S: 9. [unclear] 9. No, it's going to be 16 ...\" is a real turn that happens to contain\n    a gap, and is kept intact -- the marker is doing useful work there.\n\n    Deliberately narrow: the line must CONTAIN an [unclear], and removing every [unclear]\n    must leave nothing alphanumeric. So \"[unclear].\" and \"[unclear] [unclear]\" go, while\n    \"...\" or \"?\" alone are left -- a different kind of noise, and not what this is for.\n    \"\"\"\n    if not _UNCLEAR_RE.search(text):\n        return False\n    return not any(ch.isalnum() for ch in _UNCLEAR_RE.sub(\"\", text))\n\n\ndef _tag_for(name: str | None, current: str) -> str:\n    \"\"\"Speaker tag implied by a marker. A bare [Speaker] means 'someone else, unknown'.\"\"\"\n    if name is None:\n        return \"B\"\n    n = name.strip().lower()\n    if not n:\n        return \"B\"\n    if n in _TUTOR_NAMES:\n        return \"T\"\n    if n in _BG_NAMES:\n        return \"B\"\n    return \"S\"          # a named person in these transcripts is the student\n\n\ndef _segments(text: str, role: str) -> List[Tuple[str, str]]:\n    \"\"\"Split one utterance into (speaker_tag, text) runs on its speaker markers.\"\"\"\n    tag = ROLE_TAG.get(role, \"?\")\n    out: List[Tuple[str, str]] = []\n    pos = 0\n    for m in _SPEAKER_RE.finditer(text):\n        seg = text[pos:m.start()].strip()\n        if seg:\n            out.append((tag, seg))\n        tag = _tag_for(m.group(1), tag)\n        pos = m.end()\n    seg = text[pos:].strip()\n    if seg:\n        out.append((tag, seg))\n    return out\n\n\ndef objective_keywords(objective: str) -> set:\n    \"\"\"Content words of a learning objective, for obj_mention_meta ONLY.\n\n    This SELECTS AND REMOVES NOTHING -- the whole transcript still renders verbatim. It is\n    used to count how many student turns touch the objective's wording, which is then\n    stated as a number in the footer.\n\n    Measured over three stop-lists (this one, an aggressive one, and none at all), ZERO of\n    the 398 objectives end up with an empty keyword set, so the count never degenerates to\n    a constant.\n    \"\"\"\n    return {w for w in re.findall(r\"[a-z]+\", objective.lower())\n            if len(w) > 3 and w not in _OBJ_STOP}\n\n\ndef build_pairs(\n    roles: Sequence[str],\n    contents: Sequence[str],\n    split_speaker_tags: bool = True,\n    drop_unclear_only: bool = True,\n    merge_speaker_runs: bool = False,\n) -> List[Tuple[str, str]]:\n    \"\"\"(speaker_tag, text) for every line that will be rendered.\n\n    Factored out of render() so the footer statistics are computed from EXACTLY the lines\n    the model sees, not from the raw transcript. Counting student turns on the raw rows\n    while rendering the split ones would state a number the prompt contradicts.\n    \"\"\"\n    pairs: List[Tuple[str, str]] = []\n    for role, content in zip(roles, contents):\n        text = _clean(content)\n        if not text:\n            continue\n        if split_speaker_tags:\n            segs = _segments(text, role)\n        else:\n            stripped = _strip_speakers(text)\n            segs = [(ROLE_TAG.get(role, \"?\"), stripped)] if stripped else []\n        for tag, seg in segs:\n            if drop_unclear_only and _is_unclear_only(seg):\n                continue\n            pairs.append((tag, seg))\n    if merge_speaker_runs:\n        merged: List[Tuple[str, str]] = []\n        for tag, seg in pairs:\n            if merged and merged[-1][0] == tag:\n                merged[-1] = (tag, merged[-1][1] + \" \" + seg)\n            else:\n                merged.append((tag, seg))\n        pairs = merged\n    return pairs\n\n\ndef render(\n    roles: Sequence[str],\n    contents: Sequence[str],\n    timestamps: Sequence[str],\n    objective: str,\n    split_speaker_tags: bool = True,\n    n_resp_in_session: int | None = None,\n    drop_unclear_only: bool = True,\n    short_unclear: bool = False,\n    unclear_glyph: str = \"~\",\n    merge_speaker_runs: bool = False,\n    add_production_meta: bool = False,\n    obj_mention_meta: bool = False,\n) -> str:\n    \"\"\"Build the model input string for one (session, learning objective) pair.\n\n    `timestamps` is accepted and ignored. The whole timing channel -- inter-utterance gaps,\n    response latency, duration, objective-conditional timing -- measures about +0.0004 of\n    log loss, which does not pay for the tokens. The parameter stays so training and\n    src/context.py keep the same call shape.\n\n    `short_unclear` respells the [unclear] marker as `unclear_glyph`. It removes NO\n    content: the marker still says \"something was said here that the ASR could not\n    transcribe\", in one token instead of three.\n\n    `add_production_meta` states the student's turn count and long-turn count (>= 12 words).\n    Long student turns are the strongest scalar signal found in this data, and the prompt\n    otherwise shows the turns without ever naming the quantity.\n\n    `obj_mention_meta` states how many of the STUDENT's own turns contain a content word\n    from the objective. Topic MENTION and topic WORK are different things and the label\n    depends on the second, which is why this counts student turns only.\n\n    The learning objective is stated before AND after the transcript. Not decoration: a\n    lesson covers several objectives with different labels, so without it two rows with\n    opposite labels become the same input -- and because truncation keeps the LAST max_len\n    tokens, a header-only objective is removed from exactly the long transcripts that need\n    it most.\n    \"\"\"\n    pairs = build_pairs(roles, contents, split_speaker_tags, drop_unclear_only,\n                        merge_speaker_runs)\n    lines = []\n    for tag, seg in pairs:\n        if short_unclear:\n            seg = _UNCLEAR_RE.sub(unclear_glyph, seg)\n        lines.append(f\"{tag}: {seg}\")\n    body = \"\\n\".join(lines)\n\n    obj = objective.strip()\n    legend = \"Speakers: T=tutor, S=student, B=unattributed (noisy ASR diarisation)\"\n    if short_unclear:\n        legend += f\"; {unclear_glyph} = inaudible\"\n    footer = f\"Learning objective assessed after this lesson: {obj}\\n\"\n    if n_resp_in_session is not None:\n        footer += (f\"This lesson covered {int(n_resp_in_session)} assessed learning \"\n                   f\"objective(s); this question is about the one named above.\\n\")\n    if add_production_meta:\n        stu = [s for t, s in pairs if t == \"S\"]\n        longs = sum(1 for s in stu if len(s.split()) >= LONG_TURN_WORDS)\n        footer += (f\"The student spoke in {len(stu)} turns, {longs} of them extended.\\n\")\n    if obj_mention_meta:\n        kw = objective_keywords(obj)\n        hits = sum(1 for t, s in pairs\n                   if t == \"S\" and any(w in s.lower() for w in kw)) if kw else 0\n        footer += (f\"The student's own turns touch this objective's wording in \"\n                   f\"{hits} places.\\n\")\n    return (\n        f\"Learning objective: {obj}\\n\"\n        f\"{legend}\\n\"\n        f\"Transcript:\\n\"\n        f\"{body}\\n\"\n        f\"{footer}\"\n        f\"Question: did the student answer the assessment question for that \"\n        f\"learning objective correctly?\"\n    )\n\n\n# Bound render() to the Cell 1 flags once, so every call site -- Cell 7, the fixtures in\n# Cell 8, the sanity probe in Cell 12 and build_submission.py -- is guaranteed to pass the\n# same values. Passing them by hand at four call sites is how train/inference drift starts.\nRENDER_KW = dict(\n    split_speaker_tags=SPLIT_SPEAKER_TAGS,\n    drop_unclear_only=DROP_UNCLEAR_ONLY,\n    short_unclear=SHORT_UNCLEAR,\n    unclear_glyph=UNCLEAR_GLYPH,\n    merge_speaker_runs=MERGE_SPEAKER_RUNS,\n    add_production_meta=ADD_PRODUCTION_META,\n    obj_mention_meta=OBJ_MENTION_META,\n)\n\n\ndef render_row(roles, contents, timestamps, objective, n_resp_in_session=None):\n    \"\"\"The one call every consumer should use.\"\"\"\n    return render(roles, contents, timestamps, objective,\n                  n_resp_in_session=n_resp_in_session if ADD_SESSION_META else None,\n                  **RENDER_KW)\n","metadata":{"_uuid":"26a3e8bf-09d7-406d-a8cf-0aa11fff0504","_cell_guid":"cc2d6ae4-6687-4507-9f16-5c6a6a432c44","trusted":true,"collapsed":false,"jupyter":{"outputs_hidden":false},"execution":{"iopub.status.busy":"2026-08-13T11:42:07.905839Z","iopub.execute_input":"2026-08-13T11:42:07.905936Z","iopub.status.idle":"2026-08-13T11:42:07.91922Z","shell.execute_reply.started":"2026-08-13T11:42:07.905925Z","shell.execute_reply":"2026-08-13T11:42:07.918958Z"}},"outputs":[],"execution_count":null},{"cell_type":"code","source":"# =========================================================\n# Cell 6 - PURGED CROSS-VALIDATION\n# inlined from src/cv.py -- edit here, then port back to src/cv.py\n# =========================================================\n\"\"\"\nCross-validation for \"Trace the Ace\".\n\nWHY THIS FILE MATTERS MORE THAN THE MODEL\n-----------------------------------------\nA response is a (session, learning objective) pair, and BOTH keys can leak:\n\n  * SESSION leak - the same 45-minute transcript appears in train and validation. 36.7% of\n    sessions carry more than one objective, covering 58.8% of all rows, so this is not a\n    corner case.\n  * OBJECTIVE overlap - only a leak if the TEST objectives are disjoint from train. The\n    leaderboard shape says they effectively are (see below).\n\nYou cannot eliminate both with a partition. Sessions and objectives form a bipartite graph\nwhose largest connected component holds 93.6% of the rows; any 5-fold split of it puts\n~20% per fold, so some edge must be cut. Measured leak rates over all 35,072 rows:\n\n    code  splitter                                     sess   obj    obj    label    probe\n                                                       leak   leak   TVD    spread   spread\n    A     GroupKFold(session)                            0%   99.8%  0.057  0.0095   0.01497\n    B     StratifiedGroupKFold(session, y=label)         0%   99.8%  0.057  0.0000   0.00716\n    C     StratifiedGroupKFold(session, y=objective)     0%   99.8%  0.010  0.0155   0.01662\n    C+    StratifiedGroupKFold(session, y=obj x label)   0%   99.8%  0.014  0.0032   0.00206\n    D     StratifiedGroupKFold(objective, y=label)      51%    0.0%  0.800  0.0182   0.01586\n    E     purged: objective folds + session purge        0%    0.0%  0.800  0.0000   n/a\n\nBALANCING THE OBJECTIVE MIX IS NOT THE SAME AS REMOVING THE OBJECTIVE LEAK. They are\nopposite operations. Forcing every fold to carry the same objective distribution (low TVD)\nREQUIRES each objective to appear in every fold, which IS the 99.8% overlap. What it buys\nis fold comparability, not honesty: matched folds differ less from each other, so an A/B\ncomparison of two model changes is more sensitive. C+ is the best harness in the table on\nthat axis (probe spread 0.00206) and is still 0.029 optimistic in absolute terms.\n\nWhy C+ and not C: stratifying on the objective alone throws away the label balance, and the\nresulting base-rate spread (0.0155) costs more fold-to-fold noise than the balanced\nobjective mix saves -- C is the WORST of the six on probe spread. Stratifying on the\ncomposite class `objective x is_correct` balances both at once, at essentially no cost to\nthe objective mix (TVD 0.0139 vs C's 0.0104). Stable across fold seeds 0/1/42/2024.\n\nE assigns objectives greedily by row count and then SWAP-REFINES to equalise the label rate.\nWithout that refinement the per-fold base rate ranges 0.639-0.752 and the no-model constant\npredictor scores 0.57391 on one fold and 0.66334 on another -- a 0.089 swing, 7x the entire\nrank-1-to-rank-50 leaderboard spread, purely from fold assignment. With it the floors sit\nwithin 0.00053 of each other and the training fraction is unchanged (65.2% vs 65.3%).\n\nPURGED FOLDS (scheme E) are the only ones that leak neither key:\n\n    val   = rows whose objective is assigned to this fold\n    train = rows whose session touches NO objective in this fold\n    purge = everything else\n\nCost: training sees ~65% of rows instead of 80%. If you do not want to pay that, use D --\n80% of rows, zero objective leak, and only the ~51% session leak remains (worth 0.002-0.007).\n\nThe same TF-IDF reference model scored under each scheme, against the leaderboard:\n\n    session-grouped (objectives leak)   0.56977   auc 0.674   <- optimistic by ~0.027\n    objective-grouped (sessions leak)   0.59437   auc 0.609\n    PURGED (neither leaks)              0.59630   auc 0.602   <- trust this one\n    ---\n    leaderboard #1 / #50                0.5961 / 0.6086\n\nPurged CV lands within 0.0002 of the leaderboard leader.\n\nHOW NOISY IS THE YARDSTICK\n--------------------------\nBootstrapped standard error of the log-loss estimate, against the 0.0127 that separates\nleaderboard rank 1 from rank 50:\n\n    one validation fold (7,014 rows)     SE 0.00536    42% of the whole LB spread\n    pooled 5-fold OOF (35,072 rows)      SE 0.00238    19%\n    pooled OOF, 3 seeds averaged         SE 0.00139    11%\n\nChoosing between two checkpoints that genuinely differ by 0.002 succeeds 61% of the time on\none fold. Never select on a single fold; never select \"best epoch\" by validation loss.\n\"\"\"\nfrom __future__ import annotations\nimport numpy as np\nimport pandas as pd\nimport warnings\nfrom sklearn.model_selection import GroupKFold, StratifiedGroupKFold\n\nBASE_RATE = 0.70247\nCONST_LOGLOSS = 0.60876\nLB_FIRST = 0.5961\nLB_FIFTIETH = 0.6086\nSE_ONE_FOLD = 0.00536\nSE_POOLED_OOF = 0.00238\n\n# ---- scheme registry ---------------------------------------------------------------------\n# group : the key held intact -- no group is ever split across folds\n# strat : what the fold assignment is balanced on\n# purge : also drop training rows whose session touches the validation fold\nSCHEMES = {\n    \"A\":  dict(group=\"session_id\",            strat=None,\n               purge=False, seeded=False, sess_leak=False, obj_leak=True,\n               desc=\"GroupKFold(session)\"),\n    \"B\":  dict(group=\"session_id\",            strat=\"label\",\n               purge=False, seeded=True,  sess_leak=False, obj_leak=True,\n               desc=\"StratifiedGroupKFold(session, y=is_correct)\"),\n    \"C\":  dict(group=\"session_id\",            strat=\"objective\",\n               purge=False, seeded=True,  sess_leak=False, obj_leak=True,\n               desc=\"StratifiedGroupKFold(session, y=learning_objective_id)\"),\n    \"C+\": dict(group=\"session_id\",            strat=\"objective_label\",\n               purge=False, seeded=True,  sess_leak=False, obj_leak=True,\n               desc=\"StratifiedGroupKFold(session, y=objective x is_correct)\"),\n    \"D\":  dict(group=\"learning_objective_id\", strat=\"label\",\n               purge=False, seeded=True,  sess_leak=True,  obj_leak=False,\n               desc=\"StratifiedGroupKFold(objective, y=is_correct)\"),\n    \"E\":  dict(group=\"learning_objective_id\", strat=\"greedy_balanced\",\n               purge=True,  seeded=False, sess_leak=False, obj_leak=False,\n               desc=\"purged: objective folds + session purge\"),\n}\nFOLD_COL_OF = {\"A\": \"fold_A\", \"B\": \"fold_B\", \"C\": \"fold_C\", \"C+\": \"fold_Cp\",\n               \"D\": \"fold_D\", \"E\": \"fold_E\"}\n\n\ndef _strat_key(df: pd.DataFrame, kind: str | None, y: np.ndarray) -> np.ndarray:\n    \"\"\"The vector StratifiedGroupKFold balances across folds.\"\"\"\n    if kind in (None, \"label\"):\n        return y\n    obj = pd.factorize(df.learning_objective_id)[0]\n    if kind == \"objective\":\n        return obj\n    if kind == \"objective_label\":\n        # One class per (objective, label) pair -> 796 classes. Balancing THIS balances the\n        # objective mix and the within-objective pass rate in a single pass, which is why\n        # C+ beats both C (mix only) and B (rate only) on fold-to-fold stability.\n        return obj * 2 + y\n    raise ValueError(f\"unknown stratification key {kind!r}\")\n\n\ndef _greedy_balanced_objective_folds(df: pd.DataFrame, n_splits: int,\n                                     max_passes: int = 60) -> np.ndarray:\n    \"\"\"Objectives -> folds, balancing row count AND label rate. Seed-independent.\n\n    Pass 1 is the old largest-first packing by row count. Pass 2 swaps pairs of objectives\n    between folds while that lowers a cost combining row imbalance and base-rate imbalance.\n    Without pass 2 the fold base rates range 0.639-0.752 and single-fold numbers are not\n    comparable to each other at all.\n    \"\"\"\n    g = df.groupby(\"learning_objective_id\").is_correct.agg([\"size\", \"sum\"])\n    objs = list(g.index)\n    SZ = g[\"size\"].to_dict()\n    PS = g[\"sum\"].to_dict()\n    load = np.zeros(n_splits)\n    assign = {}\n    for o, n in g[\"size\"].sort_values(ascending=False).items():\n        k = int(np.argmin(load))\n        assign[o] = k\n        load[k] += n\n    rows = np.zeros(n_splits)\n    pos = np.zeros(n_splits)\n    for o, k in assign.items():\n        rows[k] += SZ[o]\n        pos[k] += PS[o]\n\n    def cost(rows, pos):\n        rate = pos / np.maximum(rows, 1)\n        return 3.0 * (rate.max() - rate.min()) + (rows.max() - rows.min()) / rows.sum() * n_splits\n\n    cur = cost(rows, pos)\n    for _ in range(max_passes):\n        best = None\n        for i, oi in enumerate(objs):\n            ki = assign[oi]\n            for oj in objs[i + 1:]:\n                kj = assign[oj]\n                if ki == kj:\n                    continue\n                r, p = rows.copy(), pos.copy()\n                r[ki] += SZ[oj] - SZ[oi]; r[kj] += SZ[oi] - SZ[oj]\n                p[ki] += PS[oj] - PS[oi]; p[kj] += PS[oi] - PS[oj]\n                c = cost(r, p)\n                if c < cur - 1e-12 and (best is None or c < best[0]):\n                    best = (c, oi, oj, r, p)\n        if best is None:\n            break\n        cur, oi, oj, rows, pos = best\n        assign[oi], assign[oj] = assign[oj], assign[oi]\n    return df.learning_objective_id.map(assign).values.astype(int)\n\n\ndef make_folds(df: pd.DataFrame, scheme: str = \"C+\", n_splits: int = 5,\n               seed: int = 0) -> np.ndarray:\n    \"\"\"Integer validation-fold id per row, for one of the schemes in SCHEMES.\n\n    A / B / C / C+ hold SESSIONS intact -- no transcript is ever split across folds -- and\n    differ only in what the assignment is balanced on. All four therefore share the same\n    99.8% objective overlap; see the module docstring for why that is a leak and not a\n    feature. D holds OBJECTIVES intact instead. E holds objectives intact AND purges.\n\n    `seed` is consumed only by the schemes whose SCHEMES[...][\"seeded\"] is True: GroupKFold\n    takes no random_state (it packs groups by size, deterministically) and E's assignment is\n    greedy largest-first, so both give the identical split for every seed.\n    \"\"\"\n    assert scheme in SCHEMES, f\"scheme must be one of {tuple(SCHEMES)}, got {scheme!r}\"\n    spec = SCHEMES[scheme]\n    if spec[\"strat\"] == \"greedy_balanced\":\n        return _greedy_balanced_objective_folds(df, n_splits)\n    y = (df[\"is_correct\"].values.astype(int) if \"is_correct\" in df\n         else np.zeros(len(df), dtype=int))\n    groups = df[spec[\"group\"]].values\n    if spec[\"strat\"] is None:\n        splitter = GroupKFold(n_splits=n_splits)\n    else:\n        splitter = StratifiedGroupKFold(n_splits=n_splits, shuffle=True, random_state=seed)\n    # 66 objectives have a single row, so the composite classes used by C and C+ cannot all\n    # reach 5 folds. sklearn warns; the singletons simply land wherever their session lands,\n    # which is correct and unavoidable.\n    with warnings.catch_warnings():\n        warnings.filterwarnings(\"ignore\", message=\".*least populated class.*\")\n        folds = np.full(len(df), -1, dtype=int)\n        for f, (_, va) in enumerate(splitter.split(df, _strat_key(df, spec[\"strat\"], y),\n                                                   groups)):\n            folds[va] = f\n    assert (folds >= 0).all(), \"some row was never assigned to a validation fold\"\n    return folds\n\n\ndef purged_train_index(df: pd.DataFrame, fold: int, fold_col: str = \"fold_E\") -> np.ndarray:\n    \"\"\"Row positions usable for TRAINING when validating on `fold`.\n\n    Drops the validation fold itself and every row whose session also contributed a\n    response to that fold -- so no transcript is seen on both sides. Rows dropped this way\n    are simply unused for this fold, not moved into validation.\n    \"\"\"\n    touched = df.groupby(\"session_id\")[fold_col].agg(lambda s: frozenset(s))\n    per_row = df.session_id.map(touched)\n    return np.where([fold not in t for t in per_row])[0]\n\ndef split(df: pd.DataFrame, fold: int, fold_col: str = \"fold_Cp\",\n          purge: bool = False) -> tuple[np.ndarray, np.ndarray]:\n    \"\"\"(train_idx, val_idx) for one fold.\"\"\"\n    val = np.where(df[fold_col].values == fold)[0]\n    tr = (purged_train_index(df, fold, fold_col) if purge\n          else np.where(df[fold_col].values != fold)[0])\n    return tr, val\n\ndef objective_tvd(df: pd.DataFrame, folds: np.ndarray, n_splits: int = 5) -> list:\n    \"\"\"Total-variation distance between each fold's objective mix and the global one.\n\n    0.0 means the fold carries exactly the global objective distribution. This is the thing\n    C and C+ optimise -- and, to be explicit, driving it toward 0 REQUIRES every objective\n    to appear in every fold, i.e. it maximises the objective overlap rather than removing it.\n    \"\"\"\n    glob = df.learning_objective_id.value_counts(normalize=True)\n    out = []\n    for f in range(n_splits):\n        p = (df.learning_objective_id[folds == f].value_counts(normalize=True)\n             .reindex(glob.index).fillna(0.0))\n        out.append(0.5 * (p - glob).abs().sum())\n    return out\n\n\ndef audit(df: pd.DataFrame, fold_col: str = \"fold_Cp\", purge: bool = False,\n          n_splits: int = 5) -> None:\n    \"\"\"Print leak rates, fold sizes and balance. Run this whenever you change the split.\"\"\"\n    from sklearn.metrics import log_loss\n    tvd = objective_tvd(df, df[fold_col].values, n_splits)\n    print(f\"{'fold':>5s} {'train':>7s} {'val':>6s} {'purged':>7s} \"\n          f\"{'sess leak':>10s} {'obj leak':>9s} {'val rate':>9s} {'obj TVD':>8s} \"\n          f\"{'floor':>8s}\")\n    rates, floors = [], []\n    for f in range(n_splits):\n        tr, va = split(df, f, fold_col, purge)\n        s_tr = set(df.session_id.values[tr])\n        o_tr = set(df.learning_objective_id.values[tr])\n        sl = np.isin(df.session_id.values[va], list(s_tr)).sum()\n        ol = np.isin(df.learning_objective_id.values[va], list(o_tr)).sum()\n        yv, yt = df.is_correct.values[va], df.is_correct.values[tr]\n        # \"floor\" = what a model that has learned NOTHING scores on this fold. Any spread\n        # here is fold-assignment artefact and moves your number without any modelling.\n        fl = log_loss(yv, np.full(len(va), yt.mean()), labels=[0, 1])\n        rates.append(yv.mean()); floors.append(fl)\n        print(f\"{f:5d} {len(tr):7d} {len(va):6d} {len(df)-len(tr)-len(va):7d} \"\n              f\"{sl:10d} {ol:9d} {yv.mean():9.3f} {tvd[f]:8.4f} {fl:8.5f}\")\n    print(f\"{'':5s} {'':7s} {'':6s} {'':7s} {'':10s} {'':9s} \"\n          f\"{max(rates)-min(rates):9.4f} {np.mean(tvd):8.4f} {max(floors)-min(floors):8.5f}\"\n          f\"   <- spreads (lower is better)\")\n\n\ndef audit_all(df: pd.DataFrame, n_splits: int = 5, seed: int = 0) -> pd.DataFrame:\n    \"\"\"Build every scheme and print the comparison table. Changes nothing; diagnostic only.\n\n    `train`/`val`/`unused` are per-fold row counts averaged over the folds. They sum to\n    len(df) for every scheme; only E has a non-zero `unused` column, which is the purge\n    cost. Every scheme still produces a full-coverage OOF of len(df) rows -- the purge\n    removes rows from TRAINING, never from validation.\n    \"\"\"\n    n = len(df)\n    rows = []\n    for code, spec in SCHEMES.items():\n        f = make_folds(df, scheme=code, n_splits=n_splits, seed=seed)\n        sess, obj = df.session_id.values, df.learning_objective_id.values\n        sl = ol = 0\n        rates, tr_n, va_n = [], [], []\n        for k in range(n_splits):\n            va = f == k\n            if spec[\"purge\"]:\n                touched = pd.Series(f).groupby(sess).agg(lambda s: k in set(s))\n                tr = ~df.session_id.map(touched).values\n            else:\n                tr = f != k\n            sl += np.isin(sess[va], sess[tr]).sum()\n            ol += np.isin(obj[va], obj[tr]).sum()\n            rates.append(df.is_correct.values[va].mean())\n            tr_n.append(int(tr.sum())); va_n.append(int(va.sum()))\n        rows.append({\"scheme\": code,\n                     \"train\": int(round(np.mean(tr_n))),\n                     \"train %\": 100 * np.mean(tr_n) / n,\n                     \"val\": int(round(np.mean(va_n))),\n                     \"val min\": min(va_n), \"val max\": max(va_n),\n                     \"unused\": int(round(n - np.mean(tr_n) - np.mean(va_n))),\n                     \"OOF\": sum(va_n),\n                     \"sess leak\": sl / n, \"obj leak\": ol / n,\n                     \"obj TVD\": np.mean(objective_tvd(df, f, n_splits)),\n                     \"label spread\": max(rates) - min(rates),\n                     \"splitter\": spec[\"desc\"]})\n    out = pd.DataFrame(rows).set_index(\"scheme\")\n    ints = [\"train\", \"val\", \"val min\", \"val max\", \"unused\", \"OOF\"]\n    with pd.option_context(\"display.width\", 250, \"display.max_colwidth\", 60):\n        print(f\"total labelled rows = {n:,}   ({n_splits} folds)\\n\")\n        print(out.to_string(\n            formatters={c: \"{:,}\".format for c in ints}\n                       | {\"train %\": \"{:.1f}%\".format},\n            float_format=lambda v: f\"{v:8.4f}\"))\n    return out\n\n\ndef report(y: np.ndarray, p: np.ndarray, name: str = \"\") -> dict:\n    \"\"\"Log loss / AUROC with the reference points that actually matter.\"\"\"\n    from sklearn.metrics import log_loss, roc_auc_score\n    p = np.clip(p, 1e-6, 1 - 1e-6)\n    ll = log_loss(y, p)\n    auc = roc_auc_score(y, p)\n    flag = \"\" if abs(CONST_LOGLOSS - ll) > 2 * SE_POOLED_OOF else \"  (within 2 SE of noise)\"\n    print(f\"{name:34s} logloss={ll:.5f}  auc={auc:.4f}  \"\n          f\"gain {CONST_LOGLOSS - ll:+.5f}{flag}\")\n    return {\"name\": name, \"logloss\": ll, \"auc\": auc, \"gain\": CONST_LOGLOSS - ll}","metadata":{"_uuid":"3c746721-a18d-417e-80e7-f910f967d8bf","_cell_guid":"b46cab99-3ee1-43fd-9bb2-0738e7cb7bc3","trusted":true,"collapsed":false,"jupyter":{"outputs_hidden":false},"execution":{"iopub.status.busy":"2026-08-13T11:42:07.919922Z","iopub.execute_input":"2026-08-13T11:42:07.920092Z","iopub.status.idle":"2026-08-13T11:42:07.946966Z","shell.execute_reply.started":"2026-08-13T11:42:07.920083Z","shell.execute_reply":"2026-08-13T11:42:07.946731Z"}},"outputs":[],"execution_count":null},{"cell_type":"code","source":"# =========================================================\n# Cell 7 - build the training table (cached)\n# =========================================================\nimport hashlib, inspect, json, os, time\nimport numpy as np\nimport pandas as pd\n\n# The cache is keyed on a hash of the RENDER SOURCE plus every flag that changes the\n# string. A fixed path protected only by \"delete it yourself\" is how an A/B of two\n# renderings returns a null result for the most boring possible reason.\n_RENDER_SRC = \"\".join(inspect.getsource(f) for f in (\n    _clean, _strip_speakers, _is_unclear_only, _tag_for, _segments,\n    objective_keywords, build_pairs, render))\n_CTX_KEY = hashlib.md5(\n    (_RENDER_SRC + json.dumps(RENDER_KW, sort_keys=True)\n     + repr(ADD_SESSION_META)).encode()).hexdigest()[:10]\nPARQUET = f\"/kaggle/working/train_ctx_{_CTX_KEY}.parquet\"\n\n# ---- E13: transcript schema + unseen roles ------------------------------------------------\n# ROLE_TAG.get(role, \"?\") means an unseen role renders as \"?\" -- a speaker the legend never\n# defines, on every line of that session, with no error. Provably a no-op on all 6.1M\n# training utterances; this is detection for the test set, where it is unverifiable.\n_ROLE_COUNTS = {}\n_BAD_SCHEMA = []\n\ndef _check_transcript(t, sid):\n    if not CHECK_ROLES:\n        return\n    missing = {\"role\", \"content\"} - set(t.columns)\n    if missing:\n        _BAD_SCHEMA.append((sid, sorted(missing)))\n        return\n    for r in t.role:\n        _ROLE_COUNTS[r] = _ROLE_COUNTS.get(r, 0) + 1\n\nif os.path.exists(PARQUET):\n    df = pd.read_parquet(PARQUET)\n    print(f\"using cached {PARQUET}  (render key {_CTX_KEY})\")\nelse:\n    # validate=\"one_to_one\" turns a silently-dropped or duplicated response_id into an\n    # exception. The default inner join would just shrink the table.\n    df = feats.merge(labels, on=\"response_id\", validate=\"one_to_one\")\n    assert len(df) == len(feats), f\"label merge lost {len(feats)-len(df)} rows\"\n    texts, meta = [], []\n    t0 = last = time.time()\n    print(f\"rendering {len(df):,} contexts  (render key {_CTX_KEY})\", flush=True)\n    print(\"  flags: \" + \"  \".join(f\"{k}={v}\" for k, v in RENDER_KW.items())\n          + f\"  add_session_meta={ADD_SESSION_META}\", flush=True)\n    for sid, g in df.groupby(\"session_id\", sort=False):\n        t = pd.read_csv(os.path.join(TD, f\"{sid}.csv\"), dtype=str, keep_default_na=False)\n        _check_transcript(t, sid)\n        roles, contents, stamps = t.role.tolist(), t.content.tolist(), t.timestamp.tolist()\n        stu_words = sum(len(c.split()) for r, c in zip(roles, contents) if r == \"student\")\n        h, m, s = (int(x) for x in stamps[-1].split(\":\")) if stamps else (0, 0, 0)\n        # Rendered-line statistics, from the SAME pairs the prompt is built from.\n        pr = build_pairs(roles, contents, SPLIT_SPEAKER_TAGS, DROP_UNCLEAR_ONLY,\n                         MERGE_SPEAKER_RUNS)\n        n_stu = sum(1 for tg, _ in pr if tg == \"S\")\n        n_long = sum(1 for tg, sg in pr if tg == \"S\" and len(sg.split()) >= LONG_TURN_WORDS)\n        n_resp = len(g)\n        for _, row in g.iterrows():\n            texts.append(render_row(roles, contents, stamps, row.learning_objective,\n                                    n_resp_in_session=n_resp))\n            meta.append({\"response_id\": row.response_id, \"n_utt\": len(contents),\n                         \"dur_min\": h * 60 + m + s / 60, \"stu_words\": stu_words,\n                         \"n_resp_in_session\": n_resp, \"n_lines\": len(pr),\n                         \"n_stu_turns\": n_stu, \"n_long_stu_turns\": n_long})\n        now = time.time()\n        if now - last > 20 or len(texts) == len(df):\n            el = now - t0\n            print(f\"  {len(texts):,}/{len(df):,} ({len(texts)/len(df):.0%})  \"\n                  f\"{el:.0f}s elapsed, ~{el/max(1,len(texts))*(len(df)-len(texts)):.0f}s left\",\n                  flush=True)\n            last = now\n    M_ = pd.DataFrame(meta); M_[\"text\"] = texts\n    df = df.merge(M_, on=\"response_id\", validate=\"one_to_one\")\n    df.to_parquet(PARQUET, index=False)\n    print(f\"wrote {PARQUET} in {time.time()-t0:.0f}s\")\n    if CHECK_ROLES:\n        assert not _BAD_SCHEMA, f\"transcripts missing columns, e.g. {_BAD_SCHEMA[:3]}\"\n        unseen = {r: n for r, n in _ROLE_COUNTS.items() if r not in ROLE_TAG}\n        tot = sum(_ROLE_COUNTS.values())\n        print(f\"  roles: \" + \"  \".join(f\"{r}={n:,}\" for r, n in sorted(_ROLE_COUNTS.items()))\n              + f\"   ({tot:,} utterances)\")\n        if unseen:\n            print(f\"  *** {sum(unseen.values()):,} utterances carry an UNSEEN role {list(unseen)} \"\n                  f\"and render as '?', which the speaker legend never defines ***\")\n        else:\n            print(\"  all roles are in ROLE_TAG -- no '?' lines rendered\")\n\n# ---- E14: does the feature table list every row of each session? --------------------------\n# ADD_SESSION_META states n_resp_in_session in the prompt, derived as len(g) over the\n# FEATURE table. If a test feature table were chunked or filtered, that number would be\n# wrong on every row of the affected sessions -- and the model was trained to trust it.\n# Train is self-consistent by construction; this establishes the invariant to assert on\n# test (Cell 16 does the same check there and falls back rather than lying).\nif CHECK_SESSION_META:\n    _by_sess = df.groupby(\"session_id\").size()\n    _stated = df.groupby(\"session_id\").n_resp_in_session.nunique()\n    assert (_stated == 1).all(), \"n_resp_in_session disagrees within a session\"\n    assert (df.groupby(\"session_id\").n_resp_in_session.first() == _by_sess).all(), \\\n        \"n_resp_in_session does not equal the number of feature rows for that session\"\n    print(f\"  session-meta invariant holds: {len(_by_sess):,} sessions, \"\n          f\"{_by_sess.max()} objectives at most, \"\n          f\"{100*(_by_sess > 1).mean():.1f}% carry more than one\")\n\n# Folds are a few groupbys and depend on nothing the parquet caches, so they are rebuilt on\n# every run. Changing CV_SCHEME or N_SPLITS then takes effect without re-rendering.\ndf[FOLD_COL] = make_folds(df, scheme=CV_SCHEME, n_splits=N_SPLITS, seed=SEED)\n\nch = df.text.str.len()\nprint(f\"\\n{len(df):,} rows   base rate {df.is_correct.mean():.5f}\")\nprint(f\"context chars: median {ch.median():,.0f}  p95 {ch.quantile(.95):,.0f}  max {ch.max():,.0f}\")\nprint(f\"approx tokens (chars/3.6 -- TOKEN_AUDIT replaces this proxy): median \"\n      f\"{ch.median()/3.6:,.0f}  p95 {ch.quantile(.95)/3.6:,.0f}  \"\n      f\"({(ch/3.6 > MAX_LEN).mean():.1%} of rows would truncate at MAX_LEN {MAX_LEN:,})\")\n_r = df.groupby(\"n_resp_in_session\").is_correct.agg([\"mean\", \"size\"]).head(6)\nprint(\"base rate by objectives-per-session: \"\n      + \"  \".join(f\"{k}->{v['mean']:.3f}(n={int(v['size']):,})\" for k, v in _r.iterrows()))\nprint(f\"\\nfold audit -- scheme {CV_SCHEME}: {SCHEMES[CV_SCHEME]['desc']}   (purge={USE_PURGE})\")\nprint(\"  expected: sess leak \" + (\"> 0\" if EXPECT_SESSION_LEAK else \"== 0\")\n      + \", obj leak \" + (\"> 0 (objectives appear in every fold -- by design, and it matches \"\n                         \"deployment)\" if EXPECT_OBJECTIVE_LEAK else \"== 0\"))\naudit(df, FOLD_COL, USE_PURGE, N_SPLITS)\nif COMPARE_SCHEMES:\n    print(\"\\nall schemes side by side (diagnostic -- does not change what you train on):\")\n    audit_all(df, n_splits=N_SPLITS, seed=SEED)\n","metadata":{"_uuid":"18964113-dfa6-46af-8033-621ac9c0b1ee","_cell_guid":"f0735b8e-9721-46cc-bec6-5f81bb144105","trusted":true,"collapsed":false,"jupyter":{"outputs_hidden":false},"execution":{"iopub.status.busy":"2026-08-13T11:42:07.947309Z","iopub.execute_input":"2026-08-13T11:42:07.947498Z","iopub.status.idle":"2026-08-13T11:45:24.791175Z","shell.execute_reply.started":"2026-08-13T11:42:07.947488Z","shell.execute_reply":"2026-08-13T11:45:24.79083Z"}},"outputs":[],"execution_count":null},{"cell_type":"code","source":"# =========================================================\n# Cell 8 - MODEL: backbone + scalar head, any architecture\n# inlined from src/models.py -- edit here, then port back to src/models.py\n# =========================================================\n\"\"\"\nUniform classifier wrapper so one training script and one inference script cover every\nmodel in the ensemble, across architectures.\n\nWHY NOT AutoModelForSequenceClassification\n------------------------------------------\nThe target ensemble spans three families:\n\n    deepseek-ai/DeepSeek-R1-Distill-Qwen-7B    Qwen2 architecture\n    deepseek-ai/DeepSeek-R1-Distill-Qwen-14B   Qwen2 architecture\n    Qwen/Qwen3-8B / -14B / -32B                Qwen3 architecture\n    zai-org/GLM-Z1-9B-0414 / -32B-0414         GLM-4-0414 architecture\n\nWhether transformers ships a `<Family>ForSequenceClassification` head varies by family and\nby version, and the families disagree on projection names (GLM-4-0414 fuses its MLP into\n`gate_up_proj`, the Qwen families keep `gate_proj`/`up_proj` separate). Hard-coding either\nwould silently break part of the ensemble -- a LoRA config naming modules that do not exist\nraises, and one that matches too few modules trains almost nothing while looking fine.\n\nSo: load the bare backbone with AutoModel, pool the last non-pad token, and attach our own\n`nn.Linear(hidden, 1)`. That path is identical for every family. LoRA targets are\ndiscovered by scanning for Linear layers rather than being named, so a fused `gate_up_proj`\nis picked up exactly like a split one.\n\nCheckpoints therefore consist of two parts: the PEFT adapter directory and `head.pt`.\n\nHF-TRAINER CONTRACT\n-------------------\n`forward` takes `labels` and returns a `SequenceClassifierOutput`, because Trainer reads\n`outputs[\"loss\"]` and treats everything else as logits. `main_input_name`, `config` and the\n`gradient_checkpointing_*` passthroughs exist so Trainer's introspection finds what it\nexpects on a plain nn.Module.\n\"\"\"\nfrom __future__ import annotations\nimport json\nimport os\nfrom typing import Iterable\n\nimport torch\nimport torch.nn as nn\nfrom transformers import AutoConfig, AutoModel, AutoTokenizer\nfrom transformers.modeling_outputs import SequenceClassifierOutput\n\n# prefill_tflops assumes 2 * params * tokens over 10,508 test rows at max_len 1792\n# (18.8M tokens) and ~40% MFU on the A100 80GB the competition provides (312 TF bf16).\n# TRAINING capacity and INFERENCE budget are different constraints and do not move\n# together. A 96 GB RTX PRO 6000 trains any of these in bf16 LoRA -- even 32B, whose\n# weights are ~66 GB -- but the SUBMISSION runs on one A100 80GB for at most 6 hours, and\n# that is what decides which of them you can actually ship. `bf16_train_gb` is the weight\n# footprint; add roughly 5-12 GB for activations at max_len 8192 with grad checkpointing.\nMODELS: dict[str, dict] = {\n    \"Qwen/Qwen3-1.7B\": dict(params=1.7e9, family=\"qwen3\", kaggle_4bit=\"fits 1x T4\",\n                            infer_min=9),\n    \"Qwen/Qwen3-4B\": dict(params=4.0e9, family=\"qwen3\", kaggle_4bit=\"fits 1x T4\",\n                          infer_min=20),\n    \"Qwen/Qwen3-8B\": dict(params=8.2e9, family=\"qwen3\", kaggle_4bit=\"fits 1x T4, tight\",\n                          infer_min=41),\n    \"deepseek-ai/DeepSeek-R1-Distill-Qwen-7B\": dict(\n        params=7.6e9, family=\"qwen2\", kaggle_4bit=\"fits 1x T4, tight\", infer_min=38),\n    \"zai-org/GLM-Z1-9B-0414\": dict(params=9.4e9, family=\"glm4\",\n                                   kaggle_4bit=\"fits 1x T4, tight\", infer_min=47),\n    \"Qwen/Qwen3-14B\": dict(params=14.8e9, family=\"qwen3\",\n                           kaggle_4bit=\"needs device_map across 2x T4\", infer_min=74),\n    \"deepseek-ai/DeepSeek-R1-Distill-Qwen-14B\": dict(\n        params=14.8e9, family=\"qwen2\", kaggle_4bit=\"needs device_map across 2x T4\",\n        infer_min=74),\n    \"Qwen/Qwen3-32B\": dict(params=32.8e9, family=\"qwen3\",\n                           kaggle_4bit=\"4-bit weights alone are 16 GB; 2x T4 sharded, slow\",\n                           infer_min=164),\n    \"zai-org/GLM-Z1-32B-0414\": dict(\n        params=32.6e9, family=\"glm4\",\n        kaggle_4bit=\"4-bit weights alone are 16 GB; 2x T4 sharded, slow\", infer_min=163),\n}\nINFER_BUDGET_MIN = 360          # the competition's 6-hour cap\nSAFE_BUDGET_MIN = 300           # what to actually plan against\n\nMFU = 0.40                      # achieved fraction of peak; A100 bf16 peak = 312 TFLOP/s\nA100_TFLOPS = 312.0\n\n\ndef infer_minutes(params: float, n_rows: int, mean_tokens: float,\n                  mfu: float = MFU, tflops: float = A100_TFLOPS) -> float:\n    \"\"\"E6. Prefill cost of a forward-only pass: 2 * params * tokens, over n_rows.\n\n    The static infer_min numbers in MODELS were computed at max_len=1792. The real mean\n    rendered length is ~5,455 tokens, so every one of them is roughly 3x optimistic --\n    which is exactly the kind of error that is discovered at hour 6 of a 6-hour cap.\n    Call this with numbers measured by TOKEN_AUDIT instead of trusting the registry.\n    \"\"\"\n    return 2.0 * params * mean_tokens * n_rows / (mfu * tflops * 1e12) / 60.0\n\n\ndef plan(names: Iterable[str], n_rows: int = 10508, mean_tokens: float | None = None) -> None:\n    \"\"\"Print the inference cost of a proposed ensemble against the 6-hour cap.\n\n    With mean_tokens given (or MEAN_TOKENS set by TOKEN_AUDIT) the cost is DERIVED from the\n    real token distribution. Without it, the stale registry numbers are used and flagged.\n    \"\"\"\n    mt = mean_tokens if mean_tokens is not None else globals().get(\"MEAN_TOKENS\")\n    derived = mt is not None and globals().get(\"INFER_COST_MODEL\", True)\n    total = 0.0\n    print(f\"{'model':44s} {'params':>8s} {'infer':>8s}  kaggle training\")\n    if derived:\n        print(f\"  [derived: {n_rows:,} rows x {mt:,.0f} mean tokens, \"\n              f\"{MFU:.0%} MFU on {A100_TFLOPS:.0f} TF]\")\n    else:\n        print(\"  [registry numbers -- computed at max_len 1792, ~3x optimistic. Run \"\n              \"TOKEN_AUDIT to derive them instead.]\")\n    for n in names:\n        m = MODELS.get(n)\n        if m is None:\n            print(f\"{n:44s} {'?':>8s} {'?':>8s}  not in registry\")\n            continue\n        mins = infer_minutes(m[\"params\"], n_rows, mt) if derived else float(m[\"infer_min\"])\n        total += mins\n        print(f\"{n:44s} {m['params']/1e9:7.1f}B {mins:6.0f}m  {m['kaggle_4bit']}\")\n    print(f\"{'TOTAL':44s} {'':8s} {total:6.0f}m  \"\n          f\"({'FITS' if total <= SAFE_BUDGET_MIN else 'OVER BUDGET'} \"\n          f\"vs {SAFE_BUDGET_MIN}m planning cap, {INFER_BUDGET_MIN}m hard cap)\")\n    if total > SAFE_BUDGET_MIN:\n        print(\"  -> drop a model, quantise to 8-bit, or lower --max-len\")\n\nclass LMClassifier(nn.Module):\n    \"\"\"Causal-LM backbone + scalar head on the last non-pad token.\n\n    Right padding is assumed throughout (training and inference collate identically), so\n    the final real token is at index `attention_mask.sum(1) - 1`.\n    \"\"\"\n\n    main_input_name = \"input_ids\"          # Trainer introspects this\n    # Trainer checks this attribute FIRST; if absent it falls back to \"does forward()\n    # declare **kwargs?\". A **kwargs there made it believe the model normalises its own\n    # loss, so it skipped `loss /= gradient_accumulation_steps` -- the accumulated\n    # gradient became a SUM of ACCUM means, i.e. ACCUM times too large, and MAX_GRAD_NORM\n    # clipped something other than what it names. forward() below takes\n    # num_items_in_batch explicitly instead, so no VAR_KEYWORD is exposed either way.\n    accepts_loss_kwargs = False\n\n    def __init__(self, backbone: nn.Module, hidden: int, bias_init: float = 0.0,\n                 pool: str = \"last\"):\n        super().__init__()\n        self.backbone = backbone\n        # E26. \"last\": the final non-pad hidden state, which on a causal LM has attended\n        # over everything and sits right after the objective footer. \"last+mean\": also\n        # concatenate the mean over non-pad positions, doubling the head input width.\n        # The head SHAPE depends on this, so a checkpoint trained under one value cannot\n        # load under the other -- checkpoint.json records it, load_checkpoint reads it back.\n        assert pool in (\"last\", \"last+mean\"), pool\n        self.pool = pool\n        self.head = nn.Linear(hidden * (2 if pool == \"last+mean\" else 1), 1)\n        # bias_init = logit(train base rate) starts the model EXACTLY at the constant\n        # predictor (0.60876 here) instead of at p=0.5 with random-sign logits, so warmup\n        # is not spent rediscovering the prior. weight=0 is safe: the gradient is\n        # (sigmoid(b) - y) * h, which is non-zero, so there is no dead unit.\n        nn.init.constant_(self.head.bias, float(bias_init))\n        if bias_init:\n            nn.init.zeros_(self.head.weight)\n        else:\n            nn.init.normal_(self.head.weight, std=0.01)\n        self.config = getattr(backbone, \"config\", None)\n\n    def forward(self, input_ids, attention_mask, labels=None, num_items_in_batch=None):\n        # use_cache=False matters: the backbone otherwise builds and returns a full KV\n        # cache on every forward, which this classifier immediately discards. At 10,508\n        # rows x 1,792 tokens on a 32B model that is gigabytes of pointless A100\n        # allocation, and it also conflicts with gradient checkpointing during training.\n        h = self.backbone(input_ids=input_ids, attention_mask=attention_mask,\n                          use_cache=False, output_hidden_states=False).last_hidden_state\n        last = attention_mask.sum(1) - 1\n        pooled = h[torch.arange(h.size(0), device=h.device), last]\n        if self.pool == \"last+mean\":\n            # Masked mean over real positions only. Without the mask this would average in\n            # the pad columns and make the logit depend on the batch longest row.\n            m = attention_mask.unsqueeze(-1).to(h.dtype)\n            mean = (h * m).sum(1) / m.sum(1).clamp(min=1)\n            pooled = torch.cat([pooled, mean], dim=-1)\n        logits = self.head(pooled.float()).squeeze(-1)\n        loss = None\n        if labels is not None:\n            loss = nn.functional.binary_cross_entropy_with_logits(logits, labels.float())\n        return SequenceClassifierOutput(loss=loss, logits=logits)\n\n    # -- passthroughs so Trainer / PEFT can drive checkpointing through the wrapper -------\n    def gradient_checkpointing_enable(self, **kw):\n        self.backbone.gradient_checkpointing_enable(**kw)\n\n    def gradient_checkpointing_disable(self):\n        self.backbone.gradient_checkpointing_disable()\n\n    def enable_input_require_grads(self):\n        self.backbone.enable_input_require_grads()\n\ndef find_target_modules(model: nn.Module) -> list[str]:\n    \"\"\"Every Linear inside the backbone, by leaf name -- architecture agnostic.\n\n    Excludes the lm_head / embedding projections, which are tied to the vocabulary and are\n    not what we want to adapt.\n    \"\"\"\n    skip = {\"lm_head\", \"embed_out\", \"head\", \"score\", \"classifier\"}\n    names = set()\n    for name, mod in model.named_modules():\n        if isinstance(mod, nn.Linear) or mod.__class__.__name__ in {\"Linear4bit\", \"Linear8bitLt\"}:\n            leaf = name.split(\".\")[-1]\n            if leaf not in skip:\n                names.add(leaf)\n    return sorted(names)\n\ndef load_tokenizer(name_or_path: str):\n    tok = AutoTokenizer.from_pretrained(name_or_path, trust_remote_code=True)\n    if tok.pad_token is None:\n        tok.pad_token = tok.eos_token\n    tok.padding_side = \"right\"\n    return tok\n\ndef build_backbone(name_or_path: str, quantize_4bit: bool, dtype, device_map=None):\n    kw = dict(trust_remote_code=True, dtype=dtype)\n    if quantize_4bit:\n        from transformers import BitsAndBytesConfig\n        kw[\"quantization_config\"] = BitsAndBytesConfig(\n            load_in_4bit=True, bnb_4bit_quant_type=\"nf4\",\n            bnb_4bit_compute_dtype=dtype, bnb_4bit_use_double_quant=True)\n        kw[\"device_map\"] = device_map or {\"\": 0}\n    elif device_map is not None:\n        kw[\"device_map\"] = device_map\n    try:\n        backbone = AutoModel.from_pretrained(name_or_path, attn_implementation=\"sdpa\", **kw)\n        _attn = \"sdpa\"\n    except (ValueError, TypeError) as e:\n        # E12. The bare retry is the point of this function original design, but it also\n        # turns \"sdpa was rejected\" into silence -- and eager attention at ~5,500 tokens is\n        # materially slower with no other symptom. Say so.\n        print(\"  *** attn_implementation='sdpa' REJECTED (%s: %s); falling back to the \"\n              \"model default (likely eager) ***\" % (type(e).__name__, e))\n        if globals().get(\"STRICT_ATTN\", False):\n            raise RuntimeError(\n                \"sdpa unavailable and STRICT_ATTN is on. Set STRICT_ATTN=False to accept \"\n                \"the slower eager path deliberately.\") from e\n        backbone = AutoModel.from_pretrained(name_or_path, **kw)\n        _attn = \"eager(fallback)\"\n    print(\"  attention implementation:\", _attn,\n          getattr(getattr(backbone, \"config\", None), \"_attn_implementation\", \"\"))\n    cfg = AutoConfig.from_pretrained(name_or_path, trust_remote_code=True)\n    hidden = getattr(cfg, \"hidden_size\", None) or getattr(cfg, \"n_embd\")\n    return backbone, hidden\n\nRENDER_FIXTURE_FILE = \"render_fixtures.json\"\n\n\ndef make_render_fixtures(n: int = 6) -> dict:\n    \"\"\"E19. Freeze (input -> expected output) pairs for render_row().\n\n    Cell 0 says the render must stay identical to src/context.py or \"training renders a row\n    one way and inference renders it another, which produces no error and quietly costs\n    score\". A checksum of the source cannot express that: a legitimate refactor changes the\n    source without changing the string. Fixtures test the STRING.\n\n    Synthetic inputs on purpose. Fingerprinting real training rows would force\n    build_submission.py to carry training transcripts at submission-build time -- a data\n    dependency that can silently vanish in a code competition, and then the guard fires\n    falsely on the run you least want it to.\n    \"\"\"\n    cases = [\n        ([\"tutor\"], [\"Hello there.\"], [\"00:00:01\"], \"Adding fractions.\", 1),\n        ([\"tutor\", \"student\"], [\"[unclear]\", \"Yes [unclear] I think 12.\"],\n         [\"00:00:01\", \"00:00:04\"], \"Adding fractions.\", 2),\n        ([\"tutor\"], [\"make [Speaker:Callum] to the largest, yeah\"], [\"00:01:00\"],\n         \"Ordering numbers up to 1,000,000.\", 1),\n        ([\"student\"], [\"[unclear] [unclear]\"], [\"00:02:00\"], \"Adding fractions.\", 3),\n        ([\"background\", \"student\", \"student\"],\n         [\"ok\", \"I got 4 and then 8 and then 12 and then 16 and 20 and 24 and 28.\",\n          \"yeah\"], [\"00:03:00\", \"00:03:04\", \"00:03:09\"], \"Using 2x, 5x and 10x table.\", 1),\n        ([\"tutor\", \"unknown_role\"], [\"Right.\", \"hmm\"], [\"00:04:00\", \"00:04:02\"],\n         \"Adding fractions.\", 2),\n    ][:n]\n    out = []\n    for roles, contents, stamps, obj, nresp in cases:\n        out.append({\"roles\": roles, \"contents\": contents, \"timestamps\": stamps,\n                    \"objective\": obj, \"n_resp_in_session\": nresp,\n                    \"expected\": render_row(roles, contents, stamps, obj, nresp)})\n    return {\"flags\": dict(RENDER_KW, add_session_meta=ADD_SESSION_META),\n            \"render_key\": _CTX_KEY, \"cases\": out}\n\n\ndef verify_render_fixtures(path_or_obj) -> None:\n    \"\"\"Re-run the frozen fixtures against THIS process render_row(). Raises on drift.\n\n    build_submission.py calls this before scoring a single row. A mismatch means the\n    submission would be scored under a different prompt than the model was trained on.\n    \"\"\"\n    fx = path_or_obj\n    if isinstance(fx, str):\n        with open(fx, encoding=\"utf-8\") as fh:\n            fx = json.load(fh)\n    bad = []\n    for i, c in enumerate(fx[\"cases\"]):\n        got = render_row(c[\"roles\"], c[\"contents\"], c[\"timestamps\"], c[\"objective\"],\n                         c[\"n_resp_in_session\"])\n        if got != c[\"expected\"]:\n            bad.append((i, c[\"expected\"], got))\n    if bad:\n        i, exp, got = bad[0]\n        raise RuntimeError(\n            \"RENDER DRIFT: %d/%d fixtures differ. The checkpoint was trained with flags %s \"\n            \"(render key %s); this process renders differently.\\n--- expected ---\\n%s\\n\"\n            \"--- got ---\\n%s\" % (len(bad), len(fx[\"cases\"]), fx[\"flags\"],\n                                 fx.get(\"render_key\"), exp, got))\n    print(\"  render fixtures: %d/%d match (key %s)\"\n          % (len(fx[\"cases\"]), len(fx[\"cases\"]), fx.get(\"render_key\")))\n\n\ndef save_checkpoint(model: LMClassifier, tok, out_dir: str, meta: dict) -> None:\n    \"\"\"PEFT adapter + head weights + the metadata inference needs to rebuild this.\"\"\"\n    os.makedirs(out_dir, exist_ok=True)\n    model.backbone.save_pretrained(out_dir)          # PeftModel\n    if tok is not None:\n        tok.save_pretrained(out_dir)\n    torch.save(model.head.state_dict(), os.path.join(out_dir, \"head.pt\"))\n    meta = dict(meta, pool=getattr(model, \"pool\", \"last\"))\n    if globals().get(\"RENDER_FIXTURES\", False):\n        with open(os.path.join(out_dir, RENDER_FIXTURE_FILE), \"w\", encoding=\"utf-8\") as fh:\n            json.dump(make_render_fixtures(), fh, indent=2)\n    with open(os.path.join(out_dir, \"checkpoint.json\"), \"w\", encoding=\"utf-8\") as fh:\n        json.dump(meta, fh, indent=2, default=str)\n\ndef load_adapter_and_head(model: LMClassifier, ckpt_dir: str) -> None:\n    \"\"\"Load a checkpoint dir INTO an existing model. Used by Trainer resume / load-best.\n\n    Trainer's own `_load_from_checkpoint` looks for pytorch_model.bin or a PeftModel at the\n    top level and finds neither -- the top level here is a plain nn.Module wrapping one --\n    so both hooks are overridden to call this instead.\n    \"\"\"\n    from peft import set_peft_model_state_dict\n    sft = os.path.join(ckpt_dir, \"adapter_model.safetensors\")\n    if os.path.exists(sft):\n        from safetensors.torch import load_file\n        sd = load_file(sft)\n    else:\n        sd = torch.load(os.path.join(ckpt_dir, \"adapter_model.bin\"), map_location=\"cpu\")\n    set_peft_model_state_dict(model.backbone, sd)\n    head = os.path.join(ckpt_dir, \"head.pt\")\n    if os.path.exists(head):\n        model.head.load_state_dict(torch.load(head, map_location=\"cpu\"))\n\ndef load_checkpoint(adapter_dir: str, base_path: str, dtype, device: str,\n                    merge: bool = True) -> tuple[LMClassifier, object]:\n    \"\"\"Rebuild a saved checkpoint for inference.\"\"\"\n    from peft import PeftModel\n    # Same guard as Cell 10: PeftModel.from_pretrained walks the same dispatcher chain, so\n    # an out-of-date torchao/gptq on the SUBMISSION box would kill inference identically.\n    if globals().get(\"NEUTRALISE_PEFT_BACKENDS\", True):\n        neutralise_peft_backend_probes(verbose=False)\n    tok = load_tokenizer(adapter_dir)\n    backbone, hidden = build_backbone(base_path, quantize_4bit=False, dtype=dtype)\n    backbone = PeftModel.from_pretrained(backbone, adapter_dir, dtype=dtype)\n    if merge:\n        backbone = backbone.merge_and_unload()\n    # The head width depends on the pooling mode, so it must come from the CHECKPOINT --\n    # not from whatever POOL happens to be set in this process.\n    _cj = os.path.join(adapter_dir, \"checkpoint.json\")\n    _meta = json.load(open(_cj, encoding=\"utf-8\")) if os.path.exists(_cj) else {}\n    model = LMClassifier(backbone, hidden, pool=_meta.get(\"pool\", \"last\"))\n    head = torch.load(os.path.join(adapter_dir, \"head.pt\"), map_location=\"cpu\")\n    model.head.load_state_dict(head)\n    # E19. Refuse to score if this process renders differently than training did.\n    _fx = os.path.join(adapter_dir, RENDER_FIXTURE_FILE)\n    if os.path.exists(_fx) and globals().get(\"RENDER_FIXTURES\", False):\n        verify_render_fixtures(_fx)\n    return model.to(device).eval(), tok\n\n\ndef load_calibration(adapter_dir: str):\n    \"\"\"E17. (a, b) of the Platt map fitted on this checkpoint OOF, or None.\n\n    Shipping it INSIDE the adapter directory is the point: the map and the weights it was\n    fitted for travel together and cannot be mismatched.\n    \"\"\"\n    p = os.path.join(adapter_dir, \"calibration.json\")\n    if not os.path.exists(p):\n        return None\n    c = json.load(open(p, encoding=\"utf-8\"))\n    return float(c[\"a\"]), float(c[\"b\"])","metadata":{"_uuid":"b4c2ac7f-fce0-45cb-989b-29c139d6fa3a","_cell_guid":"5fe49dd7-fe2b-44e2-8d62-2c89b52599c9","trusted":true,"collapsed":false,"jupyter":{"outputs_hidden":false},"execution":{"iopub.status.busy":"2026-08-13T11:45:24.791716Z","iopub.execute_input":"2026-08-13T11:45:24.791813Z","iopub.status.idle":"2026-08-13T11:45:24.810675Z","shell.execute_reply.started":"2026-08-13T11:45:24.791803Z","shell.execute_reply":"2026-08-13T11:45:24.810438Z"}},"outputs":[],"execution_count":null},{"cell_type":"code","source":"# =========================================================\n# Cell 9 - dataset, collate\n# =========================================================\nimport random, numpy as np, torch, torch.nn as nn\nfrom torch.utils.data import Dataset\n\ndef set_seed(s):\n    random.seed(s); np.random.seed(s); torch.manual_seed(s); torch.cuda.manual_seed_all(s)\n\nclass Rows(Dataset):\n    \"\"\"Yields the dict keys Trainer forwards straight into model(**inputs).\n\n    Truncation keeps the END of the lesson, which is where the tutor's in-session check\n    lives. That is a left-truncation, and it collides with tokenizers that PREPEND special\n    tokens: a plain `ids[-max_len:]` deletes them on every row longer than max_len, so the\n    model sees the documented input format on short rows and a headless one on long rows.\n    Qwen adds no prefix and never notices; GLM-4-0414's format opens with [gMASK]<sop>.\n    `prepend_specials` measures the prefix once and re-attaches it after the slice.\n    \"\"\"\n\n    def __init__(self, texts, labels, tok, max_len, prepend_specials=True, lengths=None,\n                 use_chat_template=None):\n        self.texts, self.labels, self.tok, self.max_len = texts, labels, tok, max_len\n        # E25. Default from the global so EVERY construction site -- the Cell 12 probe, the\n        # micro-benchmark, training, the OOF dump and build_submission -- agrees without\n        # each having to remember to pass it. A readout position that differs between\n        # training and inference is silent and total.\n        if use_chat_template is None:\n            use_chat_template = globals().get(\"USE_CHAT_TEMPLATE\", False)\n        self.chat_pre, self.chat_post = [], []\n        self.use_chat = bool(use_chat_template) and bool(\n            getattr(tok, \"chat_template\", None)) and hasattr(tok, \"apply_chat_template\")\n        if use_chat_template and not self.use_chat:\n            print(\"NOTE: USE_CHAT_TEMPLATE is on but this tokenizer has no chat_template \"\n                  \"-- falling back to the raw prompt.\")\n        if self.use_chat:\n            # Measure the template's prefix and suffix ONCE, by rendering a probe and\n            # splitting on it. The suffix is the generation prompt: the tokens an\n            # instruction-tuned model has learned mean \"your answer starts here\". Pooling\n            # reads the last non-pad position, so that is what the head sees.\n            _s = tok.apply_chat_template([{\"role\": \"user\", \"content\": \"\\u0000PROBE\\u0000\"}],\n                                         add_generation_prompt=True, tokenize=False)\n            _pre, _post = _s.split(\"\\u0000PROBE\\u0000\")\n            self.chat_pre = tok(_pre, add_special_tokens=False)[\"input_ids\"]\n            self.chat_post = tok(_post, add_special_tokens=False)[\"input_ids\"]\n        # Lengths for LengthGroupedSampler. Passed in rather than derived, because the\n        # sampler's own fallback is `[len(f[\"input_ids\"]) for f in dataset]` -- a full\n        # tokenisation pass over 22,821 transcripts just to sort them.\n        self.lengths = None if lengths is None else [int(x) for x in lengths]\n        # E34. Optional per-row session index, set by Cell 13 only when the pairwise\n        # sibling term or the sibling sampler is on, so the default path allocates and\n        # collates nothing extra.\n        self.session_idx = None\n        self.prefix: list = []\n        if prepend_specials:\n            a = tok(\"probe\", add_special_tokens=True)[\"input_ids\"]\n            b = tok(\"probe\", add_special_tokens=False)[\"input_ids\"]\n            k = len(a) - len(b)\n            # Only treat it as a prefix if the tail really is the unadorned encoding --\n            # a tokenizer that APPENDS an eos would otherwise hand us the wrong k tokens.\n            if k > 0 and a[k:] == b:\n                self.prefix = a[:k]\n\n    def __len__(self):\n        return len(self.texts)\n\n    def __getitem__(self, i):\n        if self.use_chat:\n            # Template supplies its own specials, so add_special_tokens=False on the body.\n            body = self.tok(self.texts[i], truncation=False,\n                            add_special_tokens=False)[\"input_ids\"]\n            budget = self.max_len - len(self.chat_pre) - len(self.chat_post)\n            assert budget > 0, \"MAX_LEN is smaller than the chat template itself\"\n            if len(body) > budget:\n                body = body[-budget:]          # keep the END, as always\n            ids = self.chat_pre + body + self.chat_post\n        else:\n            ids = self.tok(self.texts[i], truncation=False,\n                           add_special_tokens=True)[\"input_ids\"]\n            if len(ids) > self.max_len:\n                keep = self.max_len - len(self.prefix)\n                ids = self.prefix + ids[-keep:]\n        item = {\"input_ids\": ids, \"labels\": float(self.labels[i])}\n        if self.session_idx is not None:\n            item[\"session_idx\"] = int(self.session_idx[i])\n        return item\n\nclass Collate:\n    \"\"\"A class, not a closure: DataLoader workers pickle collate_fn under spawn.\"\"\"\n\n    def __init__(self, pad_id):\n        self.pad_id = pad_id\n\n    def __call__(self, batch):\n        n = max(len(b[\"input_ids\"]) for b in batch)\n        ids = torch.full((len(batch), n), self.pad_id, dtype=torch.long)\n        att = torch.zeros((len(batch), n), dtype=torch.long)\n        for i, b in enumerate(batch):\n            L = len(b[\"input_ids\"])\n            ids[i, :L] = torch.tensor(b[\"input_ids\"])    # right padding; pooling needs it\n            att[i, :L] = 1\n        out = {\"input_ids\": ids, \"attention_mask\": att,\n               \"labels\": torch.tensor([b[\"labels\"] for b in batch], dtype=torch.float)}\n        if \"session_idx\" in batch[0]:\n            # compute_loss pops this before calling the model, so forward() never\n            # sees it and the model signature stays clean.\n            out[\"session_idx\"] = torch.tensor([b[\"session_idx\"] for b in batch],\n                                              dtype=torch.long)\n        return out\n\nprint(\"ok\")","metadata":{"_uuid":"c7c63c3f-42a0-4abc-a5ce-0973feb27364","_cell_guid":"a40b9825-707a-47ab-80be-f2d98779d6a8","trusted":true,"collapsed":false,"jupyter":{"outputs_hidden":false},"execution":{"iopub.status.busy":"2026-08-13T11:45:24.81108Z","iopub.execute_input":"2026-08-13T11:45:24.81117Z","iopub.status.idle":"2026-08-13T11:45:24.823928Z","shell.execute_reply.started":"2026-08-13T11:45:24.811161Z","shell.execute_reply":"2026-08-13T11:45:24.823682Z"}},"outputs":[],"execution_count":null},{"cell_type":"code","source":"# =========================================================\n# Cell 10 - build the model\n# =========================================================\nimport time, torch, os, glob\nimport numpy as np\nfrom peft import LoraConfig, get_peft_model, prepare_model_for_kbit_training\n\n\ndef neutralise_peft_backend_probes(verbose=True):\n    \"\"\"Stop an unrelated, out-of-date quantisation package from blocking get_peft_model.\n\n    PEFT builds every LoRA layer by walking a chain of dispatchers -- torchao, awq, gptq,\n    eetq, aqlm, hqq, bitsandbytes, then the plain-Linear default. Each one begins by asking\n    `is_<backend>_available()`. Those helpers return False when the package is ABSENT, but\n    RAISE ImportError when it is present at a version PEFT considers too old:\n\n        ImportError: Found an incompatible version of torchao. Found version 0.10.0,\n        but only versions above 0.16.0 are supported\n\n    The Kaggle base image ships torchao 0.10.0. Nothing in this pipeline uses it -- the\n    default config is plain bf16 LoRA on nn.Linear -- but the probe runs anyway, before the\n    dispatcher can decide it is not applicable, so injection dies on the first module.\n\n    This probes each helper once and replaces the ones that raise with a stub returning\n    False, which is the answer they should have given. Backends that are genuinely present\n    and current are left completely alone, so a real bitsandbytes stays usable.\n\n    Rebinding matters: `peft/tuners/lora/torchao.py` does\n    `from peft.import_utils import is_torchao_available`, which binds the function OBJECT at\n    import time. Patching only peft.import_utils would leave that stale reference live, so\n    every already-imported peft submodule holding the same object is patched too.\n    \"\"\"\n    import inspect\n    import sys\n\n    try:\n        import peft.import_utils as piu\n    except Exception as e:                       # peft not importable at all\n        if verbose:\n            print(f\"  (peft.import_utils unavailable: {type(e).__name__}) -- nothing to do\")\n        return []\n\n    patched = []\n    for name in sorted(n for n in dir(piu)\n                       if n.startswith(\"is_\") and n.endswith(\"_available\")):\n        fn = getattr(piu, name)\n        if not callable(fn):\n            continue\n        # Only probe zero-argument helpers; anything taking arguments is not a plain\n        # availability flag and must not be stubbed blind.\n        try:\n            target = getattr(fn, \"__wrapped__\", fn)      # see through @lru_cache\n            if len(inspect.signature(target).parameters) != 0:\n                continue\n        except (TypeError, ValueError):\n            continue\n        try:\n            fn()\n            continue                                     # returned cleanly -> leave it\n        except Exception as e:\n            stub = lambda: False\n            setattr(piu, name, stub)\n            hits = 0\n            for modname, mod in list(sys.modules.items()):\n                if modname.startswith(\"peft\") and getattr(mod, name, None) is fn:\n                    setattr(mod, name, stub)\n                    hits += 1\n            patched.append((name, type(e).__name__, str(e).split(\"\\n\")[0][:110], hits))\n\n    if verbose:\n        if patched:\n            print(\"  neutralised PEFT backend probes that raise on a version mismatch:\")\n            for name, etype, msg, hits in patched:\n                print(f\"    {name}  ({etype}: {msg})\")\n                print(f\"      -> stubbed to False; rebound in {hits} already-imported peft module(s)\")\n            print(\"    None of these backends is used by this pipeline. If you later set\")\n            print(\"    USE_4BIT=True, check that is_bnb_available is NOT in the list above.\")\n        else:\n            print(\"  PEFT backend probes: all clean, nothing to neutralise\")\n    return patched\n\n\nset_seed(SEED)\nos.makedirs(OUT, exist_ok=True)\ndevice = \"cuda\" if torch.cuda.is_available() else \"cpu\"\n\n# T4 is Turing: fp16 + GradScaler (Trainer's, via args.fp16). Ampere/Ada/Blackwell get\n# bf16, which needs no scaler and cannot underflow.\nif device != \"cuda\":\n    dtype, USE_4BIT = torch.float32, False\n    print(\"WARNING: no CUDA -- plumbing smoke test only\")\nelif torch.cuda.is_bf16_supported():\n    dtype = torch.bfloat16\nelse:\n    dtype = torch.float16\nprint(f\"device {device}  dtype {dtype}  4bit {USE_4BIT}\")\n\n# prefer a locally attached copy of the base model when Internet is off\nmodel_path = MODEL\nif not os.path.isdir(model_path):\n    hits = [h for h in glob.glob(f\"/kaggle/input/**/{MODEL.replace('/', '_')}\",\n                                 recursive=True) if os.path.isdir(h)]\n    if hits:\n        model_path = sorted(hits, key=len)[0]\n        print(\"local base model:\", model_path)\n\n# Fit check BEFORE the load. from_pretrained mmaps the weights, so the backbone appears\n# to \"load\" in about a second even when it can never fit -- the OOM then lands much later,\n# at .to(device), after LoRA injection and several minutes of work. Qwen3-8B is 16.4 GB in\n# fp16/bf16 and 4.5 GB in NF4, so a 16 GB Kaggle T4 CANNOT hold it unquantised, weights\n# alone, before a single activation.\nif device == \"cuda\":\n    _vram = torch.cuda.get_device_properties(0).total_memory / 1e9\n    _params = {\"1.7b\": 1.7e9, \"4b\": 4.0e9, \"8b\": 8.2e9, \"9b\": 9.4e9,\n               \"14b\": 14.8e9, \"32b\": 32.8e9}.get(_MODEL_TAG.lower())\n    if _params:\n        _need = _params * (0.55 if USE_4BIT else 2) / 1e9\n        _act = 4 if GRAD_CKPT else 12          # rough activation budget at MAX_LEN 8192\n        print(f\"  fit check: {_MODEL_TAG} weights ~{_need:.1f} GB \"\n              f\"({'NF4' if USE_4BIT else str(dtype).split('.')[-1]}) + ~{_act} GB activations \"\n              f\"vs {_vram:.0f} GB VRAM\")\n        if _need + _act > _vram * 0.95:\n            raise RuntimeError(\n                f\"{_MODEL_TAG} will not fit: ~{_need + _act:.1f} GB needed vs {_vram:.0f} GB. \"\n                + (\"Set USE_4BIT=True (weights drop to ~%.1f GB).\" % (_params*0.55/1e9)\n                   if not USE_4BIT else\n                   \"Lower MAX_LEN, or use a smaller MODEL, or a bigger card.\")\n                + \" Raising this here rather than at .to(device) saves you the load and the \"\n                  \"LoRA injection first.\")\n\nt0 = time.time()\nprint(f\"loading backbone {model_path} ...\", flush=True)\nbackbone, hidden = build_backbone(model_path, USE_4BIT, dtype, None)\nprint(f\"  backbone ready in {time.time()-t0:.0f}s (hidden {hidden})\", flush=True)\n\nif USE_4BIT:\n    backbone = prepare_model_for_kbit_training(backbone, use_gradient_checkpointing=True)\ntargets = find_target_modules(backbone)\nprint(f\"  LoRA targets ({len(targets)}): {targets}\", flush=True)\n\n# E24. LORA_TOP_N > 0 restricts adapters to the TOP N decoder layers. target_modules must\n# then be full module PATHS, not leaf names -- PEFT matches a bare leaf name everywhere it\n# occurs. Regex over the discovered layer indices is the only way to say \"layers 20..35\".\n_lora_targets, _n_layers = targets, None\nif LORA_TOP_N:\n    import re as _re\n    _idx = sorted({int(m.group(1)) for n, _ in backbone.named_modules()\n                   for m in [_re.search(r\"\\.layers\\.(\\d+)\\.\", n + \".\")] if m})\n    assert _idx, \"could not discover decoder layer indices -- leave LORA_TOP_N=0\"\n    _n_layers = len(_idx)\n    _keep = set(_idx[-LORA_TOP_N:])\n    _lora_targets = [n for n, m in backbone.named_modules()\n                     if n.split(\".\")[-1] in targets\n                     for mm in [_re.search(r\"\\.layers\\.(\\d+)\\.\", n + \".\")]\n                     if mm and int(mm.group(1)) in _keep]\n    assert _lora_targets, f\"LORA_TOP_N={LORA_TOP_N} matched no modules\"\n    print(f\"  LORA_TOP_N={LORA_TOP_N} of {_n_layers} layers -> {len(_lora_targets)} modules \"\n          f\"(layers {min(_keep)}..{max(_keep)}). Lower layers will NOT adapt: this is the \"\n          f\"one setting most able to cost score, so A/B it before trusting it.\", flush=True)\n\nif NEUTRALISE_PEFT_BACKENDS:\n    neutralise_peft_backend_probes()\n\nbackbone = get_peft_model(backbone, LoraConfig(\n    r=LORA_R, lora_alpha=LORA_ALPHA, lora_dropout=DROPOUT, bias=\"none\",\n    task_type=\"FEATURE_EXTRACTION\", target_modules=_lora_targets))\n# E4. Checkpointing is turned on HERE, not through\n# TrainingArguments(gradient_checkpointing=True) -- Trainer would call it on the wrapper\n# before PEFT has re-enabled input grads.\n#\n# GRAD_CKPT=False trades memory for throughput. Two things matter and they are separate:\n#   * gradient_checkpointing_enable() recomputes every block in the backward pass. That is\n#     roughly a 1.3-1.5x cost on the backward, and LoRA does not need it the way full\n#     fine-tuning does -- bf16 Qwen3-8B weights are under 30 GB.\n#   * enable_input_require_grads() forces the EMBEDDING output to require grad, which makes\n#     autograd walk the backward through every layer even when only the top ones carry\n#     adapters. With LORA_TOP_N set and checkpointing off, dropping it is what actually\n#     buys the saving -- but it is REQUIRED while checkpointing is on, or the recomputed\n#     graph has no grad path and the loss will not backprop at all.\nif GRAD_CKPT:\n    backbone.gradient_checkpointing_enable()\n    backbone.enable_input_require_grads()\n    print(\"  gradient checkpointing ON  (memory-cheap, ~1.3-1.5x slower backward)\")\nelse:\n    backbone.gradient_checkpointing_disable() if hasattr(\n        backbone, \"gradient_checkpointing_disable\") else None\n    if not LORA_TOP_N:\n        backbone.enable_input_require_grads()\n    print(f\"  gradient checkpointing OFF (faster, more memory)\"\n          + (\"  + input grads NOT forced, so the backward can stop above the frozen \"\n             \"layers\" if LORA_TOP_N else \"\"))\n    print(\"  -> run the MICRO_BENCH cell before committing: it reports peak GB and ms/step\")\n\n# logit(p) of the TRAIN base rate for this fold's training rows -- not the global one, so\n# a purged/objective-grouped split still starts at its own floor.\n_bias0 = 0.0\nif HEAD_BIAS_INIT:\n    _p0 = float(df.is_correct.mean())\n    _bias0 = float(np.log(_p0 / (1 - _p0)))\n    print(f\"head bias init: base rate {_p0:.5f} -> logit {_bias0:+.4f} \"\n          f\"(model starts at the constant predictor)\")\nmodel = LMClassifier(backbone, hidden, bias_init=_bias0, pool=POOL).to(device)\ntok = load_tokenizer(model_path)\ntrainable = sum(p.numel() for p in model.parameters() if p.requires_grad)\ntotal = sum(p.numel() for p in model.parameters())\nprint(f\"trainable {trainable/1e6:.1f}M / {total/1e9:.2f}B ({trainable/total:.3%})   \"\n      f\"pool={POOL}  head in={model.head.in_features}\")","metadata":{"_uuid":"8d22ca99-b88a-4d7d-8545-79ad19c02a02","_cell_guid":"6f8923a1-6338-4a45-92a4-f6452498748f","trusted":true,"collapsed":false,"jupyter":{"outputs_hidden":false},"execution":{"iopub.status.busy":"2026-08-13T11:45:24.824313Z","iopub.execute_input":"2026-08-13T11:45:24.824404Z","iopub.status.idle":"2026-08-13T11:47:41.045826Z","shell.execute_reply.started":"2026-08-13T11:45:24.824393Z","shell.execute_reply":"2026-08-13T11:47:41.045515Z"}},"outputs":[],"execution_count":null},{"cell_type":"code","source":"import glob, json, os\nfor c in sorted(glob.glob(\"/kaggle/input/**/config.json\", recursive=True)):\n    try: j = json.load(open(c))\n    except Exception: continue\n    q = j.get(\"quantization_config\") or {}\n    print(f\"{q.get('quant_method','unquantized'):>12s}  {j.get('model_type','?'):8s}  {os.path.dirname(c)}\")","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-08-13T11:47:41.046254Z","iopub.execute_input":"2026-08-13T11:47:41.046539Z","iopub.status.idle":"2026-08-13T11:47:55.913705Z","shell.execute_reply.started":"2026-08-13T11:47:41.046528Z","shell.execute_reply":"2026-08-13T11:47:55.913423Z"}},"outputs":[],"execution_count":null},{"cell_type":"code","source":"# =========================================================\n# Cell 10b - FAMILY SANITY CHECK (run once per new MODEL)\n# =========================================================\n# Everything checked here is architecture-dependent and fails SILENTLY rather than loudly:\n# a LoRA config that matches too few modules trains almost nothing while looking fine, a\n# dropped special-token prefix costs a little score with no error, and a pooling bug shows\n# up only as \"this family just does not work as well\". Cheap to check, expensive to miss.\nimport importlib, torch\n\n_tv = importlib.import_module(\"transformers\").__version__\n_fam = getattr(model.config, \"model_type\", \"?\")\nprint(f\"family {_fam}   transformers {_tv}\")\n# glm4 landed in transformers 4.51 (2025-04-09). Older versions raise\n# \"checkpoint has model type `glm4` but Transformers does not recognize this architecture\"\n# -- and the wheels here are pinned offline, so this is a real gate, not a formality.\n_MIN = {\"glm4\": \"4.51.0\", \"qwen3\": \"4.51.0\", \"qwen2\": \"4.37.0\", \"glm4_moe\": \"4.54.0\"}\nif _fam in _MIN:\n    _have = tuple(int(x) for x in _tv.split(\".\")[:2])\n    _want = tuple(int(x) for x in _MIN[_fam].split(\".\")[:2])\n    print(f\"  needs transformers >= {_MIN[_fam]}: \"\n          + (\"ok\" if _have >= _want else \"*** TOO OLD -- rebuild the wheel bundle ***\"))\n\n# 1. special-token prefix vs LEFT truncation -----------------------------------------------\n_a = tok(\"probe\", add_special_tokens=True)[\"input_ids\"]\n_b = tok(\"probe\", add_special_tokens=False)[\"input_ids\"]\n_k = len(_a) - len(_b)\nif _k > 0 and _a[_k:] == _b:\n    _long = float((df.text.str.len() / 3.6 > MAX_LEN).mean())\n    print(f\"  tokenizer prepends {_k}: {tok.convert_ids_to_tokens(_a[:_k])}  \"\n          f\"({_long:.0%} of rows are long enough to be truncated)\")\n    print(f\"  PREPEND_SPECIALS={PREPEND_SPECIALS} -> prefix is \"\n          + (\"kept on every row\" if PREPEND_SPECIALS else\n             \"DROPPED on those rows; the model sees two input formats\"))\nelif _k:\n    print(f\"  tokenizer changes length by {_k} but not as a clean prefix -- inspect before \"\n          f\"trusting PREPEND_SPECIALS\")\nelse:\n    print(\"  tokenizer adds no prefix; left truncation is format-safe\")\n\n# 2. did LoRA actually attach, and to what -------------------------------------------------\n_lora = [n for n, m in model.backbone.named_modules() if hasattr(m, \"lora_A\")]\n_leaves = sorted({n.split(\".\")[-1] for n in _lora})\nassert _lora, \"LoRA matched NOTHING -- target_modules discovery failed for this family\"\nprint(f\"  LoRA on {len(_lora)} modules across {len(_leaves)} names: {_leaves}\")\n_fused = [l for l in _leaves if \"gate_up\" in l or \"qkv\" in l or \"c_attn\" in l]\nif _fused:\n    print(f\"  note: {_fused} is a FUSED projection. One rank-{LORA_R} adapter spans both \"\n          f\"halves, where a split architecture gets two independent rank-{LORA_R} ones, so \"\n          f\"equal LORA_R is NOT equal capacity across families. To match, pass \"\n          f\"rank_pattern={{'{_fused[0]}': {2*LORA_R}}} to LoraConfig in Cell 10.\")\n\n# 3. forward + pooling -----------------------------------------------------------------------\n_probe = Rows(df.text.iloc[:2].tolist(), df.is_correct.values[:2], tok, MAX_LEN,\n              PREPEND_SPECIALS)\n_batch = Collate(tok.pad_token_id)([_probe[0], _probe[1]])\nmodel.eval()\nwith torch.no_grad(), torch.autocast(\"cuda\", dtype=dtype, enabled=device == \"cuda\"):\n    _o1 = model(_batch[\"input_ids\"].to(device), _batch[\"attention_mask\"].to(device),\n                _batch[\"labels\"].to(device))\n    # Right padding + last-non-pad pooling means extra pad columns must change NOTHING.\n    # If they do, the pooling index or the padding side is wrong for this family.\n    _pad = torch.full((2, 8), tok.pad_token_id, dtype=torch.long)\n    _o2 = model(torch.cat([_batch[\"input_ids\"], _pad], 1).to(device),\n                torch.cat([_batch[\"attention_mask\"], torch.zeros_like(_pad)], 1).to(device))\nmodel.train()\nprint(f\"  forward: logits {tuple(_o1.logits.shape)}  loss {_o1.loss.item():.4f}  \"\n      f\"finite {bool(torch.isfinite(_o1.logits).all())}\")\n_delta = (_o1.logits.float() - _o2.logits.float()).abs().max().item()\nprint(f\"  pad-invariance: max |delta| {_delta:.2e} \"\n      + (\"ok\" if _delta < 5e-2 else \"*** POOLING IS READING PAD POSITIONS ***\"))\ndel _probe, _batch, _o1, _o2, _pad\ntorch.cuda.empty_cache() if device == \"cuda\" else None","metadata":{"_uuid":"ef4e4397-4fec-48ff-bcba-78ad55c7e755","_cell_guid":"58745c0c-4a66-4b5f-b201-80d642fb80f8","trusted":true,"collapsed":false,"jupyter":{"outputs_hidden":false},"execution":{"iopub.status.busy":"2026-08-13T11:47:55.914125Z","iopub.execute_input":"2026-08-13T11:47:55.914218Z","iopub.status.idle":"2026-08-13T11:48:00.460424Z","shell.execute_reply.started":"2026-08-13T11:47:55.914208Z","shell.execute_reply":"2026-08-13T11:48:00.460165Z"}},"outputs":[],"execution_count":null},{"cell_type":"code","source":"# =========================================================\n# Cell 12b - TOKEN_AUDIT (E2) + MICRO_BENCH (E3)\n# =========================================================\n# Two measurements that convert the notebook's compute assumptions into facts. Both are\n# cheap in code; the SECOND ONE IS NOT CHEAP IN TIME. MICRO_BENCH runs real training\n# steps, so it costs real GPU time -- roughly 12 min on a Blackwell 96 GB, 15 min on an\n# A100, 96 min on a Kaggle T4 at the default 20 steps. MICRO_BENCH_MAX_SEC caps it and the\n# projection scales from whatever completed, so lower the cap rather than skipping it: the\n# whole point is to learn the per-step cost BEFORE committing tens of hours to it.\nimport time, numpy as np, torch\n\n# ---- E2: is chars/3.6 right? ---------------------------------------------------------------\n# EVERY length decision in this notebook rests on that proxy -- Cell 7's token print, Cell\n# 13's approx_tokens bucketing, the MAX_LEN truncation estimate, the SHORT_UNCLEAR saving\n# and the inference budget. No tokenizer has ever confirmed it. If the true ratio is 3.0\n# rather than 3.6, the median row is 6,600 tokens not 5,500 and ~8% of rows truncate\n# instead of ~1%; if it is 4.5, MAX_LEN could come down and free real compute.\nMEAN_TOKENS = None\nif TOKEN_AUDIT:\n    _n = min(500, len(df))\n    _s = df.text.sample(_n, random_state=SEED)\n    _t0 = time.time()\n    _lens = np.array([len(tok(t, truncation=False, add_special_tokens=True)[\"input_ids\"])\n                      for t in _s])\n    _chars = _s.str.len().values\n    _ratio = _chars.sum() / _lens.sum()\n    _capped = np.minimum(_lens, MAX_LEN)\n    MEAN_TOKENS = float(_capped.mean())\n    print(f\"TOKEN_AUDIT on {_n} rows in {time.time()-_t0:.0f}s\")\n    print(f\"  chars/token          {_ratio:.3f}   (the notebook assumes 3.600 -> \"\n          f\"{'proxy OVERSTATES tokens' if _ratio > 3.6 else 'proxy UNDERSTATES tokens'} \"\n          f\"by {abs(100*(3.6/_ratio - 1)):.1f}%)\")\n    print(f\"  tokens: median {np.median(_lens):,.0f}  p95 {np.percentile(_lens,95):,.0f}  \"\n          f\"max {_lens.max():,.0f}\")\n    print(f\"  rows truncated at MAX_LEN {MAX_LEN:,}: {100*(_lens > MAX_LEN).mean():.2f}%   \"\n          f\"mean tokens actually processed {MEAN_TOKENS:,.0f}\")\n    # What the marker vocabulary really costs, so SHORT_UNCLEAR can be priced instead of\n    # guessed. A single-token glyph is only a win if [unclear] really is several tokens.\n    for _p in (\"[unclear]\", UNCLEAR_GLYPH, \"\\nT: \", \"\\nS: \", \"\\nB: \"):\n        _k = len(tok(_p, add_special_tokens=False)[\"input_ids\"])\n        print(f\"  {_p!r:14s} -> {_k} token(s)\")\n    _u = len(tok(\"[unclear]\", add_special_tokens=False)[\"input_ids\"])\n    _g = len(tok(UNCLEAR_GLYPH, add_special_tokens=False)[\"input_ids\"])\n    _per = float(df.text.str.count(r\"\\[unclear\\]\").mean())\n    print(f\"  SHORT_UNCLEAR would save ~{_per*(_u-_g):,.0f} tokens/row \"\n          f\"= {100*_per*(_u-_g)/max(MEAN_TOKENS,1):.1f}% \"\n          f\"({_per:.0f} markers/row x {_u-_g} tokens)\"\n          + (\"   [currently ON]\" if SHORT_UNCLEAR else \"   [currently OFF]\"))\n    if abs(_ratio - 3.6) / 3.6 > 0.10:\n        print(f\"  *** the 3.6 proxy is off by >10%. approx_tokens() only BUCKETS rows for \"\n              f\"batching, so this does not corrupt training -- but every wall-clock and \"\n              f\"truncation number quoted in this notebook is wrong by that factor. ***\")\n\n# ---- E3: what does a step actually cost? --------------------------------------------------\n# The wall-clock model behind \"~72 h per fold\" is a FLOP estimate at an assumed 40% MFU.\n# Twenty real steps settle it, tell you whether GRAD_CKPT=False fits in memory, and price\n# LORA_TOP_N -- before you spend a day finding out.\nif MICRO_BENCH and torch.cuda.is_available():\n    from torch.utils.data import DataLoader\n    # (steps + warmup) full accumulation groups. The old expression forgot the warmup\n    # and under-fed the loader, so it silently measured fewer steps than requested.\n    _warm = 3\n    _need = (MICRO_BENCH_STEPS + _warm) * BS * ACCUM\n    _bench_rows = df.sample(min(len(df), _need), random_state=SEED)\n    print(f\"MICRO_BENCH: up to {MICRO_BENCH_STEPS} steps after {_warm} warmup \"\n          f\"({_need:,} rows, cap {MICRO_BENCH_MAX_SEC}s) ...\", flush=True)\n    _ds = Rows(_bench_rows.text.tolist(), _bench_rows.is_correct.values, tok, MAX_LEN,\n               PREPEND_SPECIALS)\n    _dl = DataLoader(_ds, batch_size=BS, shuffle=False, collate_fn=Collate(tok.pad_token_id))\n    # The benchmark performs REAL optimiser steps, so it would leave the model trained --\n    # LoRA B no longer zero, head.bias drifted off logit(base rate) -- before Cell 14 starts,\n    # silently and unrecorded. Snapshot the trainable tensors and restore them afterwards so\n    # this stays a measurement and not an accidental warmup.\n    _snap = {n: p.detach().clone() for n, p in model.named_parameters() if p.requires_grad}\n    _opt = torch.optim.AdamW([p for p in model.parameters() if p.requires_grad], lr=1e-5)\n    torch.cuda.reset_peak_memory_stats(); torch.cuda.synchronize()\n    model.train()\n    _tok_seen, _t0, _done = 0, None, 0\n    for _i, _b in enumerate(_dl):\n        if _done >= MICRO_BENCH_STEPS + _warm:\n            break\n        if _t0 is not None and time.time() - _t0 > MICRO_BENCH_MAX_SEC:\n            print(f\"  (hit the {MICRO_BENCH_MAX_SEC}s cap after {_done - _warm} measured \"\n                  f\"steps -- projecting from those)\", flush=True)\n            break\n        _b = {k: v.to(device) for k, v in _b.items()}\n        with torch.autocast(\"cuda\", dtype=dtype):\n            _loss = model(**_b).loss / ACCUM\n        _loss.backward()\n        _tok_seen += int(_b[\"attention_mask\"].sum())\n        if (_i + 1) % ACCUM == 0:\n            torch.nn.utils.clip_grad_norm_(\n                [p for p in model.parameters() if p.requires_grad], MAX_GRAD_NORM)\n            _opt.step(); _opt.zero_grad(set_to_none=True)\n            _done += 1\n            if _done == _warm:                     # start the clock after warmup\n                torch.cuda.synchronize(); _t0 = time.time(); _tok_seen = 0\n    torch.cuda.synchronize()\n    _steps = max(1, _done - _warm)\n    _el = time.time() - _t0\n    _spe = max(1, len(df) * 0.8 // (BS * ACCUM))   # ~fold-0 train size\n    print(f\"\\nMICRO_BENCH: {_steps} optimiser steps (after {_warm} warmup) in {_el:.1f}s\")\n    if _steps < 3:\n        print(\"  *** fewer than 3 measured steps -- ms/step is noisy. Raise \"\n              \"MICRO_BENCH_MAX_SEC if you need a tighter number. ***\")\n    print(f\"  {1000*_el/_steps:,.0f} ms/step   {_tok_seen/_el:,.0f} tokens/s   \"\n          f\"peak {torch.cuda.max_memory_allocated()/1e9:.1f} GB \"\n          f\"of {torch.cuda.get_device_properties(0).total_memory/1e9:.0f} GB\")\n    print(f\"  projected: {_el/_steps*_spe/3600:.1f} h/epoch, \"\n          f\"{_el/_steps*_spe*EPOCHS/3600:.1f} h for {EPOCHS} epochs (train only, \"\n          f\"eval excluded)   [GRAD_CKPT={GRAD_CKPT}, LORA_TOP_N={LORA_TOP_N}, BS={BS}]\")\n    _free = (torch.cuda.get_device_properties(0).total_memory\n             - torch.cuda.max_memory_allocated()) / 1e9\n    print(f\"  headroom: {_free:.0f} GB free -- raise BS while it lasts, or set \"\n          f\"GRAD_CKPT=False if it is not already\")\n    # restore, and PROVE it -- a silent no-op here would be worse than the bug\n    _opt.zero_grad(set_to_none=True)\n    with torch.no_grad():\n        _moved = 0.0\n        for _n, _p in model.named_parameters():\n            if _p.requires_grad:\n                _moved = max(_moved, float((_p - _snap[_n]).abs().max()))\n                _p.copy_(_snap[_n])\n        _resid = max(float((_p - _snap[_n]).abs().max())\n                     for _n, _p in model.named_parameters() if _p.requires_grad)\n    print(f\"  weights restored: benchmark had moved them by up to {_moved:.2e}, \"\n          f\"residual after restore {_resid:.2e}\")\n    assert _resid == 0.0, \"MICRO_BENCH failed to restore the model -- do NOT train from here\"\n    del _opt, _dl, _ds, _snap\n    torch.cuda.empty_cache()\nelif MICRO_BENCH:\n    print(\"\\nMICRO_BENCH skipped -- no CUDA\")\n\n# ---- E6: what actually fits in the 6-hour submission window? -------------------------------\nif INFER_COST_MODEL:\n    print()\n    plan([MODEL if MODEL in MODELS else \"Qwen/Qwen3-8B\"], n_rows=10508,\n         mean_tokens=MEAN_TOKENS)\n","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-08-13T11:48:00.460827Z","iopub.execute_input":"2026-08-13T11:48:00.460921Z","iopub.status.idle":"2026-08-13T11:54:03.942699Z","shell.execute_reply.started":"2026-08-13T11:48:00.460912Z","shell.execute_reply":"2026-08-13T11:54:03.942435Z"}},"outputs":[],"execution_count":null},{"cell_type":"code","source":"# =========================================================\n# Cell 13 - TRAIN (HF Trainer)\n# =========================================================\nimport glob, importlib, inspect, json, math, os, platform, shutil, time\nimport numpy as np, pandas as pd, torch\nimport torch.nn.functional as F\nfrom sklearn.metrics import log_loss, roc_auc_score\nfrom torch.utils.data import SequentialSampler\nfrom transformers import Trainer, TrainingArguments, TrainerCallback\n\n# ---- split -----------------------------------------------------------------------------\nif FOLD >= 0:\n    tr_idx, va_idx = split(df, FOLD, FOLD_COL, purge=USE_PURGE)\n    tr, va = df.iloc[tr_idx].reset_index(drop=True), df.iloc[va_idx].reset_index(drop=True)\n    sess_leak = int(va.session_id.isin(set(tr.session_id)).sum())\n    obj_leak = int(va.learning_objective_id.isin(set(tr.learning_objective_id)).sum())\n    print(f\"[{CV_SCHEME}] fold {FOLD}: train {len(tr):,}  val {len(va):,}  \"\n          f\"unused {len(df)-len(tr)-len(va):,}  | session leak {sess_leak}  \"\n          f\"objective overlap {obj_leak}\")\n    if not EXPECT_SESSION_LEAK:\n        assert not sess_leak, (f\"scheme {CV_SCHEME} holds sessions intact but {sess_leak} \"\n                               f\"val rows share a session with train -- check make_folds\")\n    if not EXPECT_OBJECTIVE_LEAK:\n        assert not obj_leak, (f\"scheme {CV_SCHEME} holds objectives intact but {obj_leak} \"\n                              f\"val rows share an objective with train -- check make_folds\")\nelse:\n    tr, va = df.reset_index(drop=True), df.iloc[:512].reset_index(drop=True)\n    print(f\"full-data run: train {len(tr):,} (val is an in-sample peek, ignore it)\")\n    assert not LOAD_BEST, \"FOLD=-1 validates in-sample; LOAD_BEST would select on it\"\n\napprox = tr.text.str.len().values / 3.6\nif (approx > MAX_LEN).mean() > 0.10:\n    print(f\"WARNING: ~{(approx > MAX_LEN).mean():.0%} of rows exceed MAX_LEN {MAX_LEN}; \"\n          f\"those keep only their final {MAX_LEN} tokens (median ~{np.median(approx):.0f})\")\n\ndef approx_tokens(frame):\n    \"\"\"Bucketing proxy: chars/3.6, capped at MAX_LEN. The cap matters -- every row longer\n    than MAX_LEN is truncated to exactly MAX_LEN, so they are all the same length in the\n    batch and sorting them against each other by raw character count is noise.\n\n    This only BUCKETS rows for batching, so TOKEN_AUDIT finding the 3.6 ratio wrong costs\n    a little padding efficiency, never correctness.\"\"\"\n    return np.minimum(frame.text.str.len().values / 3.6, MAX_LEN).astype(int)\n\n# The validation fold serves two jobs. Training-time eval wants to be cheap and only needs\n# to rank checkpoints; the OOF feeds calibration and any blend downstream and must cover\n# every row of the fold. Built separately; only the first is subset.\nva_oof = va.copy()\nva_eval = va_oof\nif EVAL_SUBSET and EVAL_SUBSET < len(va_oof):\n    va_eval = va_oof.sample(n=EVAL_SUBSET, random_state=SEED)\nif SORT_EVAL_BY_LENGTH:\n    # E10. Split out from GROUP_BY_LENGTH so the two can move independently: this one is\n    # worth ~24.5% of every eval pass and, because _get_eval_sampler is sequential, it is\n    # also what makes the OOF join positionally valid. Descending, so the largest batch\n    # runs first and an OOM shows up in the first seconds rather than 80% of the way in.\n    va_eval = va_eval.iloc[np.argsort(-approx_tokens(va_eval), kind=\"stable\")]\n    va_oof = va_oof.iloc[np.argsort(-approx_tokens(va_oof), kind=\"stable\")]\nva_eval = va_eval.reset_index(drop=True)\nva_oof = va_oof.reset_index(drop=True)\n\nds_tr = Rows(tr.text.tolist(), tr.is_correct.values, tok, MAX_LEN, PREPEND_SPECIALS,\n             lengths=approx_tokens(tr))\nds_va = Rows(va_eval.text.tolist(), va_eval.is_correct.values, tok, MAX_LEN,\n             PREPEND_SPECIALS, lengths=approx_tokens(va_eval))\nds_oof = Rows(va_oof.text.tolist(), va_oof.is_correct.values, tok, MAX_LEN,\n              PREPEND_SPECIALS, lengths=approx_tokens(va_oof))\ncoll = Collate(tok.pad_token_id)\n\nif EVAL_SUBSET and EVAL_SUBSET < len(va_oof):\n    _se = 0.0054 * (7014 / max(1, len(ds_va))) ** 0.5\n    print(f\"eval on {len(ds_va):,} of {len(ds_oof):,} val rows (EVAL_SUBSET, seed {SEED}) \"\n          f\"-- one-fold SE ~{_se:.4f} vs 0.0054 on the full fold; final OOF still covers \"\n          f\"all {len(ds_oof):,}\")\n\n# ---- cadence -> Trainer strategies -------------------------------------------------------\nsteps_per_epoch = max(1, len(ds_tr) // (BS * ACCUM))\ntotal_steps = max(1, int(steps_per_epoch * EPOCHS))\n\ndef hf_cadence(strategy, every, spe, name):\n    \"\"\"(strategy, steps) in Trainer's vocabulary.\n\n    *_EVERY is a FRACTION OF AN EPOCH under both \"epoch\" and \"steps\". Deriving the step\n    count from steps_per_epoch is the point -- a hand-typed absolute step count does not\n    divide total_steps in general, and the remainder is the tail of the cosine schedule,\n    i.e. exactly the part you most want measured.\n    \"\"\"\n    if strategy == \"no\" or not every:\n        return \"no\", None\n    frac = float(every)\n    assert frac > 0, f\"{name} must be > 0 (fraction of an epoch)\"\n    if strategy == \"epoch\" and frac == 1.0:\n        return \"epoch\", None\n    return \"steps\", max(1, round(frac * spe))\n\neval_strategy, eval_steps = hf_cadence(EVAL_STRATEGY, EVAL_EVERY, steps_per_epoch, \"EVAL_EVERY\")\nsave_strategy, save_steps = hf_cadence(SAVE_STRATEGY, SAVE_EVERY, steps_per_epoch, \"SAVE_EVERY\")\n\nload_best = LOAD_BEST and eval_strategy != \"no\"\nif load_best and save_strategy != eval_strategy:\n    print(f\"note: LOAD_BEST needs save_strategy == eval_strategy; forcing save to \"\n          f\"{eval_strategy}/{eval_steps}\")\n    save_strategy, save_steps = eval_strategy, eval_steps\nif load_best and eval_strategy == \"steps\" and save_steps % eval_steps:\n    print(f\"note: save_steps {save_steps} is not a multiple of eval_steps {eval_steps}; \"\n          f\"forcing save_steps = {eval_steps}\")\n    save_steps = eval_steps\n\nlog_strategy, log_steps = (eval_strategy, eval_steps) if LOG_WITH_EVAL else (\"steps\", LOGGING_STEPS)\nif log_strategy == \"no\":\n    log_strategy, log_steps = \"steps\", LOGGING_STEPS\n\n# ---- metrics ------------------------------------------------------------------------------\ndef compute_metrics(p):\n    z = np.asarray(p.predictions).reshape(-1).astype(np.float64)\n    y = np.asarray(p.label_ids).reshape(-1)\n    pr = np.clip(1/(1+np.exp(-z)), 1e-6, 1-1e-6)\n    out = {\"logloss\": float(log_loss(y, pr, labels=[0, 1]))}\n    if len(np.unique(y)) > 1:\n        out[\"auc\"] = float(roc_auc_score(y, pr))\n    return out\n\n# ---- calibration + blend ---------------------------------------------------------------------\ndef fit_logistic(X, y, iters=200, l2=1e-6):\n    \"\"\"Newton-fitted logistic regression. Convex, so this is the exact optimum.\n\n    Used for both the 2-parameter Platt map and the small scalar blend, so the two cannot\n    disagree about how a probability is produced.\n    \"\"\"\n    X = np.asarray(X, dtype=np.float64); y = np.asarray(y, dtype=np.float64)\n    X = np.column_stack([X, np.ones(len(X))]) if X.ndim > 1 else \\\n        np.column_stack([X, np.ones(len(X))])\n    w = np.zeros(X.shape[1]); w[0] = 1.0\n    for _ in range(iters):\n        p = 1.0/(1.0 + np.exp(-(X @ w)))\n        g = X.T @ (p - y) / len(y) + l2 * w\n        H = (X.T * (p*(1-p))) @ X / len(y) + (l2 + 1e-9) * np.eye(X.shape[1])\n        step = np.linalg.solve(H, g)\n        w -= step\n        if np.abs(step).max() < 1e-12:\n            break\n    return w\n\ndef fit_platt(z, y):\n    \"\"\"p = sigmoid(a*logit + b). a < 1 shrinks an over-confident model toward the base rate.\"\"\"\n    w = fit_logistic(np.asarray(z, dtype=np.float64).reshape(-1, 1), y)\n    return float(w[0]), float(w[1])\n\ndef _grouped_folds(groups, k=5, seed=0):\n    \"\"\"E16. Assign whole GROUPS to inner folds, largest-first onto the lightest fold.\n\n    The inner split inside calibration_report must not put two rows of the same session on\n    opposite sides: 58.8% of rows have a sibling, siblings share ~99.5% of their prompt, and\n    a model's error on them is correlated. A random inner split therefore reports a\n    calibration gain that is partly memorised sibling structure.\n    \"\"\"\n    s = pd.Series(groups).value_counts()\n    load = np.zeros(k); assign = {}\n    for g, n in s.items():\n        i = int(np.argmin(load)); assign[g] = i; load[i] += n\n    return pd.Series(groups).map(assign).values\n\ndef calibration_report(z, y, groups=None, k=5, seed=0):\n    \"\"\"Honest (out-of-fold) estimate of what calibration buys, plus the map to ship.\n\n    Fitting a Platt map and scoring it on the SAME rows is in-sample and always looks good,\n    so the reported gain comes from a k-fold split INSIDE the OOF -- session-grouped when\n    CALIB_GROUPED_CV is on. The (a, b) written out is the FULL fit; that one is applied to\n    TEST, never to these rows. Refit on POOLED OOF in build_submission.py before shipping.\n    \"\"\"\n    z = np.asarray(z, dtype=np.float64); y = np.asarray(y, dtype=np.float64)\n    if CALIB_GROUPED_CV and groups is not None:\n        part = _grouped_folds(groups, k, seed)\n    else:\n        part = np.random.default_rng(seed).permutation(len(z)) % k\n    p_oof = np.empty(len(z))\n    for i in range(k):\n        te = part == i\n        if te.all() or not te.any():\n            p_oof[te] = 1.0/(1.0 + np.exp(-z[te])); continue\n        a, b = fit_platt(z[~te], y[~te])\n        p_oof[te] = 1.0/(1.0 + np.exp(-(a*z[te] + b)))\n    raw = float(log_loss(y, np.clip(1/(1+np.exp(-z)), 1e-6, 1-1e-6), labels=[0, 1]))\n    cal = float(log_loss(y, np.clip(p_oof, 1e-6, 1-1e-6), labels=[0, 1]))\n    a, b = fit_platt(z, y)\n    return {\"a\": a, \"b\": b, \"logloss_raw\": raw, \"logloss_calibrated\": cal,\n            \"gain\": raw - cal, \"grouped_inner_cv\": bool(CALIB_GROUPED_CV and groups is not None)}\n\nBLEND_FEATURES = [\"n_resp_in_session\", \"n_stu_turns\", \"n_long_stu_turns\", \"n_utt\"]\n\ndef blend_report(z, y, frame, groups=None, k=5, seed=0):\n    \"\"\"E27. Does a small logistic on [llm_logit, scalars] beat the calibrated logit alone?\n\n    Fitted on the OOF, so it needs NO retraining and cannot change the model -- worst case\n    it reports zero and you ignore it. The honest question is not what the scalars are\n    worth on their own (n_resp_in_session alone is +0.0098) but what they are worth ON TOP\n    of a logit the LLM produced while reading a prompt that already states some of them.\n    Expect most of it to be absorbed; the number below is what is left.\n\n    Cross-fitted with the same session grouping as the calibration, for the same reason.\n    \"\"\"\n    z = np.asarray(z, dtype=np.float64); y = np.asarray(y, dtype=np.float64)\n    feats_ = [c for c in BLEND_FEATURES if c in frame.columns]\n    if len(z) < BLEND_MIN_ROWS or not feats_:\n        return {\"skipped\": f\"need >= {BLEND_MIN_ROWS} rows and >=1 feature; \"\n                           f\"have {len(z)} rows, {len(feats_)} features\"}\n    X = np.column_stack([z] + [frame[c].values.astype(np.float64) for c in feats_])\n    mu, sd = X[:, 1:].mean(0), X[:, 1:].std(0) + 1e-9\n    X[:, 1:] = (X[:, 1:] - mu) / sd\n    part = _grouped_folds(groups, k, seed) if (CALIB_GROUPED_CV and groups is not None) \\\n        else np.random.default_rng(seed).permutation(len(z)) % k\n    p_base, p_blend = np.empty(len(z)), np.empty(len(z))\n    for i in range(k):\n        te = part == i\n        if te.all() or not te.any():\n            continue\n        a, b = fit_platt(z[~te], y[~te])\n        p_base[te] = 1.0/(1.0 + np.exp(-(a*z[te] + b)))\n        w = fit_logistic(X[~te], y[~te])\n        p_blend[te] = 1.0/(1.0 + np.exp(-(X[te] @ w[:-1] + w[-1])))\n    base = float(log_loss(y, np.clip(p_base, 1e-6, 1-1e-6), labels=[0, 1]))\n    bl = float(log_loss(y, np.clip(p_blend, 1e-6, 1-1e-6), labels=[0, 1]))\n    w_full = fit_logistic(X, y)\n    return {\"features\": feats_, \"mu\": mu.tolist(), \"sd\": sd.tolist(),\n            \"w\": w_full.tolist(), \"logloss_platt\": base, \"logloss_blend\": bl,\n            \"gain_over_platt\": base - bl, \"n\": int(len(z))}\n\ndef compare_oof(path_a, path_b, n_boot=2000, seed=0):\n    \"\"\"PAIRED comparison of two oof_*.csv files. The only honest way to call an A/B here.\n\n    Both arms score the same rows, so the row-sampling error is common and cancels. A LEVEL\n    standard error (the 0.0054 one-fold figure) is the wrong yardstick for a DIFFERENCE and\n    is several times too large.\n    \"\"\"\n    a = pd.read_csv(path_a); b = pd.read_csv(path_b)\n    m = a.merge(b, on=\"response_id\", suffixes=(\"_a\", \"_b\"))\n    assert len(m), \"no shared response_id -- these OOFs are from different folds\"\n    if len(m) < len(a) or len(m) < len(b):\n        print(f\"  note: comparing on {len(m):,} shared rows of {len(a):,}/{len(b):,}\")\n    y = m[\"is_correct_a\"].values.astype(float)\n    la = -(y*np.log(np.clip(m.prob_a, 1e-6, 1-1e-6)) + (1-y)*np.log(np.clip(1-m.prob_a, 1e-6, 1-1e-6)))\n    lb = -(y*np.log(np.clip(m.prob_b, 1e-6, 1-1e-6)) + (1-y)*np.log(np.clip(1-m.prob_b, 1e-6, 1-1e-6)))\n    d = (la - lb).values\n    rng = np.random.default_rng(seed)\n    boot = np.array([d[rng.integers(0, len(d), len(d))].mean() for _ in range(n_boot)])\n    lo, hi = np.percentile(boot, [2.5, 97.5])\n    print(f\"  A {os.path.basename(path_a)}: {la.mean():.5f}\")\n    print(f\"  B {os.path.basename(path_b)}: {lb.mean():.5f}\")\n    print(f\"  A - B = {d.mean():+.5f}  95% CI [{lo:+.5f}, {hi:+.5f}]  \"\n          f\"paired SE {d.std()/np.sqrt(len(d)):.5f}\")\n    print(\"  \" + (\"B is better\" if lo > 0 else \"A is better\" if hi < 0 else\n                  \"INDISTINGUISHABLE -- the CI straddles 0\"))\n    return {\"mean_diff\": float(d.mean()), \"ci\": [float(lo), float(hi)], \"n\": int(len(d))}\n\n# ---- callbacks -------------------------------------------------------------------------------\nclass FinalStepEval(TrainerCallback):\n    \"\"\"Force one eval + save on the very last optimiser step.\n\n    *_EVERY is a fraction of an epoch, so eval_steps rarely divides total_steps exactly.\n    Without this the remainder -- the tail of the cosine schedule, at the lowest LR -- is\n    never evaluated and never checkpointed.\n    \"\"\"\n    def on_step_end(self, args, state, control, **kw):\n        if state.max_steps and state.global_step >= state.max_steps:\n            control.should_evaluate = True\n            control.should_save = True\n        return control\n\nclass TimeBudget(TrainerCallback):\n    \"\"\"E31. Stop cleanly after TIME_BUDGET_MIN and leave a resumable checkpoint.\n\n    on_step_end runs before Trainer services should_save, so setting both flags here\n    produces a checkpoint and then a clean exit. SAVE_ONLY_MODEL=False and the\n    _load_from_checkpoint override mean RESUME already works; nothing currently triggers\n    the stop.\n\n    A budget-stopped checkpoint sits mid-cosine at a high LR and is NOT a fair read of the\n    endpoint. Only compare completed runs.\n    \"\"\"\n    def __init__(self, minutes):\n        self.limit = minutes * 60.0\n        self.t0 = None\n    def on_train_begin(self, args, state, control, **kw):\n        self.t0 = time.time()\n    def on_step_end(self, args, state, control, **kw):\n        if self.t0 and time.time() - self.t0 > self.limit:\n            el = (time.time() - self.t0) / 60\n            print(f\"\\n[TIME_BUDGET] {el:.0f} min elapsed at step {state.global_step}/\"\n                  f\"{state.max_steps} -- saving and stopping. Set RESUME='auto' to continue. \"\n                  f\"This checkpoint is mid-schedule and is not comparable to a finished run.\",\n                  flush=True)\n            control.should_save = True\n            control.should_training_stop = True\n        return control\n\nclass EvalLogger(TrainerCallback):\n    \"\"\"One plain line per evaluation, plus the whole history at the end.\n\n    The notebook table is an HTML widget that redraws in place: it shows the current state\n    in the browser and writes nothing to the log stream, so a committed run ends with no\n    record of how validation moved. These prints are what survives.\n    \"\"\"\n    def __init__(self):\n        self.rows, self.best, self.t0 = [], float(\"inf\"), None\n    def on_train_begin(self, args, state, control, **kw):\n        self.t0 = time.time()\n    def on_evaluate(self, args, state, control, metrics=None, **kw):\n        if not metrics:\n            return\n        loss = metrics.get(\"eval_loss\", float(\"nan\"))\n        ll = metrics.get(\"eval_logloss\", float(\"nan\"))\n        auc = metrics.get(\"eval_auc\", float(\"nan\"))\n        mark = \"\"\n        if loss < self.best:\n            self.best, mark = loss, \"  <- best\"\n        step, ep = int(state.global_step), (state.epoch or 0.0)\n        train = next((l[\"loss\"] for l in reversed(state.log_history) if \"loss\" in l),\n                     float(\"nan\"))\n        el = (time.time() - self.t0) / 60 if self.t0 else 0.0\n        el_s = f\"{el:.0f}m\" if el < 90 else f\"{el/60:.1f}h\"\n        print(f\"[eval] step {step:>5}/{int(state.max_steps)}  epoch {ep:5.2f}  \"\n              f\"train {train:.5f}  val_loss {loss:.5f}  val_logloss {ll:.5f}  \"\n              f\"val_auc {auc:.5f}  {el_s}{mark}\", flush=True)\n        self.rows.append((step, ep, train, loss, auc))\n    def on_train_end(self, args, state, control, **kw):\n        if not self.rows:\n            return\n        print(f\"\\neval history -- fold {FOLD}, seed {SEED}, cv {CV_SCHEME}\")\n        print(f\"{'step':>6} {'epoch':>6} {'train':>10} {'val_loss':>10} {'val_auc':>9}\")\n        for s, e, t, l, a in self.rows:\n            print(f\"{s:6d} {e:6.2f} {t:10.5f} {l:10.5f} {a:9.4f}\")\n        b = min(self.rows, key=lambda r: r[3])\n        print(f\"best: step {b[0]}  epoch {b[1]:.2f}  val_loss {b[3]:.5f}  val_auc {b[4]:.4f}\"\n              f\"   (constant predictor is 0.60876; one-fold SE is 0.0054)\", flush=True)\n\nclass Epoch1Snapshot(TrainerCallback):\n    \"\"\"E15. Dump a full OOF at the end of epoch 1 as well as at the end of training.\n\n    Costs one extra OOF pass (~1.8% of a 3-epoch run) and answers \"were epochs 2 and 3\n    worth two thirds of the budget?\" with a PAIRED comparison of two real OOF files\n    instead of an eyeballed validation curve. Read it before deciding to cut EPOCHS.\n    \"\"\"\n    def __init__(self):\n        self.done = False\n    def on_epoch_end(self, args, state, control, **kw):\n        if self.done or (state.epoch or 0) < 0.999 or EPOCHS <= 1.0:\n            return control\n        self.done = True\n        print(f\"\\n[snapshot] end of epoch 1 (step {state.global_step}) -- dumping OOF\",\n              flush=True)\n        dump(\"epoch1\")\n        return control\n\n# ---- Trainer subclass ----------------------------------------------------------------------\nclass Args(TrainingArguments):\n    @property\n    def place_model_on_device(self):\n        # The model is already on the GPU and may be 4-bit. Letting Trainer call .to() on\n        # the wrapper walks nn.Module._apply over bnb Params4bit and can strip quant_state.\n        return False\n\nclass SiblingBatchSampler(torch.utils.data.Sampler):\n    \"\"\"E34. Keep rows of the same session in the same micro-batch.\n\n    Without this a pairwise sibling term is nearly inert: LengthGroupedSampler chunks into\n    megabatches of ~800 rows and then sorts by length, so two rows of one session land in\n    the same micro-batch of 2 only about 1.4% of the time.\n\n    Note this REPLACES length grouping, so it changes batch composition as well as adding a\n    loss term. That is two simultaneous changes to the optimisation regime -- A/B it against\n    a run with PAIR_LOSS_W=0 and this sampler ON, not against the default.\n    \"\"\"\n    def __init__(self, session_ids, batch_size, lengths=None, seed=0):\n        self.groups, self.bs, self.seed = {}, batch_size, seed\n        for i, s in enumerate(session_ids):\n            self.groups.setdefault(s, []).append(i)\n        self.lengths = lengths\n        self.n = len(session_ids)\n    def __len__(self):\n        return self.n\n    def __iter__(self):\n        rng = np.random.default_rng(self.seed)\n        keys = list(self.groups)\n        rng.shuffle(keys)\n        if self.lengths is not None:            # keep some length coherence between groups\n            keys.sort(key=lambda k: -max(self.lengths[i] for i in self.groups[k]))\n        out = []\n        for k in keys:\n            out.extend(self.groups[k])\n        return iter(out)\n\nclass LMClassifierTrainer(Trainer):\n    \"\"\"Trainer that knows our checkpoint layout is adapter/ + head.pt, not a state dict.\"\"\"\n\n    def __init__(self, *a, tokenizer_for_save=None, sess_of_row=None, **kw):\n        super().__init__(*a, **kw)\n        self.tok_for_save = tokenizer_for_save\n        self.sess_of_row = sess_of_row\n        self.pair_seen = 0\n\n    def create_optimizer(self):\n        \"\"\"Three param groups: LoRA (decayed), head weight (fast, decayed), head bias\n        (fast, NOT decayed). Decaying the bias would pull it off logit(base rate), which is\n        exactly what HEAD_BIAS_INIT put there. The scheduler is a LambdaLR, so it scales\n        each group's own base_lr.\"\"\"\n        if self.optimizer is None:\n            named = list(self.model.named_parameters())\n            head_b = [p for n, p in named if p.requires_grad and n.startswith(\"head.bias\")]\n            head_w = [p for n, p in named if p.requires_grad and n.startswith(\"head.weight\")]\n            body = [p for n, p in named if p.requires_grad and not n.startswith(\"head.\")]\n            self.optimizer = torch.optim.AdamW(\n                [{\"params\": body, \"lr\": self.args.learning_rate,\n                  \"weight_decay\": self.args.weight_decay},\n                 {\"params\": head_w, \"lr\": HEAD_LR, \"weight_decay\": self.args.weight_decay},\n                 {\"params\": head_b, \"lr\": HEAD_LR, \"weight_decay\": 0.0}],\n                lr=self.args.learning_rate, weight_decay=self.args.weight_decay)\n        return self.optimizer\n\n    def compute_loss(self, model, inputs, return_outputs=False, **kw):\n        \"\"\"BCE, plus an optional pairwise term on DISCORDANT SIBLINGS (E34).\n\n        5,323 within-session pairs carry opposite labels while sharing ~99.5% of their\n        prompt characters, so they are the only place the objective-conditioning channel is\n        directly supervised -- and per-row BCE on them is dominated by the shared\n        transcript. The term asks only that the correct sibling rank higher.\n\n        Kept at or below 0.1: a rank loss is NOT a proper scoring rule and log loss is. If\n        `a` in the calibration report drops relative to a control run, this is why.\n        \"\"\"\n        sess = inputs.pop(\"session_idx\", None)\n        out = model(**inputs)\n        loss = out.loss\n        if PAIR_LOSS_W > 0 and sess is not None:\n            lab = inputs[\"labels\"].float()\n            same = sess[:, None] == sess[None, :]\n            diff = lab[:, None] != lab[None, :]\n            iu = torch.triu(torch.ones_like(same, dtype=torch.bool), diagonal=1)\n            i, j = (same & diff & iu).nonzero(as_tuple=True)\n            if i.numel():\n                self.pair_seen += int(i.numel())\n                z = out.logits.float()\n                loss = loss + PAIR_LOSS_W * F.binary_cross_entropy_with_logits(\n                    z[i] - z[j], (lab[i] > lab[j]).float())\n        return (loss, out) if return_outputs else loss\n\n    def _get_train_sampler(self, *a, **kw):\n        \"\"\"Feed our precomputed lengths in. Trainer's own path only reads a `length` column\n        off a datasets.Dataset; with a plain torch Dataset it falls through to tokenising\n        every row to measure it, which here is a second pass over 400 MB of transcript.\"\"\"\n        ds = getattr(self, \"train_dataset\", None)\n        lengths = getattr(ds, \"lengths\", None)\n        if PAIR_SIBLING_BATCH and self.sess_of_row is not None:\n            return SiblingBatchSampler(self.sess_of_row, self.args.train_batch_size,\n                                       lengths, seed=SEED)\n        if not self.args.group_by_length or lengths is None:\n            return super()._get_train_sampler(*a, **kw)\n        from transformers.trainer_pt_utils import LengthGroupedSampler\n        return LengthGroupedSampler(\n            self.args.train_batch_size * self.args.gradient_accumulation_steps,\n            dataset=ds, lengths=list(lengths), model_input_name=\"input_ids\")\n\n    def _get_eval_sampler(self, eval_dataset=None, *a, **kw):\n        \"\"\"ALWAYS sequential.\n\n        Upstream's _get_eval_sampler returns a LengthGroupedSampler when\n        args.group_by_length is set -- and that sampler starts with\n        torch.randperm(len(lengths)). evaluation_loop concatenates in dataloader order and\n        never restores dataset order, so dump() would pair each response_id with some other\n        row's logit: a silently scrambled OOF, while the in-training eval_logloss stayed\n        correct (labels ride along in the same permuted batches, so the metric is\n        order-invariant and nothing looks wrong).\n\n        ds_va / ds_oof are already sorted by descending approx_tokens, so sequential order\n        IS length-grouped -- we keep the batching win and lose the permutation. It also\n        avoids upstream's `lengths=None` fallback, which re-tokenises the whole fold on\n        every single evaluate() call.\n        \"\"\"\n        return SequentialSampler(eval_dataset if eval_dataset is not None\n                                 else self.eval_dataset)\n\n    def _save(self, output_dir=None, state_dict=None):\n        out = output_dir or self.args.output_dir\n        save_checkpoint(self.model, self.tok_for_save, out,\n                        dict(CKPT_META, step=int(self.state.global_step),\n                             epoch=self.state.epoch))\n\n    def _load_from_checkpoint(self, resume_from_checkpoint, model=None):\n        load_adapter_and_head(model or self.model, resume_from_checkpoint)\n\n    def _load_best_model(self):\n        if self.state.best_model_checkpoint:\n            load_adapter_and_head(self.model, self.state.best_model_checkpoint)\n            print(f\"loaded best checkpoint {self.state.best_model_checkpoint} \"\n                  f\"(eval_loss {self.state.best_metric:.5f})\")\n\n# Metadata stamped into every checkpoint, so an adapter directory is self-describing and\n# build_submission.py can refuse a mismatch instead of scoring one.\nCKPT_META = {\"base_model\": MODEL, \"model_tag\": _MODEL_TAG, \"max_len\": MAX_LEN,\n             \"fold\": FOLD, \"seed\": SEED, \"use_4bit\": USE_4BIT, \"pool\": POOL,\n             \"use_chat_template\": USE_CHAT_TEMPLATE,\n             \"lora_r\": LORA_R, \"lora_alpha\": LORA_ALPHA, \"lora_top_n\": LORA_TOP_N,\n             \"render_key\": _CTX_KEY, \"add_session_meta\": ADD_SESSION_META, **RENDER_KW}\n\n# ---- arguments ------------------------------------------------------------------------------\n_sig = set(inspect.signature(TrainingArguments.__init__).parameters)\n_EVAL_KEY = \"eval_strategy\" if \"eval_strategy\" in _sig else \"evaluation_strategy\"\n\n_kw = {\n    \"output_dir\": OUT,\n    \"overwrite_output_dir\": False,\n    \"num_train_epochs\": EPOCHS,\n    \"per_device_train_batch_size\": BS,\n    \"per_device_eval_batch_size\": EVAL_BS,\n    \"gradient_accumulation_steps\": ACCUM,\n    \"group_by_length\": GROUP_BY_LENGTH,\n    \"learning_rate\": LR,\n    \"weight_decay\": WEIGHT_DECAY,\n    \"warmup_ratio\": WARMUP_RATIO,\n    \"lr_scheduler_type\": \"cosine\",\n    \"max_grad_norm\": MAX_GRAD_NORM,\n    \"bf16\": dtype == torch.bfloat16,\n    \"fp16\": dtype == torch.float16,\n    \"gradient_checkpointing\": False,        # already handled on the backbone in Cell 10\n    \"dataloader_num_workers\": WORKERS,\n    # NOT True. dataloader_drop_last is not a train-only flag: Trainer passes it into\n    # get_eval_dataloader AND get_test_dataloader, so trainer.predict(ds_oof) would return\n    # floor(n/EVAL_BS)*EVAL_BS rows. No fold size here is divisible by EVAL_BS=4, so dump()\n    # died on a length mismatch AFTER the whole run, before writing a single artefact.\n    \"dataloader_drop_last\": False,\n    \"remove_unused_columns\": False,\n    \"label_names\": [\"labels\"],\n    \"logging_strategy\": log_strategy,\n    \"logging_steps\": log_steps,\n    \"logging_first_step\": True,\n    _EVAL_KEY: eval_strategy,\n    \"eval_steps\": eval_steps,\n    \"save_strategy\": save_strategy,\n    \"save_steps\": save_steps,\n    \"save_total_limit\": SAVE_TOTAL_LIMIT or None,\n    \"save_only_model\": SAVE_ONLY_MODEL,\n    \"load_best_model_at_end\": load_best,\n    \"metric_for_best_model\": \"eval_loss\",\n    \"greater_is_better\": False,\n    \"seed\": SEED,\n    \"data_seed\": SEED,\n    \"report_to\": [],\n    \"disable_tqdm\": False,\n}\n_dropped = [k for k in _kw if k not in _sig]\nfor k in _dropped:\n    _kw.pop(k)\nif _dropped:\n    print(\"note: this transformers version has no\", \", \".join(_dropped), \"-- dropped\")\nargs = Args(**_kw)\n\n_callbacks = [EvalLogger()]\nif EVAL_ON_FINAL_STEP:\n    _callbacks.append(FinalStepEval())\nif SNAPSHOT_EPOCH1 and EPOCHS > 1.0:\n    _callbacks.append(Epoch1Snapshot())\nif TIME_BUDGET_MIN:\n    _callbacks.append(TimeBudget(TIME_BUDGET_MIN))\n\n_sess_idx = pd.factorize(tr.session_id)[0] if (PAIR_LOSS_W > 0 or PAIR_SIBLING_BATCH) else None\nif _sess_idx is not None:\n    ds_tr.session_idx = _sess_idx           # Collate picks this up, see Cell 9\ntrainer = LMClassifierTrainer(\n    model=model, args=args, train_dataset=ds_tr, eval_dataset=ds_va,\n    data_collator=coll, compute_metrics=compute_metrics, callbacks=_callbacks,\n    tokenizer_for_save=tok, sess_of_row=(tr.session_id.tolist()\n                                         if PAIR_SIBLING_BATCH else None))\n\n# Guard the gradient-accumulation contract. Trainer skips `loss /= accum_steps` whenever it\n# believes the model normalises the loss itself, which it infers from a **kwargs in\n# forward(). LMClassifier.forward takes num_items_in_batch explicitly and sets\n# accepts_loss_kwargs=False, so the division happens and MAX_GRAD_NORM clips the gradient\n# it is supposed to.\nif hasattr(trainer, \"model_accepts_loss_kwargs\"):\n    assert not trainer.model_accepts_loss_kwargs, (\n        \"Trainer thinks the model normalises its own loss -- gradients would be \"\n        f\"{ACCUM}x too large and MAX_GRAD_NORM would clip the wrong thing.\")\n\nprint(f\"\\n{total_steps} optimiser steps over {EPOCHS} epoch(s) \"\n      f\"(effective batch {BS*ACCUM}, {steps_per_epoch} steps/epoch)\")\nprint(f\"  eval  {eval_strategy}\" + (f\" / {eval_steps} steps = {eval_steps/steps_per_epoch:.2f} \"\n      f\"epoch, ~{total_steps/eval_steps:.0f} evals\" if eval_steps else \"\"))\nprint(f\"  save  {save_strategy}\" + (f\" / {save_steps} steps\" if save_steps else \"\")\n      + f\"   limit {SAVE_TOTAL_LIMIT or 'none'}   load_best {load_best}\")\n\n# ---- the OOF dump ------------------------------------------------------------------------------\ndef dump(tag):\n    \"\"\"Predict the FULL validation fold with the current weights, write OOF + adapter.\n\n    Deliberately ds_oof, not ds_va: training-time eval may have run on an EVAL_SUBSET, and\n    an OOF covering 2,000 of 7,014 rows would silently poison the calibration and any blend\n    fitted on it downstream.\n    \"\"\"\n    pred = trainer.predict(ds_oof, metric_key_prefix=f\"final_{tag}\")\n    z = np.asarray(pred.predictions).reshape(-1).astype(np.float64)\n    # Two ways the OOF can be silently wrong; both are one line to rule out.\n    assert len(z) == len(va_oof), (\n        f\"predict returned {len(z)} of {len(va_oof)} rows -- dataloader_drop_last is \"\n        f\"reaching the predict loader\")\n    assert np.array_equal(np.asarray(pred.label_ids).reshape(-1).astype(int),\n                          va_oof.is_correct.values.astype(int)), (\n        \"predict returned rows in a different order than va_oof -- _get_eval_sampler is \"\n        \"not sequential, so response_id would be paired with the wrong logit\")\n    p = 1/(1+np.exp(-z))\n    ll = float(log_loss(va_oof.is_correct, np.clip(p, 1e-6, 1-1e-6), labels=[0, 1]))\n    auc = float(roc_auc_score(va_oof.is_correct, p)) if va_oof.is_correct.nunique() > 1 else 0.5\n    print(f\"fold {FOLD} [{tag}]: logloss {ll:.5f}  auc {auc:.4f}  on {len(va_oof):,} rows  \"\n          f\"(constant 0.60876; one-fold SE on this split is 0.0054)\")\n    out = {\"logloss\": ll, \"auc\": auc, \"n\": int(len(va_oof))}\n    groups = va_oof.session_id.values if \"session_id\" in va_oof else None\n    if CALIBRATE_OOF and va_oof.is_correct.nunique() > 1:\n        cal = calibration_report(z, va_oof.is_correct.values.astype(float), groups, seed=SEED)\n        print(f\"           calibration: a={cal['a']:.4f} b={cal['b']:+.4f}   \"\n              f\"raw {cal['logloss_raw']:.5f} -> {cal['logloss_calibrated']:.5f}  \"\n              f\"({cal['gain']:+.5f}, out-of-fold\"\n              f\"{', session-grouped' if cal['grouped_inner_cv'] else ''})\")\n        if cal[\"a\"] < 0.85:\n            print(f\"           a={cal['a']:.3f} < 1 -> OVER-confident; a Platt map on \"\n                  f\"POOLED OOF is worth fitting in build_submission.py\")\n        out[\"calibration\"] = cal\n    if BLEND_SCALARS and va_oof.is_correct.nunique() > 1:\n        bl = blend_report(z, va_oof.is_correct.values.astype(float), va_oof, groups, seed=SEED)\n        out[\"blend\"] = bl\n        if \"skipped\" in bl:\n            print(f\"           blend: skipped ({bl['skipped']})\")\n        else:\n            print(f\"           blend {bl['features']}: platt {bl['logloss_platt']:.5f} -> \"\n                  f\"{bl['logloss_blend']:.5f}  ({bl['gain_over_platt']:+.5f} over Platt, \"\n                  f\"cross-fitted). The LLM logit has already read some of these from the \"\n                  f\"prompt, so this is the RESIDUAL, not the feature's standalone value.\")\n    ad = f\"{OUT}/adapter_{tag}\"\n    cols = {\"response_id\": va_oof.response_id, \"logit\": z, \"prob\": p,\n            \"is_correct\": va_oof.is_correct}\n    if OOF_JOIN_KEYS:\n        # E8. Without these the OOF cannot be joined to anything -- no per-objective\n        # analysis, no scalar blend, no session-grouped inner CV downstream.\n        for c in (\"session_id\", \"learning_objective_id\", \"n_resp_in_session\",\n                  \"n_utt\", \"dur_min\", \"stu_words\", \"n_stu_turns\", \"n_long_stu_turns\"):\n            if c in va_oof.columns:\n                cols[c] = va_oof[c].values\n        cols[\"fold\"] = FOLD\n        cols[\"model_tag\"] = _MODEL_TAG\n    pd.DataFrame(cols).to_csv(\n        f\"{OUT}/oof_{_MODEL_TAG}_{tag}_fold{FOLD}_seed{SEED}.csv\", index=False)\n    save_checkpoint(model, tok, ad, dict(CKPT_META, tag=tag, logloss=ll, auc=auc,\n                                         step=int(trainer.state.global_step)))\n    if SHIP_CALIBRATION and \"calibration\" in out:\n        # E17. The map travels INSIDE the adapter directory, so it cannot be paired with\n        # weights it was not fitted for.\n        with open(os.path.join(ad, \"calibration.json\"), \"w\", encoding=\"utf-8\") as fh:\n            json.dump({\"a\": out[\"calibration\"][\"a\"], \"b\": out[\"calibration\"][\"b\"],\n                       \"fitted_on\": f\"fold{FOLD}_seed{SEED}\", \"n\": int(len(va_oof)),\n                       \"note\": \"p = sigmoid(a*logit + b). Refit on POOLED OOF before \"\n                               \"shipping an ensemble.\"}, fh, indent=2)\n    if WRITE_BLEND_JSON and out.get(\"blend\") and \"skipped\" not in out[\"blend\"]:\n        # E28. Emit the transform AND round-trip it here, so build_submission cannot apply\n        # a different one than was fitted.\n        bj = {\"features\": out[\"blend\"][\"features\"], \"mu\": out[\"blend\"][\"mu\"],\n              \"sd\": out[\"blend\"][\"sd\"], \"w\": out[\"blend\"][\"w\"],\n              \"note\": \"z_std = (x - mu)/sd for the scalar columns; \"\n                      \"p = sigmoid([logit, z_std] @ w[:-1] + w[-1])\"}\n        with open(os.path.join(ad, \"blend.json\"), \"w\", encoding=\"utf-8\") as fh:\n            json.dump(bj, fh, indent=2)\n        Xc = np.column_stack([z] + [(va_oof[c].values.astype(float) - m) / s\n                                    for c, m, s in zip(bj[\"features\"], bj[\"mu\"], bj[\"sd\"])])\n        pc = 1/(1+np.exp(-(Xc @ np.array(bj[\"w\"][:-1]) + bj[\"w\"][-1])))\n        assert np.isfinite(pc).all() and ((pc > 0) & (pc < 1)).all(), \"blend round-trip broke\"\n        print(f\"           blend.json round-trip ok (mean p {pc.mean():.4f})\")\n    return out\n\nresume = RESUME\nif resume == \"auto\":\n    _ck = sorted(glob.glob(f\"{OUT}/checkpoint-*\"),\n                 key=lambda p: int(p.rsplit(\"-\", 1)[-1]) if p.rsplit(\"-\", 1)[-1].isdigit() else -1)\n    resume = _ck[-1] if _ck else None\n    print(f\"RESUME='auto' -> {resume or 'no checkpoint found, starting fresh'}\")\nelif not isinstance(resume, str):\n    resume = resume or None\n\ntrain_out = trainer.train(resume_from_checkpoint=resume)\n\nprint()\nstats = {\"final\": dump(\"final\")}\nstats[\"best\"] = {\"eval_loss\": trainer.state.best_metric,\n                 \"checkpoint\": trainer.state.best_model_checkpoint}\nif PAIR_LOSS_W > 0:\n    print(f\"pairwise term: {trainer.pair_seen:,} discordant sibling pairs seen across \"\n          f\"training ({trainer.pair_seen/max(1,total_steps):.1f} per step). Of 5,323 such \"\n          f\"pairs in the full data, a batch-local term only sees those that co-occur\"\n          + (\"\" if PAIR_SIBLING_BATCH else \" -- turn on PAIR_SIBLING_BATCH or this is inert\"))\n\n# ---- E32: average the last k adapters in delta-weight space -----------------------------------\nif SWA_LAST_K > 1:\n    from safetensors.torch import load_file, save_file\n    cks = sorted(glob.glob(f\"{OUT}/checkpoint-*\"),\n                 key=lambda p: int(p.rsplit(\"-\", 1)[-1]))[-SWA_LAST_K:]\n    if len(cks) < 2:\n        print(f\"SWA: only {len(cks)} checkpoint(s) on disk (SAVE_TOTAL_LIMIT={SAVE_TOTAL_LIMIT}) \"\n              f\"-- raise it to keep {SWA_LAST_K}\")\n    else:\n        # B_avg @ A_avg != mean(B_i A_i). Stacking is exact: concatenate A along its OUTPUT\n        # dim and B along its INPUT dim, then B_stack @ A_stack = sum_i B_i A_i. Dividing B\n        # by k gives the mean, and r' = k*r with alpha' = k*alpha keeps PEFT's alpha/r\n        # scale unchanged.\n        sds = [load_file(os.path.join(c, \"adapter_model.safetensors\")) for c in cks]\n        merged = {}\n        for k_ in sds[0]:\n            ts = [sd[k_] for sd in sds]\n            if \"lora_A\" in k_:\n                merged[k_] = torch.cat(ts, dim=0)\n            elif \"lora_B\" in k_:\n                merged[k_] = torch.cat(ts, dim=1) / len(ts)\n            else:\n                merged[k_] = torch.stack(ts).mean(0)\n        outd = f\"{OUT}/adapter_swa{len(cks)}\"\n        os.makedirs(outd, exist_ok=True)\n        for f in os.listdir(cks[-1]):\n            if f not in (\"adapter_model.safetensors\",):\n                try: shutil.copy(os.path.join(cks[-1], f), os.path.join(outd, f))\n                except (IsADirectoryError, PermissionError): pass\n        save_file(merged, os.path.join(outd, \"adapter_model.safetensors\"))\n        cfgp = os.path.join(outd, \"adapter_config.json\")\n        cfg = json.load(open(cfgp, encoding=\"utf-8\"))\n        cfg[\"r\"], cfg[\"lora_alpha\"] = cfg[\"r\"]*len(cks), cfg[\"lora_alpha\"]*len(cks)\n        json.dump(cfg, open(cfgp, \"w\", encoding=\"utf-8\"), indent=2)\n        hs = [torch.load(os.path.join(c, \"head.pt\"), map_location=\"cpu\") for c in cks\n              if os.path.exists(os.path.join(c, \"head.pt\"))]\n        if hs:\n            torch.save({k_: torch.stack([h[k_].float() for h in hs]).mean(0)\n                        for k_ in hs[0]}, os.path.join(outd, \"head.pt\"))\n        print(f\"SWA: averaged {len(cks)} adapters -> {outd}  (r {cfg['r']}, \"\n              f\"alpha {cfg['lora_alpha']}). Load it with load_checkpoint and score the OOF; \"\n              f\"ship only if compare_oof puts the paired CI on its side.\")\n\ndef _ver(mod):\n    try:\n        return getattr(importlib.import_module(mod), \"__version__\", \"?\")\n    except Exception:\n        return \"absent\"\n\njson.dump({\"base_model\": MODEL, \"model_tag\": _MODEL_TAG, \"max_len\": MAX_LEN,\n           \"fold\": FOLD, \"seed\": SEED, \"proxy_mode\": PROXY_MODE,\n           # Provenance. Without this a prompt A/B is unauditable: two runs either side of\n           # a render change produce meta files that differ only in the results block.\n           \"render\": dict(RENDER_KW, add_session_meta=ADD_SESSION_META,\n                          prepend_specials=PREPEND_SPECIALS, render_key=_CTX_KEY,\n                          parquet=PARQUET),\n           \"optim\": {\"lr\": LR, \"head_lr\": HEAD_LR, \"weight_decay\": WEIGHT_DECAY,\n                     \"warmup_ratio\": WARMUP_RATIO, \"max_grad_norm\": MAX_GRAD_NORM,\n                     \"bs\": BS, \"accum\": ACCUM, \"effective_batch\": BS*ACCUM,\n                     \"epochs\": EPOCHS, \"steps_per_epoch\": steps_per_epoch,\n                     \"total_steps\": total_steps, \"scheduler\": \"cosine\",\n                     \"grad_ckpt\": GRAD_CKPT, \"pair_loss_w\": PAIR_LOSS_W,\n                     \"pair_sibling_batch\": PAIR_SIBLING_BATCH,\n                     \"time_budget_min\": TIME_BUDGET_MIN},\n           \"lora\": {\"r\": LORA_R, \"alpha\": LORA_ALPHA, \"scale\": LORA_ALPHA/LORA_R,\n                    \"dropout\": DROPOUT, \"use_4bit\": USE_4BIT, \"top_n\": LORA_TOP_N,\n                    \"targets\": targets, \"head_bias_init\": HEAD_BIAS_INIT, \"pool\": POOL,\n                    \"use_chat_template\": USE_CHAT_TEMPLATE, \"swa_last_k\": SWA_LAST_K},\n           \"tokens\": {\"mean_tokens_measured\": globals().get(\"MEAN_TOKENS\"),\n                      \"proxy_chars_per_token\": 3.6},\n           \"env\": {m: _ver(m) for m in (\"torch\", \"transformers\", \"peft\", \"accelerate\",\n                                        \"sklearn\", \"bitsandbytes\", \"numpy\", \"pandas\")}\n                  | {\"python\": platform.python_version(),\n                     \"gpu\": torch.cuda.get_device_name(0) if torch.cuda.is_available() else \"cpu\"},\n           \"cv\": {\"scheme\": CV_SCHEME, \"fold_col\": FOLD_COL, \"n_splits\": N_SPLITS,\n                  \"purge\": USE_PURGE, \"splitter\": SCHEMES[CV_SCHEME][\"desc\"],\n                  \"expect_session_leak\": EXPECT_SESSION_LEAK,\n                  \"expect_objective_leak\": EXPECT_OBJECTIVE_LEAK},\n           \"n_train\": len(tr), \"n_val\": len(ds_oof), \"n_eval\": len(ds_va),\n           \"eval_bs\": EVAL_BS, \"eval_subset\": EVAL_SUBSET,\n           \"group_by_length\": GROUP_BY_LENGTH, \"sort_eval_by_length\": SORT_EVAL_BY_LENGTH,\n           \"eval\": {\"strategy\": eval_strategy, \"steps\": eval_steps,\n                    \"every_epochs\": EVAL_EVERY, \"final_step_eval\": EVAL_ON_FINAL_STEP},\n           \"save\": {\"strategy\": save_strategy, \"steps\": save_steps},\n           \"load_best\": load_best,\n           \"train_runtime_s\": train_out.metrics.get(\"train_runtime\"),\n           \"results\": stats,\n           \"log_history\": trainer.state.log_history},\n          open(f\"{OUT}/meta_fold{FOLD}_seed{SEED}.json\", \"w\"), indent=2, default=str)\nprint(\"\\n\", {k: v for k, v in stats.items() if k != \"best\"})\n\n# ---- E15: was epoch 1 enough? -------------------------------------------------------------\n_e1 = f\"{OUT}/oof_{_MODEL_TAG}_epoch1_fold{FOLD}_seed{SEED}.csv\"\n_fin = f\"{OUT}/oof_{_MODEL_TAG}_final_fold{FOLD}_seed{SEED}.csv\"\nif SNAPSHOT_EPOCH1 and os.path.exists(_e1):\n    print(f\"\\nepoch 1 vs final (paired, same rows) -- did epochs 2-{EPOCHS:.0f} earn their \"\n          f\"{100*(1-1/EPOCHS):.0f}% of the budget?\")\n    compare_oof(_e1, _fin)\n\nif COMPARE_OOF:\n    print(f\"\\npaired comparison vs {COMPARE_OOF}\")\n    compare_oof(_fin, COMPARE_OOF)\n","metadata":{"_uuid":"14b6fdbb-d8c8-436f-bb25-7b92a8de5d12","_cell_guid":"19c8cf05-397c-423b-8076-fea24c7c6b81","trusted":true,"collapsed":false,"jupyter":{"outputs_hidden":false},"execution":{"iopub.status.busy":"2026-08-13T11:54:03.943186Z","iopub.execute_input":"2026-08-13T11:54:03.943315Z","execution_failed":"2026-08-13T11:58:20.619Z"}},"outputs":[],"execution_count":null},{"cell_type":"code","source":"# =========================================================\n# Cell 15 - INFERENCE CONTRACT\n# inlined for build_submission.py -- edit here, then port back\n# =========================================================\n\"\"\"\nEverything the submission path needs that training already got right, plus the guards that\nonly matter once. Nothing here runs during training: the cell defines functions and then\nself-tests them on CPU with fake logits, so a bug surfaces now rather than inside the\n6-hour submission window.\n\nThe failure modes this exists to prevent, in order of how expensive they are:\n\n  1. A PERMUTED SUBMISSION. Length-sorted batching reorders 10,508 rows. If the order is\n     not restored, the file still has the right shape, the right row count and the right\n     value range -- and scores +0.117 worse. There is no symptom. `predict_frame` never\n     indexes positionally and `order_canary` re-scores a sample unsorted to prove it.\n  2. A CRASH BEFORE ANY FILE EXISTS. The one irreversible failure here: the window closes\n     and there is nothing to submit. Every per-row operation is wrapped, falls back to the\n     base rate, and counts what it caught. A submission that is 0.5% fallback beats no\n     submission by an unbounded margin.\n  3. RENDER DRIFT. The checkpoint carries frozen fixtures; scoring refuses to start if this\n     process renders differently than training did.\n  4. A WRONG n_resp_in_session. ADD_SESSION_META puts that number in the prompt, so the\n     model was trained to trust it. If the test feature table does not list every row of a\n     session, the number is wrong on every row of that session.\n\"\"\"\nimport json, os, time\nimport numpy as np\nimport pandas as pd\n\nBASE_RATE_FALLBACK = 0.70247      # global train base rate; the honest prior for a dead row\n\n\n# ---- E14: is n_resp_in_session trustworthy on this table? ----------------------------------\ndef session_meta_ok(features: pd.DataFrame) -> tuple:\n    \"\"\"(n_resp_by_session, is_trustworthy).\n\n    Train is self-consistent by construction -- Cell 7 asserts it. Test is not verifiable\n    from inside, so the checks are structural: every session must appear with a contiguous\n    block of rows and the count must be plausible against the training distribution\n    (1..10 objectives; 63.3% of sessions carry exactly one).\n    \"\"\"\n    n_by = features.groupby(\"session_id\").size()\n    reasons = []\n    if n_by.max() > 10:\n        reasons.append(f\"a session lists {n_by.max()} objectives; train never exceeded 10\")\n    if len(features) and features.session_id.duplicated().sum() and \\\n            not features.session_id.is_monotonic_increasing:\n        # not fatal, just worth knowing the table is interleaved rather than grouped\n        pass\n    ok = not reasons\n    if not ok:\n        print(\"  *** session-meta looks WRONG: \" + \"; \".join(reasons))\n        print(\"  *** falling back to n_resp_in_session=None (the footer line is omitted). \"\n              \"That is a train/inference mismatch and costs score, but it is smaller than \"\n              \"stating a number the model was trained to believe and that is false.\")\n    else:\n        print(f\"  session-meta ok: {len(n_by):,} sessions, max {n_by.max()} objectives, \"\n              f\"{100*(n_by == 1).mean():.1f}% single-objective\")\n    return n_by, ok\n\n\n# ---- E5 + E19: render a test frame, never raising -------------------------------------------\ndef render_frame(features: pd.DataFrame, transcript_dir: str, adapter_dir: str = None):\n    \"\"\"(texts, fallback_mask). A row that cannot be rendered gets an empty text and True.\"\"\"\n    if adapter_dir and RENDER_FIXTURES:\n        fx = os.path.join(adapter_dir, RENDER_FIXTURE_FILE)\n        if os.path.exists(fx):\n            verify_render_fixtures(fx)      # raises on drift -- BEFORE any GPU time\n        else:\n            print(\"  note: checkpoint carries no render fixtures; drift is unguarded\")\n    n_by, meta_ok = session_meta_ok(features)\n    texts = [None] * len(features)\n    bad = np.zeros(len(features), dtype=bool)\n    pos = {rid: i for i, rid in enumerate(features.response_id)}\n    for sid, g in features.groupby(\"session_id\", sort=False):\n        try:\n            t = pd.read_csv(os.path.join(transcript_dir, f\"{sid}.csv\"),\n                            dtype=str, keep_default_na=False)\n            roles, contents = t.role.tolist(), t.content.tolist()\n            stamps = t.timestamp.tolist() if \"timestamp\" in t.columns else [\"\"] * len(t)\n            assert len(contents), \"empty transcript\"\n        except Exception as e:\n            if not INFER_SAFE_FALLBACK:\n                raise\n            for rid in g.response_id:\n                bad[pos[rid]] = True; texts[pos[rid]] = \"\"\n            print(f\"  fallback: session {sid} unreadable ({type(e).__name__}: {e})\")\n            continue\n        nr = int(n_by.get(sid, len(g))) if meta_ok else None\n        for _, row in g.iterrows():\n            i = pos[row.response_id]\n            try:\n                texts[i] = render_row(roles, contents, stamps, row.learning_objective, nr)\n            except Exception as e:\n                if not INFER_SAFE_FALLBACK:\n                    raise\n                bad[i] = True; texts[i] = \"\"\n                print(f\"  fallback: response {row.response_id} render failed \"\n                      f\"({type(e).__name__}: {e})\")\n    if bad.any():\n        print(f\"  *** {bad.sum():,} of {len(features):,} rows ({100*bad.mean():.2f}%) fell \"\n              f\"back to the base rate ***\")\n    return texts, bad\n\n\n# ---- E9 + E1: score, length-sorted, order restored by KEY not by position -------------------\ndef predict_frame(model, tok, features: pd.DataFrame, texts, device=\"cuda\",\n                  batch_size=None, sort_by_length=None, max_len=None,\n                  adapter_dir_for_meta=None):\n    \"\"\"response_id -> logit. Returns a DataFrame; the caller MERGES, never concatenates.\n\n    Length-sorting is worth ~19.7% at EVAL_BS=4 because padding is charged per batch. The\n    entire risk of that win is losing the row order, so this function never returns a bare\n    array: it returns response_id alongside the logit and the caller joins on it.\n    \"\"\"\n    import torch\n    bs = batch_size or EVAL_BS\n    sort_by_length = INFER_SORT_BY_LENGTH if sort_by_length is None else sort_by_length\n    max_len = max_len or MAX_LEN\n    ids = list(features.response_id)\n    order = np.arange(len(texts))\n    if sort_by_length:\n        order = np.argsort(-np.array([len(t) for t in texts]), kind=\"stable\")\n    coll_ = Collate(tok.pad_token_id)\n    # The readout position is a property of the CHECKPOINT, not of this process. Reading it\n    # back is the same contract as the head width in load_checkpoint.\n    _chat = USE_CHAT_TEMPLATE\n    if adapter_dir_for_meta:\n        _cj = os.path.join(adapter_dir_for_meta, \"checkpoint.json\")\n        if os.path.exists(_cj):\n            _chat = json.load(open(_cj, encoding=\"utf-8\")).get(\n                \"use_chat_template\", USE_CHAT_TEMPLATE)\n            if _chat != USE_CHAT_TEMPLATE:\n                print(f\"  note: checkpoint says use_chat_template={_chat}, this process \"\n                      f\"has {USE_CHAT_TEMPLATE} -- following the CHECKPOINT\")\n    ds = Rows([texts[i] for i in order], np.zeros(len(order)), tok, max_len,\n              PREPEND_SPECIALS, use_chat_template=_chat)\n    logits = np.empty(len(order), dtype=np.float64)\n    model.eval()\n    t0 = time.time()\n    with torch.no_grad():\n        for s in range(0, len(ds), bs):\n            batch = coll_([ds[j] for j in range(s, min(s + bs, len(ds)))])\n            batch = {k: v.to(device) for k, v in batch.items() if k != \"labels\"}\n            out = model(**batch)\n            logits[s:s + len(out.logits)] = out.logits.float().cpu().numpy().reshape(-1)\n            if s and s % (bs * 200) == 0:\n                el = time.time() - t0\n                print(f\"    {s:,}/{len(ds):,}  {el/60:.1f}m elapsed, \"\n                      f\"~{el/s*(len(ds)-s)/60:.0f}m left\", flush=True)\n    unsorted = np.empty(len(order), dtype=np.float64)\n    unsorted[order] = logits            # undo the sort in INDEX space, once, here\n    return pd.DataFrame({\"response_id\": ids, \"logit\": unsorted})\n\n\ndef order_canary(model, tok, features, texts, pred, device=\"cuda\", n=64, tol=1e-2,\n                 adapter_dir_for_meta=None):\n    \"\"\"E1. Re-score a sample UNSORTED at batch_size=1 and require the logits to match.\n\n    This is the only test that can catch a permutation, because a permuted submission is\n    valid in every other respect. Prints max|delta| unconditionally so drift is visible\n    even when it passes.\n    \"\"\"\n    if not INFER_ORDER_CANARY or not len(features):\n        return\n    rng = np.random.default_rng(0)\n    pick = rng.choice(len(features), size=min(n, len(features)), replace=False)\n    sub = features.iloc[pick].reset_index(drop=True)\n    ref = predict_frame(model, tok, sub, [texts[i] for i in pick], device,\n                        batch_size=1, sort_by_length=False,\n                        adapter_dir_for_meta=adapter_dir_for_meta)\n    m = ref.merge(pred, on=\"response_id\", suffixes=(\"_ref\", \"_got\"))\n    assert len(m) == len(sub), \"canary rows missing from the prediction frame\"\n    d = float(np.abs(m.logit_ref - m.logit_got).max())\n    print(f\"  order canary: {len(m)} rows re-scored unsorted, max|delta| {d:.2e}\")\n    assert d < tol, (\n        f\"ORDER/BATCHING MISMATCH: max|delta| {d:.2e} > {tol}. Either the sort was not \"\n        f\"undone (a permuted submission costs ~+0.117 and has no other symptom) or \"\n        f\"batching changes the logits, which means padding is leaking into the pooling.\")\n\n\n# ---- E17 + E28: apply the shipped transforms -------------------------------------------------\ndef apply_transforms(logit: np.ndarray, adapter_dir: str, frame: pd.DataFrame = None):\n    \"\"\"logit -> probability, using whatever the checkpoint shipped.\n\n    Order matters and is fixed: a blend REPLACES the Platt map (it was fitted on raw logits\n    plus scalars and already carries its own intercept), it does not compose with it.\n    \"\"\"\n    z = np.asarray(logit, dtype=np.float64)\n    bj = os.path.join(adapter_dir, \"blend.json\")\n    if os.path.exists(bj) and frame is not None:\n        b = json.load(open(bj, encoding=\"utf-8\"))\n        if all(c in frame.columns for c in b[\"features\"]):\n            X = np.column_stack([z] + [(frame[c].values.astype(float) - m) / s\n                                       for c, m, s in zip(b[\"features\"], b[\"mu\"], b[\"sd\"])])\n            print(f\"  applying blend.json on {b['features']}\")\n            return 1/(1+np.exp(-(X @ np.array(b[\"w\"][:-1]) + b[\"w\"][-1])))\n        print(f\"  blend.json needs {b['features']} but the test frame lacks some -- \"\n              f\"falling back to the Platt map\")\n    ab = load_calibration(adapter_dir)\n    if ab:\n        a, b_ = ab\n        print(f\"  applying calibration.json  a={a:.4f} b={b_:+.4f}\")\n        return 1/(1+np.exp(-(a*z + b_)))\n    print(\"  no calibration shipped -- raw sigmoid\")\n    return 1/(1+np.exp(-z))\n\n\n# ---- E11 + E1: write the file ------------------------------------------------------------------\ndef write_submission(sub_format: pd.DataFrame, pred: pd.DataFrame, path: str,\n                     fallback_mask=None, clip=None):\n    \"\"\"Join on response_id, clip, assert, write.\n\n    The join is the contract: `sub_format` defines the required rows and their order, and a\n    left merge with validate=\"one_to_one\" turns a missing or duplicated prediction into an\n    exception instead of a silently misaligned column.\n    \"\"\"\n    clip = SUBMIT_CLIP if clip is None else clip\n    out = sub_format[[\"response_id\"]].merge(pred, on=\"response_id\", how=\"left\",\n                                            validate=\"one_to_one\")\n    if fallback_mask is not None and fallback_mask.any():\n        fb = set(pred.response_id[fallback_mask])\n        out.loc[out.response_id.isin(fb), \"probability\"] = BASE_RATE_FALLBACK\n    miss = out.probability.isna()\n    if miss.any():\n        print(f\"  *** {miss.sum():,} rows had no prediction -- base rate ***\")\n        out.loc[miss, \"probability\"] = BASE_RATE_FALLBACK\n    if clip:\n        # A single row at p=1e-6 with y=1... costs nothing; at p=1e-6 with y=0 it costs\n        # 13.8/N. Clipping to [0.01, 0.99] caps one broken row at 4.6/N instead.\n        out[\"probability\"] = out.probability.clip(clip, 1 - clip)\n    assert len(out) == len(sub_format), \"row count changed\"\n    assert out.response_id.tolist() == sub_format.response_id.tolist(), \\\n        \"ORDER CHANGED vs the submission format -- never index positionally\"\n    assert np.isfinite(out.probability).all(), \"non-finite probability\"\n    assert ((out.probability > 0) & (out.probability < 1)).all(), \"probability out of range\"\n    out.to_csv(path, index=False)\n    print(f\"  wrote {path}: {len(out):,} rows, p in \"\n          f\"[{out.probability.min():.4f}, {out.probability.max():.4f}], \"\n          f\"mean {out.probability.mean():.4f}\")\n    return out\n\n\n# ---- E7 + E6: cost before you commit -------------------------------------------------------\ndef dry_run_estimate(n_scored: float, seconds: float, n_total: int):\n    \"\"\"Extrapolate the full pass from a DRY_RUN_N sample, against the 6-hour cap.\"\"\"\n    per = seconds / max(n_scored, 1)\n    total = per * n_total / 60.0\n    print(f\"  DRY RUN: {n_scored:,.0f} rows in {seconds/60:.1f}m -> {per:.3f}s/row -> \"\n          f\"{total:.0f}m for {n_total:,} rows\")\n    print(f\"  vs {SAFE_BUDGET_MIN}m planning cap / {INFER_BUDGET_MIN}m hard cap: \"\n          + (\"FITS\" if total <= SAFE_BUDGET_MIN else\n             \"TIGHT\" if total <= INFER_BUDGET_MIN else \"OVER BUDGET\"))\n    return total\n\n\n# ---- self-test on CPU, with fake logits -------------------------------------------------------\n# Cheap proof that the join/clip/assert path does what it claims, run every time this cell\n# executes so it cannot rot. No model, no GPU.\n_sf = pd.DataFrame({\"response_id\": [f\"r{i}\" for i in range(10)], \"probability\": 0.5})\n_pr = pd.DataFrame({\"response_id\": [f\"r{i}\" for i in range(10)][::-1],   # deliberately reversed\n                    \"logit\": np.linspace(-3, 3, 10)[::-1]})\n_pr[\"probability\"] = 1/(1+np.exp(-_pr.logit))\nimport tempfile\nwith tempfile.TemporaryDirectory() as _td:\n    _o = write_submission(_sf, _pr[[\"response_id\", \"probability\"]],\n                          os.path.join(_td, \"_selftest_sub.csv\"), clip=0.01)\nassert abs(_o.probability.iloc[0] - 1/(1+np.exp(3))) < 1e-9, \\\n    \"write_submission did not realign a reversed prediction frame\"\nassert _o.probability.min() >= 0.01 and _o.probability.max() <= 0.99, \"clip not applied\"\nprint(\"inference contract: self-test passed \"\n      \"(reversed frame realigned by key, clip applied, asserts live)\")\nif RENDER_FIXTURES:\n    verify_render_fixtures(make_render_fixtures())\nprint(f\"flags: sort={INFER_SORT_BY_LENGTH} canary={INFER_ORDER_CANARY} \"\n      f\"fallback={INFER_SAFE_FALLBACK} clip={SUBMIT_CLIP} dry_run={DRY_RUN_N or 'off'}\")\n","metadata":{"trusted":true,"execution":{"execution_failed":"2026-08-13T11:58:20.62Z"}},"outputs":[],"execution_count":null},{"cell_type":"code","source":"# (free cell -- was junk: \"#jghg hghhghgh\")\n","metadata":{"trusted":true,"execution":{"execution_failed":"2026-08-13T11:58:20.62Z"}},"outputs":[],"execution_count":null},{"cell_type":"code","source":"# =========================================================\n# Cell 12 - save the artifacts\n# =========================================================\nimport os, json, zipfile\n\nprint(\"run directory:\")\nfor f in sorted(os.listdir(OUT)):\n    p = os.path.join(OUT, f)\n    n = os.path.getsize(p) if os.path.isfile(p) else sum(\n        os.path.getsize(os.path.join(r, x)) for r, _, xs in os.walk(p) for x in xs)\n    print(f\"   {f:28s} {n/1e6:8.1f} MB\")\n\n# Trainer's checkpoint-*/ dirs carry optimizer.pt, scheduler.pt and rng_state.pth -- the\n# resume machinery, hundreds of MB, useless once the run is done. Only the adapters,\n# the OOF predictions and the metadata need to leave the box.\nSKIP_FILES = {\"optimizer.pt\", \"scheduler.pt\", \"rng_state.pth\", \"trainer_state.json\",\n              \"training_args.bin\", \"scaler.pt\"}\nbase = os.path.basename(OUT)\nzpath = f\"/kaggle/working/{base}.zip\"\nwith zipfile.ZipFile(zpath, \"w\", zipfile.ZIP_DEFLATED, compresslevel=6) as z:\n    for r, _, fs in os.walk(OUT):\n        for f in fs:\n            if f in SKIP_FILES:\n                continue\n            p = os.path.join(r, f)\n            z.write(p, os.path.relpath(p, os.path.dirname(OUT)))\nprint(f\"\\nzipped -> {zpath}  ({os.path.getsize(zpath)/1e6:.0f} MB, resume state excluded)\")\nprint(\"\\n\", json.dumps(json.load(open(f\"{OUT}/meta_fold{FOLD}_seed{SEED}.json\")), indent=2)[:800])","metadata":{"_uuid":"631d56e3-81d9-4d8f-9b46-cf0c4fd31787","_cell_guid":"0d4d11fc-26af-4b9d-8985-f08f9e9d471f","trusted":true,"collapsed":false,"jupyter":{"outputs_hidden":false},"execution":{"execution_failed":"2026-08-13T11:58:20.62Z"}},"outputs":[],"execution_count":null}]}