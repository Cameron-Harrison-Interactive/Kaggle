# Episode 91477855 — our seat1 $141,264 vs opp seat0 $19,871

Opp = melon-only NW bot (no animals, no land). Adaptive plant mix looked different
because tomato hedge / market context differed — backbone tape still drove routing.

## Issues observed
1. **SW empty strips** after d10 unlock: tape plants most of SW but leaves some
   interior cells bare for stretches. This is **tape choreography**, not a bug to
   fix with plant-on-empty (that overlay broke meta and keep-gate hard).

2. **NW/SW corner weeds from our own crops** (d13–d15 especially):
   - (0,0)(1,0) wheat: planted, watered once, then 2 dry days → WEED d13
   - (0,7)(0,8)(0,9) SW strawberry: planted d10, missed water d11–d12 → WEED d13
   - (1,8)(2,8)(4,9) similar d15
   Cause: `consecutive_unwatered` hit 2. Tape water path does not revisit far
   corners every day once labor is stretched across 3 quads. NOT pathing into
   locked tiles — pure coverage shortfall.

## Fixes in v16.3_StrictRoute
- **Removed** `_empty_plant_optimizer` entirely (user: routing must stay strict).
- **Restored** exact v15 `_water_optimizer` (experimental water rewrites −$30k–$85k).
- **PASS on WEED → DIG** only (clears dead corners so tape can reuse tiles).
- **PASS on animal+fert → COLLECT** kept.
- Adaptive memory/classifier retained; no plant-path invention.

## What still needs a careful future fix
Corner water coverage under 3-quad load. Any fix must be keep-gate tested and
must not rewrite MOVE routes into multi-step water paths (destroyed the bot).
